# Quick Task 260613-1cb: Loosen audit-safe auto-approval criteria

## Goal

Allow small-to-medium autonomous audit-safe PRs to pass the policy gate while preserving manual-only handling for sensitive areas.

## Tasks

- Update `.github/workflows/policy.json` so `auditSafe` allows up to 10 files and 400 changed lines, and broadens safe paths to bounded source areas.
- Keep sensitive path overrides manual-only, including workflows, planning, Prisma/generated code, package manifests, API routes, auth, sync, HMAC, Prisma client, panel/server/user/config libraries, middleware, and sensitive types.
- Update workflow documentation to describe the new thresholds and exclusions.
- Add focused policy tests covering relaxed limits, sensitive-path denials, missing file metadata, and unknown line counts.

## Verification

- `node --test .github/workflows/scripts/__tests__/evaluate-pr-policy.test.cjs`
- `npm test`
- `npm run lint`
- `npx tsc --noEmit`
- `npx prettier --check .github/workflows/policy.json .github/workflows/documentation.md .github/workflows/scripts/__tests__/evaluate-pr-policy.test.cjs`
- `actionlint -config-file .github/actionlint.yaml`
- `git diff --check`
