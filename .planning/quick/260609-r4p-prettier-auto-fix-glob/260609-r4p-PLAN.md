---
status: complete
---

# Prettier Auto-Fix Recursive Glob

Fix the shared CI auto-fix formatter path so recursive Prettier globs are passed through to Prettier instead of being expanded shallowly by Bash.

## Tasks

- Confirm the repeated formatter-only CI failures from June 8 and isolate the shared root cause in `.github/actions/prettier-auto-fix`.
- Patch the shared action so deeper `src/**` files are formatted during auto-fix runs.
- Verify the touched workflow/action files with targeted Prettier and `actionlint`, then publish the fix as a PR.
