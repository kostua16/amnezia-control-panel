# Quick Plan: PR #837 workflow improvement intake

## Source
- PR #837: fix: resolve #834 - AFX-I04: Trim the plugin list to the audit mission (audit-fix)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/837

## Summary
PR #837 trims audit-fix.yml from 9 to 4 mission-relevant plugins, removing security-guidance, code-simplifier, frontend-design, superpowers, and caveman. The same 9-plugin bloat persists in 4 other workflows, and 2 workflows have orphaned caveman marketplace references. Follow-up should propagate the trim and prevent future drift.

## Quick Wins
- Remove orphaned caveman marketplace from audit-auto-prs.yml and code-review.yml -- audit-auto-prs.yml:85 and code-review.yml:154 reference the juliusbrussee/caveman.git marketplace but neither lists caveman@caveman in their plugins block — unnecessary external fetch adding startup latency. (owner: claude[bot], type: quick-task)
- Apply AFX-I04 plugin trim to _auto-fix-ci.yml (9 → 4 plugins) -- _auto-fix-ci.yml:225-234 carries the identical 9-plugin bloat that caused audit-fix.yml timeouts (AFX-I01). This workflow also fixes code issues under fleet concurrency pressure — frontend-design, superpowers, and caveman are audit-irrelevant here too. (owner: claude[bot], type: quick-task)
- Apply AFX-I04 plugin trim to fix-issue.yml (9 → mission-relevant subset) -- fix-issue.yml:176-185 carries the same 9-plugin list. Issue-fixing is code-centric — frontend-design, superpowers, caveman add resolution overhead without contributing to the /gsd:debug fix mission. Trim to typescript-lsp, serena, context7, code-review (plus security-guidance if fix touches auth/routes). (owner: claude[bot], type: quick-task)

## pr837.x Phase Candidates
- pr837.2: Centralize per-workflow plugin manifests to prevent AFX-I04-class drift -- PR #837 trimmed audit-fix.yml but left _auto-fix-ci.yml, claude.yml, gsd-planning-execute.yml, and fix-issue.yml with the old 9-plugin list. Extract plugin + marketplace config into a .github/actions/select-plugins composite action (or YAML anchors in a shared file) so future trims propagate automatically rather than requiring per-workfile edits across 5+ files. (owner: claude[bot])
- pr837.2: Add CI guard that validates plugin-marketplace consistency across workflows -- Two workflows (audit-auto-prs.yml, code-review.yml) reference the caveman marketplace without using any caveman plugin — an orphan that would be caught by a lint rule checking: every marketplace URL must resolve to at least one @marketplace plugin in the same block. Extend the existing e2e suite (scripts/__tests__/*.test.cjs) with a plugin-hygiene assertion. (owner: claude[bot])
- pr837.1: Plugin budget tracking in workflow-e2e-scenarios.md and policy.json -- The AFX-I04 rationale (9 plugins → 45min timeout starvation) is documented only in a YAML comment. Add a plugin_budget field to policy.json per workflow so fleet-back-pressure and timeout tuning can reference a declarative constraint instead of an inline comment that drifts out of sync. (owner: claude[bot])
