# Quick Plan: PR #768 workflow improvement intake

## Source
- PR #768: fix: resolve #766 - WHO-I03: Make 50KB 'runs_data' truncation JSON-safe (workflow-health-optimize)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/768

## Summary
PR #768 fixes WHO-I03 by making 50KB truncation JSON-safe in workflow-health-optimize.yml (drop whole entries from array instead of mid-string substring). Analysis found the same JSON-unsafe truncation bug in issue-catch-up.yml and identified the pattern as a candidate for shared utility extraction.

## Quick Wins
- Fix same JSON-unsafe truncation in issue-catch-up.yml -- issue-catch-up.yml:402-404 uses identical json.substring(0, 51200) + '...[truncated]' pattern that WHO-I03 fixed in workflow-health-optimize.yml. The 'categorized' output is injected into a Claude agent prompt as JSON (line 461), so invalid JSON will cause parse failures. Apply the same drop-entries-while-oversized loop. (owner: claude, type: quick-task)
- Add unit test for JSON-safe truncation helper -- The new while-loop truncation in workflow-health-optimize.yml:225-232 has no test coverage. A small test case verifying the output is valid JSON and under 51200 bytes when input exceeds the limit would prevent regressions of WHO-I03. (owner: claude, type: quick-task)

## pr768.x Phase Candidates
- pr768.2: Extract shared JSON-safe truncation utility for workflow outputs -- Three workflows (workflow-health-optimize, issue-catch-up, and potentially others) use the 51200-byte output truncation pattern. Extract to .github/workflows/scripts/truncate-output.cjs exporting a function like truncateJsonOutput(data, maxBytes=51200) that drops trailing entries and returns valid JSON. Centralizing prevents copy-paste divergence and ensures all workflows adopt JSON-safe truncation. (owner: claude)
