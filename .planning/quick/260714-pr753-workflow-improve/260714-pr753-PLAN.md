# Quick Plan: PR #753 workflow improvement intake

## Source
- PR #753: fix: resolve #749 - APR-I05: Fix the empty "Auto PR audit report" evidence section in PR bodies (audit-auto-prs)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/753

## Summary
PR #753 removes duplicate claude-last-output from audit-auto-prs.yml evidence section. Fix is correct — evidence already carries the same value. Three follow-ups: mark TODO resolved, audit other workflows for same duplication pattern, and add empty-rationale guard to shared action.

## Quick Wins
- Mark APR-I05 resolved in docs/TODOs-2.md -- docs/TODOs-2.md:130 lists APR-I05 as P2/open. PR #753 resolves it. Update status column from 'S' to 'D' (done) and add merged PR reference. (owner: docs, type: quick-task)
- Audit build-automation-pr-body callers for evidence + claude-last-output duplication -- 8 workflows pass claude-last-output (grep confirmed). Only audit-auto-prs had evidence duplication, but a quick scan of _auto-fix-ci.yml:304-314, fix-issue.yml:268-274, and suggest-improvements.yml:162-169 for the same evidence+claude-last-output overlap would prevent recurrence. (owner: workflows, type: quick-task)
- Add empty-rationale guard in build-automation-pr-body.cjs -- build-automation-pr-body.cjs:274-279 falls through 4 rationale fallbacks to a generic message. When claudeLastOutput is omitted (as after PR #753) and structured output has no rationale key, the 'Why Automation Changed This' section renders a boilerplate message with no signal. Log a warning or add a distinct fallback message like 'No structured rationale captured — see Evidence section for audit details.' (owner: workflows, type: quick-task)

## pr753.x Phase Candidates
- pr753.1: Systematic build-automation-pr-body input contract audit -- build-automation-pr-body is a shared action used by 8+ workflows. Each caller passes a different subset of 18 inputs with no schema validation. The APR-I05 bug (duplicate input causing empty PR section) surfaced because the action silently accepts any input combination. A phase adding input validation — e.g., warn when both evidence and claude-last-output carry the same value, or require at least one rationale source — would prevent whole-class duplication bugs across all callers. (owner: workflows)
- pr753.2: Add build-automation-pr-body unit tests for empty-input edge cases -- build-automation-pr-body.cjs is exported as a module (line 416-427) but has no test coverage. The APR-I05 bug was a runtime PR-body rendering failure that could have been caught by testing the buildAutomationPrBody function with empty claudeLastOutput, empty structuredOutput, and evidence containing raw expression syntax. Adding .github/workflows/scripts/__tests__/build-automation-pr-body.test.cjs would cover the 5 required headings, empty-input fallbacks, and duplicate-input detection. (owner: workflows)
