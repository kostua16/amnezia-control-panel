# Quick Plan: PR #1076 workflow improvement intake

## Source
- PR #1076: fix: resolve #1071 - MNT-I09: Trim allowed-tools to the sweep's needs (maintenance)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/1076

## Summary
PR #1076 trims maintenance.yml allowed-tools from broad wildcards ('Bash(git *)', 'Bash(gh issue:*)') to specific subcommands. Same overly-broad patterns exist in 3 other workflows. Two quick fixes + one governance improvement surfaced.

## Quick Wins
- Trim Bash(git *) in deepseek review workflows -- deepseek-code-review.yml:166 and deepseek.yml:117 grant Bash(git *) allowing git push, git reset --hard. Review-only workflows need only git diff/log/show. Same class as the fix in maintenance.yml:110. (owner: kos-maintenance, type: workflow-improvement)
- Trim Bash(gh:*) and Bash(git:*) in monitor workflow -- monitor-amnezia-control-panel-github-runs.yml:240 uses colon-star Bash(gh:*)/Bash(git:*). Colon-star grants every subcommand including gh repo delete, git push --force. Narrow to actual agent prompt usage. (owner: kos-maintenance, type: workflow-improvement)

## pr1076.x Phase Candidates
- pr1076.1: Centralize allowed-tools policy in policy.json -- PR #1076 fixed one workflow manually. Same broad-to-specific trimming needed for deepseek-*.yml, monitor-*.yml. Add per-workflow-category tool grants to policy.json. A gate action can validate allowed-tools compliance at PR time — catch overly broad entries before merge, not in ad-hoc maintenance sweeps. (owner: kos-maintenance)
