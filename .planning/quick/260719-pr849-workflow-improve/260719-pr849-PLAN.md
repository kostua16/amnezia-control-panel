# Quick Plan: PR #849 workflow improvement intake

## Source
- PR #849: fix(workflows): AFX-I04 plugin trim + security-guidance in pattern-rules-only mode (audit-fix)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/849

## Summary
PR #849 trims audit-fix.yml from 9→5 plugins (removes code-simplifier, frontend-design, superpowers, caveman), drops caveman marketplace, and adds security-guidance pattern-rules-only env vars (ENABLE_CODE_SECURITY_REVIEW/STOP_REVIEW/COMMIT_REVIEW='0'). Key follow-up: the same 9-plugin bloat persists in fix-issue.yml, _auto-fix-ci.yml, and gsd-planning-execute.yml — same timeout-pressure rationale applies. The security-guidance optimization (agent can't run git → disable LLM layers) also applies to fix-issue and _auto-fix-ci.

## Quick Wins
- Trim plugins in fix-issue.yml, _auto-fix-ci.yml, gsd-planning-execute.yml -- Remove code-simplifier, frontend-design, superpowers, caveman plugins and caveman marketplace from these three workflows — identical rationale to AFX-I04 (#834). fix-issue.yml:163-185, _auto-fix-ci.yml:225-234, gsd-planning-execute.yml:245-254 all carry the full 9-plugin list that audit-fix.yml just trimmed. (owner: claude, type: quick-task)
- Add security-guidance pattern-rules-only env vars to fix-issue.yml and _auto-fix-ci.yml -- Both workflows forbid git in their prompts. Add ENABLE_CODE_SECURITY_REVIEW='0', STOP_REVIEW='0', COMMIT_REVIEW='0' to their run-zai steps (matching the naming convention used in the Summary and PR description). Saves one LLM call per turn with zero security coverage loss (commit review can never fire; regex Edit/Write hooks remain active). (owner: claude, type: quick-task)
- Right-size audit-fix timeout-minutes after plugin trim -- audit-fix.yml:53 still sets timeout-minutes: 45. Original 45 was raised because 9 plugins caused 29/30 timeout cancellations. With 5 plugins + pattern-rules-only security-guidance, measure post-merge runtime over 5-6 runs and reduce to 30-35 min if median stays under 30. (owner: claude, type: quick-task)

## pr849.x Phase Candidates
- pr849.1: Centralize plugin manifests into a shared composite action -- 13 workflows each inline their own plugin lists. PR #849 edited only audit-fix.yml, but same trim must be manually replicated to fix-issue, _auto-fix-ci, gsd-planning-execute, claude, code-review, audit-auto-prs. A shared .github/actions/agent-plugins/action.yml lets a single trim propagate to all consumers and makes plugin-count auditing trivial. (owner: claude)
- pr849.2: Add plugin-list consistency gate to maintenance sweep -- Plugin lists already diverge: audit-fix has 5, claude has 10, code-review has 7, suggest-improvements has 3. Add a check to maintenance.yml that flags workflows where plugin count exceeds a threshold or where a marketplace URL is listed without a matching plugin. Prevents future bloat recurrence. (owner: claude)
- pr849.2: Instrument plugin cold-start time in run-zai action output -- AFX-I04 trim decision was based on observed timeout rates, not direct per-plugin overhead measurement. Add before/after timestamping in run-zai/action.yml and expose plugin_setup_seconds output. Enables data-driven trim decisions instead of guessing at timeout budgets. (owner: claude)
