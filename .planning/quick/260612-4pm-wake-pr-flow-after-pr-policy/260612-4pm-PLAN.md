---
status: complete
date: 2026-06-12
---

# Wake PR Flow After PR Policy

## Goal

Keep PR orchestration aligned with its configured required checks by re-running `pr-flow.yml` after `PR Policy` completes.

## Tasks

- Confirm the stale `flow/checks-pending` pattern on automation PRs by comparing PR statuses, run ordering, and the orchestrator trigger list.
- Update `.github/workflows/pr-flow.yml` so `workflow_run` also reacts to completed `PR Policy` runs.
- Add a regression test proving the trigger list still includes `PR Policy`.
- Verify workflow syntax plus the targeted workflow trigger test, lint, and formatting checks.
