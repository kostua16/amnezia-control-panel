# Quick Plan: PR #1019 workflow improvement intake

## Source
- PR #1019: fix: resolve #1017 - APR-E10: Escalate twice-recommended systemic fixes to issues (audit-auto-prs)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/1019

## Summary
PR #1019 adds escalate-repeated-fixes.cjs + unit tests but does not wire the script into audit-auto-prs.yml. Two follow-up quick tasks for the integration gap, two phase suggestions for e2e coverage and search correctness.

## Quick Wins
- Wire escalate-repeated-fixes.cjs into audit-auto-prs.yml as a post-audit job -- Script is dead code until wired — modeled on existing publish-digest job at audit-auto-prs.yml:357, pass --structured-output and --run-url, permissions: issues: write. (owner: claude[bot], type: quick-task)
- Capture created escalation issue URL from createEscalationIssue for GitHub output -- createEscalationIssue() discards gh issue create stdout containing the issue URL — parse it and call setOutput so downstream steps can link to new escalations. (owner: claude[bot], type: quick-task)

## pr1019.x Phase Candidates
- pr1019.2: Add e2e test scenario for APR-E10 escalation flow -- Unit tests cover pure functions but no integration test exercises full main() flow (structured output → log upsert → streak threshold → escalation issue). Add characterization test mocking gh CLI to prevent silent-failure regressions at integration level. (owner: claude[bot])
- pr1019.2: Tighten hasMergedFixPr search query to reduce false-positive hex fragment matches -- 12-char hex fingerprint in --search can match unrelated commit SHA fragments in PR bodies. Quote fingerprint with distinguishing token 'fingerprint:' and update escalation issue template so fix PRs consistently contain the searchable token. (owner: claude[bot])
