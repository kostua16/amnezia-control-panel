# Quick Plan: PR #971 workflow improvement intake

## Source
- PR #971: fix: resolve #925 - WHO-E02: Cron-collision detector + stagger proposals (workflow-health-optimize)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/971

## Summary
PR #971 adds WHO-E02 cron-collision detection to the workflow-health-optimize agent/skill and updates the YAML prompt. Found 3 quick tasks (stale TODO, ':00' thundering herd, DRY violation) and 3 phase suggestions (cron registry, e2e test, TODO auto-update).

## Quick Wins
- Fix stale WHO-E02 TODO — 'YAML still needed' contradicts PR's own diff -- PR diff updates WHO-E02 in TODOs-2.md:70 to say 'YAML prompt update still needed' but the same PR includes those exact YAML changes to workflow-health-optimize.yml:344-357. Self-contradicting note will confuse readers. (owner: docs, type: quick-task)
- Stagger gsd-planning-execute.yml off minute :00 -- New stagger rules say 'never propose minute 0' yet gsd-planning-execute.yml:5 fires at '0 */6 * * *'. Pre-existing violation the optimizer can't auto-fix. Shift to :03 or :12. (owner: workflow, type: quick-task)
- Deduplicate WHO-E02 section between agent and skill files -- WHO-E02 block (~30 lines) copy-pasted verbatim between kos-workflow-health-optimize.md:40-68 and SKILL.md:37-49. Future edits will drift. Agent file should reference skill as single source. (owner: workflow, type: quick-task)

## pr971.x Phase Candidates
- pr971.1: Centralized cron registry with PR-change validation -- Single source of truth for cron schedules prevents drift and removes DRY burden from agent/skill files. CI check validates registry matches actual workflow crons on PR change. (owner: workflow-governance)
- pr971.2: E2e test for cron collision detection logic -- Untested collision detection logic will silently break when schedules change or parser is refactored. Only tracking-issue and pr651 flows have e2e coverage currently. (owner: ci-correctness)
- pr971.4: Auto-validate TODO status on PR merge to prevent stale notes -- Stale TODO notes erode trust in the backlog. WHO-E02's 'YAML still needed' caveat was wrong — a merge-time check scanning TODOs-2.md IDs vs PR changes would prevent this. (owner: planning-automation)
