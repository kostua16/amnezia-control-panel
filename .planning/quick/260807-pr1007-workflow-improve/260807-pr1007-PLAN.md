# Quick Plan: PR #1007 workflow improvement intake

## Source
- PR #1007: fix: resolve #995 - MNT-E03: Automation KPI digest (maintenance)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/1007

## Summary
PR #1007 adds fleet-kpi-digest.cjs (445 lines) + 303-line test suite for MNT-E03. Script queries gh API for fleet bot PR/issue/gate-skip activity and upserts a digest issue. Tests cover parsing, classification, rendering, and NDJSON edge cases. Three follow-ups identified: a stdin pipe bug in runGh, missing workflow wiring, and a server-side filter optimization.

## Quick Wins
- Fix runGh stdin pipe — body-file - reads EOF -- fleet-kpi-digest.cjs:371 hardcodes stdio to ['ignore','pipe','pipe'], but main() passes { input: body } at line 703 for 'gh issue edit --body-file -'. execFileSync with stdio[0]='ignore' drops input, so the issue body will be empty. Fix: when options.input is truthy, set stdio[0] to 'pipe'. The sibling script upsert-human-disposition-digest.cjs:132 does this correctly. (owner: claude[bot], type: quick-task)
- Wire fleet-kpi-digest into maintenance.yml -- The script is created and tested but never invoked from maintenance.yml. The maintenance job prompt (line 72-93) has no step calling fleet-kpi-digest.cjs. Add a post-claude step that runs the script with --repo and --window-hours 12, capturing structured output. (owner: claude[bot], type: quick-task)
- Use server-side conclusion=skipped filter in queryGateSkips -- fleet-kpi-digest.cjs:521-535 queries status=completed then client-side filters conclusion=='skipped' in jq. The GitHub API supports -f conclusion=skipped as a query parameter, pushing the filter server-side and cutting payload from all completed runs to just skipped ones. (owner: claude[bot], type: quick-task)

## pr1007.x Phase Candidates
- pr1007.2: Extract shared NDJSON parser into reusable script utility -- parseGhJsonLines in fleet-kpi-digest.cjs:391-412 is a general-purpose gh NDJSON stream parser with failure surfacing. upsert-human-disposition-digest.cjs:130 has its own simpler runGh. Extract into scripts/lib/gh-json-lines.cjs so all fleet scripts share one tested parser, and add e2e coverage. (owner: claude[bot])
- pr1007.4: Integrate fleet KPI digest into maintenance workflow as MNT-E03 completion -- MNT-E03 (docs/TODOs-2.md:215) calls for a 12h window digest. The script exists but maintenance.yml has no step calling it. Add a dedicated workflow step after claude_run so the digest issue upserts on every maintenance run. Cross-reference with MON-E09 and XC-5 for future metrics consolidation. (owner: claude[bot])
