# Quick Task 260429-c3m: add ADMIN_PASSWORD env support to seed

## Tasks

### 1. Update seed.ts to support ADMIN_PASSWORD env var
- **files**: `src/lib/seed.ts`
- **action**: Use `process.env.ADMIN_PASSWORD` when set, fallback to `'admin'`
- **verify**: Password sourced from env when present, defaults otherwise

### 2. Create .env with defaults
- **files**: `.env`, `.env.example`
- **action**: Add JWT_SECRET and ADMIN_PASSWORD to .env.example (values as placeholders), add JWT_SECRET with a generated value and ADMIN_PASSWORD=admin to .env
- **verify**: Files exist with correct structure
