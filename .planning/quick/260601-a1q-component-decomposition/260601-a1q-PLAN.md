# Quick Task 260601-a1q: Component decomposition — split chain-node-routing-drawer

## Problem

`src/components/chains/chain-node-routing-drawer.tsx` is 1,853 lines — the largest file in the project. It manages 25+ state variables, auto-save logic, geo-routing forms, and IP/domain routing in a single component. Related files `chain-flow-editor.tsx` (816 lines) and `routing-rules-list.tsx` (643 lines) have similar bloat. Duplicated utility functions (country-code-to-flag, protocol labels) exist across 3+ files.

## Scope

### 1. Split chain-node-routing-drawer.tsx (1,853 → ~4 files)
- **files**: `src/components/chains/chain-node-routing-drawer.tsx`
- **action**:
  - Extract `GeoRoutingForm` component (geo match types, region options, geo auto-save)
  - Extract `IpDomainRoutingForm` component (IP/domain rule CRUD, reordering)
  - Extract `useRoutingAutoSave` hook (debounced save, form validation, dirty tracking)
  - Keep drawer shell in original file (~200 lines): open/close state, tab switching, data loading
- **verify**: Each file under 300 lines; all existing tests pass
- **done**: `chain-node-routing-drawer.tsx` under 300 lines

### 2. Extract shared routing utilities
- **files**: `src/components/chains/chain-node-routing-drawer.tsx`, `src/components/routing/geo-rules-list.tsx`, `src/components/routing/create-routing-rule-form.tsx`
- **action**:
  - Create `src/lib/format-country.ts` — country-code-to-flag emoji helper
  - Create `src/lib/routing-constants.ts` — MATCH_TYPE_OPTIONS, REGION_OPTIONS, PROTOCOL_OPTIONS, ACTION_OPTIONS
  - Replace inline definitions in all 3+ files with imports
- **verify**: `grep -r "REGION_OPTIONS" src/components/` shows imports, no inline definitions
- **done**: Zero duplicated routing constants across components

### 3. Simplify chain-flow-editor.tsx (816 lines)
- **files**: `src/components/chains/chain-flow-editor.tsx`
- **action**:
  - Extract `ChainTemplateSelector` sub-component
  - Extract `ChainNodeRenderer` sub-component
  - Extract `ChainControls` sub-component (zoom, layout, apply button)
- **verify**: Each file under 250 lines; chain editor renders identically
- **done**: `chain-flow-editor.tsx` under 250 lines

## Acceptance Criteria
- [ ] `chain-node-routing-drawer.tsx` under 300 lines
- [ ] `chain-flow-editor.tsx` under 250 lines
- [ ] Zero duplicated country-code-to-flag or routing option constants
- [ ] All existing functionality preserved (no behavioral changes)
- [ ] TypeScript compiles without errors
- [ ] Lint passes on all changed files

## Risk
- Medium — large refactor surface; must verify all tab states and auto-save paths still work
- Mitigate by doing one file at a time, running dev server to visually verify after each split

## Estimated Effort
2-3 focused sessions
