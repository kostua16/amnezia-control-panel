# Quick Plan: PR #645 workflow improvement intake

## Source
- PR #645: fix(workflows): reconcile stale pending PR checks
- URL: https://github.com/kostua16/amnezia-control-panel/pull/645

## Summary
PR #645 adds stale-pending PR check reconciliation in required-check-evidence.cjs (findExactHeadWorkflowRunAnyStatus, pendingChecksForWorkflowRun, shouldFallback expansion). 3 quick follow-ups + 3 roadmap suggestions across ci-correctness, workflow-governance, approval-policy.

## Quick Wins
- Add reason string to stale-pending reconciliation branch in required-check-evidence.cjs -- PR #644 debug required manual dry-run to confirm reconciliation fired; a reason string in the return object would make this self-evident in run logs without re-running dry-runs. (owner: kos-fix-issue, type: quick-task)
- Test pendingChecksForWorkflowRun with queued run status -- GitHub has a queued pre-in_progress state not covered by the 3 new tests; pendingChecksForWorkflowRun maps it via run.status ?? 'pending' which is correct but untested. (owner: kos-fix-issue, type: quick-task)
- Exclude resolved debug session files from future PR diffs via gitignore or PR-size-guard -- Debug files with status:resolved are reference-only; committing them sets a precedent that clutters future PR diffs. (owner: kos-fix-issue, type: quick-task)

## pr645.x Phase Candidates
- pr645.2: Add reconciliation-fire counter to maintenance sweep from PR Orchestrator run logs -- Without monitoring, we won't know if the reconciliation fires often (expected for planning PRs) or rarely (possible over-fit to PR #644). (owner: kos-maintenance)
- pr645.1: Structured debug-session lifecycle with 30-day retention policy for resolved sessions -- The pr644 debug file proves the pattern works but without lifecycle rules the .planning/debug/ directory will accumulate indefinitely. (owner: kos-maintenance)
- pr645.3: E2E scenario covering full dispatch chain after M11b reconciliation -- PR #644 proved the gap: reconciliation returned passed but end-to-end dispatch (label removal, review, finalizer) wasn't verified until manual dry-run. (owner: kos-maintenance)
