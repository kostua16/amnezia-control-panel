# Quick Plan: PR #942 workflow improvement intake

## Source
- PR #942: fix: resolve #941 - [project-manager] Queue manager workflow failed
- URL: https://github.com/kostua16/amnezia-control-panel/pull/942

## Summary
PR #942 adds transient-HTTP retry to project-manager.cjs ghJson by copying TRANSIENT_PATTERNS + an attempt loop verbatim from lib/sticky-comment.cjs — now the third copy of this logic (sticky-comment.cjs:32 canonical, project-manager.cjs:2429 new, umbrella-sub-issues.cjs:41 bare/no-retry). Prior quick task .planning/quick/260714-pr761-workflow-improve/PLAN.md:12 already scoped the shared-lib extraction; #942 deepened the duplication instead. Two real gaps: (1) the new retry path has zero test coverage in project-manager.test.cjs, and (2) project-manager ghJson retries unconditionally while sticky-comment gates retry behind retry:true to avoid double-applying mutating calls — a latent duplicate-write landmine once any ghJson caller wraps a POST/PATCH.

## Quick Wins
- Extract shared lib/retry.cjs from three duplicated transient-retry copies -- PR #942 copies the transient pattern set + retry loop verbatim into project-manager.cjs (GH_TRANSIENT_PATTERNS/GH_MAX_RETRIES/GH_RETRY_BASE_MS at lines 2426-2438, loop at 2440-2478) despite lib/sticky-comment.cjs:32 already owning the canonical TRANSIENT_PATTERNS + isTransient. umbrella-sub-issues.cjs:41 has a third bare ghJson (JSON.parse(gh(args))) with NO retry. Prior quick task .planning/quick/260714-pr761-workflow-improve/PLAN.md:12 already scoped exactly this extraction. Move isTransient/TRANSIENT_PATTERNS/MAX_RETRIES/RETRY_BASE_MS + the attempt loop into lib/retry.cjs; have project-manager ghJson and umbrella-sub-issues ghJson consume it, collapsing 3 copies to 1 and closing the umbrella-sub-issues no-retry gap. (owner: workflow-scripts, type: workflow-improvement)
- Add transient-retry test coverage for project-manager ghJson -- scripts/__tests__/project-manager.test.cjs (65.8K) has zero matches for transient|retry|HTTP 5|sleep|ghJson — the entire retry path added in #942 is untested. The comment at project-manager.cjs:2430 asserts the patterns 'Must mirror lib/sticky-comment.cjs TRANSIENT_PATTERNS', so any future divergence regresses with no signal. Add cases: (a) 5xx stderr triggers retry-then-success, (b) non-transient error short-circuits to fallback [], (c) max-retries exhausted throws. Note execFileSync('sleep', ...) at line 2455 forks a real subprocess per retry — extract an injectable delay hook so tests don't pay real wall-clock and can assert retry count. (owner: workflow-scripts, type: quick-task)
- Guard ghJson retry against mutating gh api calls -- project-manager.cjs:2429 ghJson retries unconditionally on every caller. lib/sticky-comment.cjs:67 deliberately gates retry behind options.retry===true because a late 504 on a POST/PATCH/DELETE double-applies the mutation (duplicate comment, second id delete) — its own comment at sticky-comment.cjs:60-66 spells this out. Today project-manager's ghJson callers are reads (pr list :3133, issue list :3143, GET comments :3243) so it is safe, but the POST at :3239 already bypasses ghJson via bare gh() precisely because it is a write — the moment a future write is routed through ghJson it silently double-posts. Mirror sticky-comment's read/write opt-in (retry:true) on ghJson before that happens. (owner: workflow-scripts, type: workflow-improvement)

## pr942.x Phase Candidates
- pr942.2: Centralized transient-retry policy + call-site audit for all workflow scripts -- Retry behavior is per-script folklore: sticky-comment.cjs has full read/write-aware retry, project-manager.cjs just got unconditional retry (#942), umbrella-sub-issues.cjs has none. Milestone: audit every gh()/ghJson() call site across .github/workflows/scripts (project-manager, umbrella-sub-issues, sticky-comment, orchestrate-pr-flow, classify-* helpers), classify each as read vs mutating, route all through lib/retry.cjs using the sticky-comment read/write opt-in contract, and add a characterization test asserting no mutating call site retries without explicit opt-in. Prevents the next #942-style copy from reintroducing a duplicate-write landmine and gives umbrella-sub-issues the resilience #942 gave project-manager. (owner: workflow-scripts)
- pr942.1: Lint/test guard forbidding duplicate TRANSIENT_PATTERNS and retry-loop definitions -- PR #942's own comment at project-manager.cjs:2430 ('Must mirror lib/sticky-comment.cjs TRANSIENT_PATTERNS') proves the author knew the duplication was wrong but shipped it anyway — no gate caught it, and the e2e suite (scripts/__tests__/*.test.cjs) stayed green. Add a repo test asserting TRANSIENT_PATTERNS, MAX_RETRIES, RETRY_BASE_MS, and the attempt loop live only in lib/retry.cjs (fail if the regex set or those constants appear outside that module). Turns 'must mirror' from an unenforced comment into a build-breaking invariant; pairs with the ci-correctness extraction milestone. (owner: workflow-scripts)
