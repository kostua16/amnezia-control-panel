# Quick Plan: PR #824 workflow improvement intake

## Source
- PR #824: fix: resolve #748 - MON-I05: Anchor the monitor window to scheduled runs only (monitor-github-runs)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/824

## Summary
PR #824 anchors monitor window to scheduled-only runs via 'event' field filter. 3 quick tasks: stale source label, undocumented manual-trigger behavior, missing event-awareness in failure grouping. 2 phase suggestions: shared run-window action, report-failure event guard.

## Quick Wins
- Fix stale source label in fallback branch of Resolve monitor window step -- PR #824 updates the filter chain but misses the source label in the fallback branch. The monitor prompt receives 'monitor_since_source: previous-completed-run' even though the previous run is now guaranteed to be a scheduled run. Inaccurate telemetry. (owner: claude[bot], type: quick-task)
- Document manual-trigger monitor window behavior in workflow dispatch inputs -- Operators dispatching manually may expect their run to anchor the next scheduled window, but it won't — manual dispatches always fall through to the 2-hour fallback lookback since only schedule events match the filter. Silent behavior difference between schedule and dispatch triggers. (owner: claude[bot], type: quick-task)
- Add event-awareness note to monitor prompt failure grouping guidance -- The monitor groups by (workflowName, event) and may declare 'workflow down' if consecutive manual dispatches fail — those are operator-triggered, not indicative of systemic failure. A one-liner note prevents false positives. (owner: claude[bot], type: quick-task)

## pr824.x Phase Candidates
- pr824.2: Centralize event-aware run window logic into a shared action -- Both monitor-amnezia-control-panel-github-runs.yml and workflow-health-optimize.yml filter runs by event type to determine analysis windows. Extracting this into a shared action (e.g. .github/actions/resolve-run-window/) eliminates duplicated Node.js heredoc logic and ensures consistency. (owner: claude[bot])
- pr824.1: Add event-type guard to the monitor's report-failure job -- Manual dispatch failures should not auto-open issues or dispatch triage. The self-heal step already has a 'github.event_name == 'schedule'' guard at line 291; report-failure (line 476) should match. (owner: claude[bot])
