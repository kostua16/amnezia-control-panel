---
status: complete
date: 2026-06-12
---

# Convert Graphify Output To Local Cache

## Goal

Treat `graphify-out/` as ignored local generated state so graphify hooks and updates stay useful without creating PR conflicts.

## Tasks

- Remove tracked `graphify-out/` artifacts while leaving local files on disk.
- Ignore the full `graphify-out/` directory.
- Update project graphify guidance to describe local-only cache behavior and forbid staging graph artifacts.
- Add PR policy protection that blocks `graphify-out/**` changes.
- Add regression coverage and run targeted workflow, lint, format, and diff checks.
