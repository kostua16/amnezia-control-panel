# Summary: PR #750 workflow improvement intake

- Source PR: https://github.com/kostua16/amnezia-control-panel/pull/750
- Summary: PR #750 wires the maintenance workflow to persist agent pruning changes through branch → detect → validate → push → PR. The pipeline is sound (reuses existing actions correctly). Two tactical gaps found: a redundant fetch in detect_changes and a missing policy.json branch prefix that forces PM to classify maintenance PRs via generic claude- fallback. One governance-level gap: the trigger policy lacks an explicit maintenance PR entry despite the workflow now being a full PR producer.
- Quick tasks: 2
- Phase suggestions: 2
- pr750.x mapping: pr750.1 x1, pr750.2 x1
