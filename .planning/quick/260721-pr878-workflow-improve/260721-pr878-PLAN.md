# Quick Plan: PR #878 workflow improvement intake

## Source
- PR #878: fix: resolve #730 - MON-I04: Cache disk-heal log scans between runs (monitor-github-runs)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/878

## Summary
PR #878 adds a '--created ">=$CUTOFF"' (3-hour ISO-8601 window) to the disk-pressure self-heal step in monitor-amnezia-control-panel-github-runs.yml. Prevents hourly schedule from re-scanning stale ENOSPC runs. Diff is 21 lines — narrow, correct, well-commented. Two quick tasks: cross-platform date portability gap and dispatch-idempotency gap. Two phase suggestions: centralize cutoff window env var and add test coverage for the disk-heal logic.

## Quick Wins
- Replace GNU date -d with cross-platform portable timestamp generation -- Line 305: 'date -u -d '3 hours ago'' uses GNU-only flag; if workflow ever runs on non-self-hosted runners, empty CUTOFF silently disables self-heal (the PR comment warns of this exact risk). Use perl-based strftime or conditional macOS/GNU date. (owner: monitor-workflow-owner, type: quick-task)
- Add dispatch-throttle to prevent duplicate cleanup-runner triggers -- Line 311: 'gh workflow run cleanup-runner.yml' dispatches unconditionally when matches>=2. Overlapping hourly cycles can dispatch twice before cleanup completes. Add in_progress check or concurrency group. Cite: monitor-amnezia-control-panel-github-runs.yml:311. (owner: monitor-workflow-owner, type: quick-task)

## pr878.x Phase Candidates
- pr878.2: Centralize CUTOFF_HOURS as workflow-level env var shared between scan and heal steps -- Disk-heal hardcodes 3h (line 305). ZAI scan uses 'lookback_hours' input (line 14, default 3). Unlinked — if one changes, the other drifts. Extract as env.DISK_HEAL_CUTOFF_HOURS referenced by both. Cite: monitor-amnezia-control-panel-github-runs.yml:14 vs :305. (owner: monitor-workflow-owner)
- pr878.2: Add unit tests for disk-heal step's CUTOFF filtering and match-counting logic -- The self-heal step is correctness-critical — silent regression (empty CUTOFF) disables it entirely. The workflow has a verify step (line 327) for actionlint/shellcheck but no logic-level tests. Add mock-based tests in scripts/__tests__/. Cite: monitor-amnezia-control-panel-github-runs.yml:296-314. (owner: monitor-workflow-owner)
