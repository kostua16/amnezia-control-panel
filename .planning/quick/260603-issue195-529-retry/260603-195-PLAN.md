# Quick Task 260603-195: Classify ZAI 529 As Retryable

## Goal

Fix issue #195 by treating ZAI `API Error: 529` / temporary overload evidence as the existing `rate_limited` category, and make `/fix` retry the shared Claude wrapper when the execution output contains that overload evidence even if a fresh API probe returns HTTP 200.

## Tasks

1. Extend Claude log scanning and execution JSON parsing so 529 overload evidence is classified as `rate_limited`.
2. Apply the same 529 classification to `analyze-claude-runs.sh`.
3. Add a small shared retry classifier for `run-claude-params` and use it from both retry-check steps while keeping output names unchanged.
4. Add focused unit tests for scan classification and retry decisions.
5. Verify with tests, lint, TypeScript, Prettier, shell syntax, and actionlint.
