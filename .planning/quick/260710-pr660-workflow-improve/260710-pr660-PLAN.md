# Quick Plan: PR #660 workflow improvement intake

## Source
- PR #660: fix(review): collect Kilo review thread feedback
- URL: https://github.com/kostua16/amnezia-control-panel/pull/660

## Summary
PR #660 fixes review-thread collection to fail-closed and extracts extractReviewThreads as a testable pure function. Two residual gaps: same fail-open runJson pattern in sibling fetchers, no author filtering in extracted function. Plus shared-lib, dispatch-policy, and bundle-validation opportunities.

## Quick Wins
- Harden fetchReviews and fetchComments to fail-closed or document intentional difference -- collect-review-feedback.cjs:89-113 — fetchReviews/fetchComments still use runJson() with [] fallback, same fail-open pattern that let fetchReviewThreads silently drop Kilo findings. PR #660 fixed threads but left these open. (owner: workflow-maintainer, type: quick-task)
- Add optional authorFilter to extractReviewThreads for Kilo-only bundles -- collect-review-feedback.cjs:138 — extractReviewThreads returns all thread authors. When auto-cover-review dispatches for Kilo-only blockers, human review comments add noise. Optional authorFilter parameter reduces agent context waste. (owner: workflow-maintainer, type: quick-task)

## pr660.x Phase Candidates
- pr660.2: Add runJsonStrict to sticky-comment.cjs shared lib -- runJson() in sticky-comment.cjs silently returns fallback on gh failure. PR #660 worked around it with raw run()+JSON.parse() in fetchReviewThreads. A runJsonStrict variant eliminates future boilerplate and prevents fail-open regressions in sibling fetchers. (owner: workflow-maintainer)
- pr660.3: Severity-aware auto-cover dispatch for Kilo findings -- auto-cover-review dispatches fix-review for any Kilo current-head blocker regardless of severity. A suggestion triggers the same full repair cycle as critical/warning. Severity-aware dispatch prevents unnecessary agent runs on low-impact findings. (owner: workflow-maintainer)
- pr660.1: Post-collection bundle assertion step in fix-review workflow -- fix-review.yml feedback step writes the bundle but never validates structural completeness before the agent reads it. A post-collection assertion catching empty/partial bundles prevents wasting agent turns on incomplete data. (owner: workflow-maintainer)
