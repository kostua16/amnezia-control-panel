# Quick Plan: PR #683 workflow improvement intake

## Source
- PR #683: fix: resolve #679 - [todo-backlog] audit-auto-prs: optimize/audit review backlog (APR)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/683

## Summary
PR #683 resolves APR-I03 by adding Bash(actionlint *) to audit-auto-prs.yml allowed-tools. Two concrete follow-ups: (1) separator inconsistency (space vs colon) across sibling workflows, (2) remaining APR TODOs from docs/TODOs-2.md. Phase suggestion: cross-workflow actionlint tool-permission alignment and shared timeout-cancellation safety net (XC-1/XC-2).

## Quick Wins
- Normalize actionlint allowed-tools separator across audit-auto-prs.yml and monitor-amnezia-control-panel-github-runs.yml -- audit-auto-prs.yml:81 uses 'Bash(actionlint *)' (space) but monitor-amnezia-control-panel-github-runs.yml:169 uses 'Bash(actionlint:*)' (colon). Both patterns work as run-zai Bash tool globs, but diverging conventions make audits harder. Pick one (colon is more common in this repo's allowed-tools lists) and align both. (owner: claude, type: quick-task)
- Implement APR-I01: raise audit-auto-prs timeout or cut MAX_TURNS to fit 15-min budget -- docs/TODOs-2.md APR-I01 (P1, S effort): runs #149 and #160 died at ~925s hitting the 15-min timeout. Either raise timeout-minutes to ~25 or cut MAX_TURNS from 90; add STOP_BY deadline per XC-2. Direct workflow YAML change in .github/workflows/audit-auto-prs.yml:6,13. (owner: claude, type: quick-task)
- Implement APR-I08: drop unused install-uv from audit-auto-prs setup -- docs/TODOs-2.md APR-I08 (P3, S effort): uv (Python) is installed for a GitHub-PR-audit task that never uses it. Remove 'install-uv: 'true'' from .github/workflows/audit-auto-prs.yml:46 to save setup time every 3 hours. (owner: claude, type: quick-task)

## pr683.x Phase Candidates
- pr683.2: Add actionlint to allowed-tools for all workflow-editing agents (WHO, AFX, suggest-improvements, _auto-fix-ci, fix-review) -- audit-auto-prs and monitor-amnezia-control-panel-github-runs are the only workflows with actionlint in allowed-tools, but workflow-health-optimize, audit-fix, suggest-improvements, _auto-fix-ci, and fix-review all can edit workflow YAML. The prompt for audit-auto-prs already mandates 'run actionlint for workflow YAML changes' — extend that mandate + tool permission to the other five. Without it, YAML fixes ship without the mandated check or the agent burns turns on failed tool calls. (owner: claude)
- pr683.2: Implement XC-1: catch cancelled (timeout) runs in report-failure across all six audit workflows -- docs/TODOs-2.md XC-1 cross-reference: WHO-I01, AFX-I02, APR-I02, MON-I01 all share the same root cause — 'if: failure()' does not match 'cancelled' (timeout), so two weeks of audit-fix dead runs produced zero issues. A single shared 'report-failure' composite action change (add 'cancelled' to the condition) fixes all six workflows at once. The existing .github/actions/report-failure is the single target. (owner: claude)
- pr683.2: Implement XC-2: pass STOP_BY wall-clock deadline to run-zai agents in timeout-sensitive workflows -- docs/TODOs-2.md XC-2 cross-reference: AFX-I03 and APR-I01 both need a wall-clock deadline so the agent wraps up before the job timeout kills it (losing all work). Add a shared 'run-zai' input or prompt-embedded STOP_BY timestamp, consumed by agent contracts, so the agent self-terminates with a report instead of being killed. Targets .github/actions/run-zai and agent contracts. (owner: claude)
