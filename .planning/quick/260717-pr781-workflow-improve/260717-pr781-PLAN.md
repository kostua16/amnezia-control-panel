# Quick Plan: PR #781 workflow improvement intake

## Source
- PR #781: fix: resolve #779 - WHO-I04: Exclude self by workflow path, not display name (workflow-health-optimize)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/781

## Summary
PR #781 fixes WHO-I04 by switching self-exclusion from display-name ('r.name') to file-path ('r.path') comparison in workflow-health-optimize.yml. Diff is 3 lines — clean, targeted fix. Two concrete follow-ups identified: (1) same rename fragility exists in concurrency.group keys across 5 workflows, (2) monitor workflow passes display name to agent prompt while already using path for gh CLI — inconsistent.

## Quick Wins
- Replace github.workflow in concurrency.group with stable hardcoded identifiers -- Same rename fragility that caused #779 — display name changes silently break concurrency groups, letting parallel runs collide. 5 workflows affected: workflow-health-optimize, suggest-improvements, audit-fix, monitor-amnezia-control-panel-github-runs, maintenance, docs-drift. (owner: claude, type: quick-task)
- Pass workflow file path instead of display name to monitor agent prompt -- monitor-amnezia-control-panel-github-runs.yml:193 passes '${{ github.workflow }}' to agent prompt while line 30 already has a path-based 'MONITOR_WORKFLOW_FILE' env var for gh CLI. Inconsistent — same rename fragility class as WHO-I04. (owner: claude, type: quick-task)

## pr781.x Phase Candidates
- pr781.1: Establish repo-wide stable workflow identifier convention -- WHO-I04 showed display-name fragility in self-exclusion; concurrency.group problem spans 5+ workflows. Adopt 'env.WORKFLOW_ID' (kebab-case file-stem) as single source of truth for workflow identity in concurrency groups, agent prompts, and self-exclusion filters. Add to e2e regression gate. (owner: claude)
