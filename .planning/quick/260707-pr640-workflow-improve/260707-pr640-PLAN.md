# Quick Plan: PR #640 workflow improvement intake

## Source
- PR #640: fix(workflows): keep stateful PRs in project-manager scope
- URL: https://github.com/kostua16/amnezia-control-panel/pull/640

## Summary
PR #640 adds selectPrInspectionCandidates to keep older stateful PRs in project-manager scope, adds needsProjectManagerAttention label check, and defensively treats undated needs-review labels as blocking. Found 3 quick tasks (label-list dedup, edge-case tests, labelUpdatedAfter scope comment) and 2 phase suggestions (issue-side parity, central label registry).

## Quick Wins
- Extract shared attention/blocker label list into constant -- needsProjectManagerAttention (lines 400–411) duplicates 5 labels already in hasReviewBlocker's blockerLabels (lines 389–395): ai-review-concerns, security-review-concerns, deps-review-manual, deps-review-blocked, antigravity-review-concerns. Adding/removing a label requires editing both arrays. Extract a shared 'PM_ATTENTION_LABELS' constant referenced by both functions. (owner: kos-maintenance, type: quick-task)
- Add edge-case tests for selectPrInspectionCandidates -- PM1b tests only the happy path (1 stateful PR among 12). Missing: no stateful PRs (pure truncation), limit=0, limit exceeds array length, multiple attention labels on same PR (no duplicates in result), PR outside limit without attention label stays excluded. These guard against future regressions. (owner: kos-maintenance, type: quick-task)
- Annotate labelUpdatedAfter callers with undated=true implication -- The undated-label-means-active change in labelUpdatedAfter (line 451) was motivated by needs-review but affects all callers: hasMaintainerRejection line 433 (needs-review), and any future callers. Add a brief JSDoc noting this behavioral contract so future callers don't assume undated means stale. (owner: kos-maintenance, type: quick-task)

## pr640.x Phase Candidates
- pr640.3: Add selectIssueInspectionCandidates parity for stateful issues -- PRs now have selectPrInspectionCandidates keeping stateful PRs in scope, but issues at collectSnapshot line 1999 still use plain sortNewest(issues).slice(0, issueLimit). Stateful issues (labeled in-progress, keep-open, needs-review, priority/high) can age out of the window and go unmonitored. Mirror the PR pattern: a needsAttention check for issue labels + fallback inclusion. (owner: kos-project-manager)
- pr640.1: Centralize flow/control label registry -- Three functions now maintain independent label arrays: hasReviewBlocker (blockerLabels), needsProjectManagerAttention (9 labels), and normalizeLabels consumers. Labels like flow/checks-failed, flow/finalizer-dispatched, flow/manual-only are scattered across functions with no single source of truth. A central LABEL_REGISTRY module with semantic groups (attention, blocker, flow, approval) would prevent drift and make onboarding easier. (owner: kos-maintenance)
