# Quick Plan: PR #843 workflow improvement intake

## Source
- PR #843: fix: resolve #699 - MNT-I03: Align 50-turn budget with the 10-min timeout (maintenance)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/843

## Summary
PR #843 fixes MNT-I03 by lowering maintenance MAX_TURNS 50→30 and adding a BUDGET hint line. The cross-cutting concern XC-2 (turn-budget vs timeout mismatch) remains open for other workflows. Two concrete follow-ups identified.

## Quick Wins
- Add budget-hint line to suggest-improvements.yml (same 50-turn/10-min mismatch as old maintenance) -- suggest-improvements.yml has MAX_TURNS=50 with timeout-minutes:10 — identical mismatch that PR #843 just fixed in maintenance.yml. The BUDGET hint pattern (e.g. 'BUDGET: 50 turns, 10-minute timeout…') should be added to its prompt at line ~101, and MAX_TURNS should be re-evaluated downward or the timeout raised. (owner: workflow-maintainer, type: quick-task)
- Parametrize the BUDGET hint in maintenance.yml prompt to reference ${{ env.MAX_TURNS }} -- The newly added line reads 'BUDGET: 30 turns, 10-minute timeout' as a hardcoded string in the prompt YAML. If MAX_TURNS is changed again, the hint will silently drift. Replace with 'BUDGET: ${{ env.MAX_TURNS }} turns, 10-minute timeout' to keep them in sync. (owner: workflow-maintainer, type: quick-task)

## pr843.x Phase Candidates
- pr843.2: Audit and align all workflow MAX_TURNS vs timeout-minutes (XC-2) -- TODOs-2.md XC-2 documents this as a cross-cutting root cause affecting AFX-I01, AFX-I03, APR-I01, MNT-I03, MON-I02. PR #843 fixes one instance. Other misaligned workflows visible in grep: dependency-review.yml (80 turns / 10 min), suggest-improvements.yml (50 / 10), triage.yml (40 / 15), project-manager.yml (30 / 45). A systematic audit-and-align phase would close XC-2 entirely. (owner: workflow-team)
- pr843.1: Add a CI lint rule that warns when MAX_TURNS × estimated-seconds-per-turn > timeout-minutes × 60 -- The budget/timeout mismatch is a recurring class of bugs. A lightweight CI check (or workflow-health-optimize sub-check) that flags workflows where MAX_TURNS exceeds a safe multiple of the job timeout would prevent future regressions like the one PR #843 fixed. (owner: workflow-team)
