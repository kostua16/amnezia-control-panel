# Summary: PR #863 workflow improvement intake

- Source PR: https://github.com/kostua16/amnezia-control-panel/pull/863
- Summary: PR #863 wraps 'runs_data' in an envelope '{failureLogCapApplied, failureLogCapMax, failureLogCapTotal, runs: [...]}' to bound failure-log API calls at 10. The cap logic is sound, but the downstream claude agent in the 'optimize' job is never told about the new envelope shape — it still expects a raw array. Three concrete follow-ups identified: downstream envelope awareness, truncation ordering, and skipped-run traceability.
- Quick tasks: 3
- Phase suggestions: 2
- pr863.x mapping: pr863.2 x2
