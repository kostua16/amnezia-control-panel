# Quick Plan: PR #863 workflow improvement intake

## Source
- PR #863: fix: resolve #707 - WHO-I02: Bound failure-log collection in 'collect-runs' (workflow-health-optimize)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/863

## Summary
PR #863 bounds failure-log API calls in collect-runs with MAX_DETAILED_FAILURES=10 cap and wraps output in an envelope. Key finding: the optimize agent prompt embeds runs_data raw but now receives an envelope object instead of the expected array — latent contract drift.

## Quick Wins
- Unwrap envelope in runs_data output for downstream optimize agent -- PR #863 wraps runs_data in {failureLogCapApplied, runs:[...]} but the optimize prompt at workflow-health-optimize.yml:308 embeds it raw. The kos-workflow-health-optimize agent expects an array. Add a separate runs-data-array output with just JSON.stringify(trimmed) so the optimizer gets the array it expects, or update the prompt to instruct .runs access. (owner: workflow-health-optimize, type: quick-task)
- Promote MAX_DETAILED_FAILURES from inline constant to workflow env var -- Hardcoded at workflow-health-optimize.yml:110. Promote to env.MAX_DETAILED_FAILURES: '10' alongside existing MAX_TURNS and NODE_VERSION for tunability without editing the script block. (owner: workflow-health-optimize, type: quick-task)

## pr863.x Phase Candidates
- pr863.2: Persist envelope cap metadata into health-summary JSONL for burst-failure trend detection -- Envelope fields failureLogCapApplied and failureLogCapTotal are logged via core.info but not persisted to JSONL. The persistHealthSummary call at line 240 writes full runSummaries pre-cap. Add a cap-event JSONL entry {type:'cap_applied', max, total, timestamp} so trend analysis can detect when the 10-run cap is consistently hit — signals burst failures or a too-low limit. (owner: workflow-health-optimize)
