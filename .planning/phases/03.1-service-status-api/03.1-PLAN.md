# Phase 3.1: Service Status API - Plan

**Created:** 2026-04-28
**Status:** Ready for execution

## Tasks

### Task 1: Create Service Status API
**File:** `src/app/api/services/[service]/status/route.ts` (NEW)

Dynamic route. GET handler:
1. Validate service param (allow "awg" or "3x-ui")
2. Map to systemd service name
3. Use execSync (from child_process, not shelljs — simpler) to run `systemctl is-active <name>`
4. Return { service, status: "online"|"offline", timestamp: ISO string }
5. Error handling: if command fails, return status "offline"

### Task 2: Create Service Status Hook
**File:** `src/lib/use-service-status.ts` (NEW)

React hook for polling service status. useQuery pattern (tanstack/react-query already installed).

## Verification
- tsc + lint pass
- API returns proper JSON
