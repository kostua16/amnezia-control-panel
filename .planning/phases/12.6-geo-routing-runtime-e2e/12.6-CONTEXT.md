# Phase 12.6: Geo-routing runtime E2E - Context

**Gathered:** 2026-05-01
**Status:** Ready for planning

<domain>
## Phase Boundary

GeoIP-based routing evaluation is invoked from the real chain/traffic application path. GEO-04 import path is narrowed to match shipped scope (v2fly geoip.dat only, sendmiche/rulite marked as future enhancement).

Gap: GEO-03, GEO-04 from v1.1 audit.

**GEO-03:** `resolveGeoRoute()` and `evaluateGeoRulesFromDB()` exist in `src/lib/geo-routing.ts` but have zero imports elsewhere in `src/`. No runtime path from GeoIP evaluation to chain/traffic application.

**GEO-04:** Audit expected sendmiche/rulite import. Only v2fly geoip.dat import is implemented. Either add sendmiche/rulite support OR narrow the requirement text.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices at Claude's discretion — infrastructure phase.

Key constraints:
- `resolveGeoRoute(ip)` returns `GeoRoutingResult` with `action` (ALLOW/BLOCK/ROUTE) and optional `chainId`
- The chain application path is in `src/lib/config-applier.ts` and the routing apply API at `src/app/api/routing/apply/route.ts`
- GEO-03 fix: call `resolveGeoRoute` from the chain config application path before deciding how to route traffic
- GEO-04 fix: narrow requirement to match shipped scope (v2fly geoip.dat import only). Add TODO/future comment for sendmiche/rulite. Do NOT implement sendmiche/rulite parser — that's a future feature.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/lib/geo-routing.ts` — `resolveGeoRoute(ip)`, `evaluateGeoRulesFromDB()`, `lookupGeoIP()`
- `src/lib/config-applier.ts` — applies chain config to remote panels
- `src/app/api/routing/apply/route.ts` — routing rule application endpoint
- `src/app/api/routing/geoip-lookup/route.ts` — GeoIP lookup API
- `src/lib/geoip-manager.ts` — v2fly geoip.dat download and lookup

### Established Patterns
- Fail-open on errors (return ALLOW)
- IPv4 only
- SQLite as canonical rule store
- Priority-based rule evaluation (lower = higher priority)

### Integration Points
- `src/lib/config-applier.ts` — add geo-routing evaluation before chain application
- `src/app/api/routing/apply/route.ts` — add geo-routing check in apply flow
- `.planning/REQUIREMENTS.md` — update GEO-04 text to match shipped scope

</code_context>

<specifics>
## Specific Ideas

No specific requirements — close GEO-03, GEO-04 from audit.

</specifics>

<deferred>
## Deferred Ideas

sendmiche/rulite import parser, per-user geo-routing, geo-rule A/B testing — future phases.

</deferred>
