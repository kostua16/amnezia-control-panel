---
phase: "12.10"
plan: "02"
subsystem: push-wizard
tags: [push, api-keys, per-panel, configVersion, retries]
dependency_graph:
  requires: []
  provides: [panelApiKeys-state, per-panel-api-key-input, accurate-push-results]
  affects: [push-wizard, panel-selector, push-result-summary]
tech_stack:
  added: []
  patterns: [controlled-password-input, show-hide-toggle, api-result-prefer-over-ws]
key_files:
  created: []
  modified:
    - src/components/push/panel-selector.tsx
    - src/components/push/push-wizard.tsx
    - src/components/push/push-result-summary.tsx
decisions: []
metrics:
  duration_seconds: 133
  completed_at: "2026-05-02T09:19:00Z"
  tasks_completed: 2
  files_modified: 3
---

# Phase 12.10 Plan 02: Per-Panel API Key Input and Accurate Push Results Summary

PushWizard step 1 now collects plaintext API keys per selected panel (password input with show/hide toggle), sends real keys in push and rollback requests (replacing hardcoded empty strings), and displays accurate configVersion and retry counts in step 4 results using the push API's PushAllResult response.

## Tasks Completed

### Task 1: Add API key inputs to PanelSelector and wire PushWizard state
**Commit:** `6d95c8a`
**Files:** `src/components/push/panel-selector.tsx`, `src/components/push/push-wizard.tsx`

- Extended `PanelSelectorProps` with optional `panelApiKeys` and `onApiKeyChange` props
- Added inline password `<Input>` per selected panel row with Eye/EyeOff toggle (lucide-react)
- Added `panelApiKeys` state (`Record<number, string>`) and `handleApiKeyChange` callback in PushWizard
- **Bug fix (Rule 1):** Removed local `const panelApiKeys` in `handlePush` that shadowed component state with empty strings
- **Bug fix (Rule 1):** Replaced hardcoded `apiKey: ''` in `handleRollback` with `panelApiKeys[panelId] ?? ''`
- Disabled "Next: Preview Changes" button when any selected panel has empty/whitespace API key
- Added `setPanelApiKeys({})` reset in `handleRetry`

### Task 2: Fix step 4 results to use push API response for accurate configVersion/retries
**Commit:** `abb6119`
**Files:** `src/components/push/push-wizard.tsx`, `src/components/push/push-result-summary.tsx`

- `handlePush` success branch now stores `json.data` as `PushAllResult` from the API response
- Auto-advance useEffect guards with `if (!pushResults)` to prefer API result over WS-event-built result
- Added `pushResults` to auto-advance useEffect dependency array
- PushResultSummary success subtext now shows `v{N}` for configVersion and `{N} retries` when applicable

## Deviations from Plan

### Auto-fixed Issues

None -- plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: info_disclosure | panel-selector.tsx | API keys held in React state (volatile, cleared on page close/retry). No localStorage. Single-admin UX per CONTEXT.md convention. |
| threat_flag: transport | push-wizard.tsx | API keys sent in POST body to /api/panels/push over HTTPS. HMAC signing on panel-sync-client ensures payload integrity. |

## Self-Check: PASSED
