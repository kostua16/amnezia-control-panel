---
phase: 12.11-tailscale-transport-chain-apply
plan: 03
subsystem: infra
tags: [tailscale, transport, resolvePanelTransport, chain-apply, panel-sync, push]

# Dependency graph
requires:
  - phase: 12.11
    plan: 01
    provides: resolvePanelTransport with __setDeps/__resetDeps test mocking
provides:
  - Tailscale transport resolution in chains/apply route (panelCredentials with resolved URLs)
  - Tailscale transport resolution in panel-sync-client pushConfigToAllPanels
  - 2 transport resolution integration tests
affects: [chain-apply-route, panel-sync-client, push-wizard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Hostname correlation matching between RemotePanel and chain nodes (no FK exists)
    - Transport resolution as best-effort overlay: falls back to registered panelUrl on failure

key-files:
  created: []
  modified:
    - src/app/api/chains/apply/route.ts
    - src/lib/panel-sync-client.ts
    - src/lib/__tests__/panel-sync-client.test.ts

key-decisions:
  - "generateChainConfig NOT awaited -- still synchronous (Plan 02 not yet applied to chain-router.ts)"
  - "Hostname correlation matching for RemotePanel-to-chain-node (no serverId FK in schema)"
  - "Transport resolution wrapped in try/catch with console.warn fallback in panel-sync-client"

patterns-established: []

requirements-completed: [TSCL-04, CHAIN-01]

# Metrics
duration: 5min
completed: 2026-05-02
---

# Phase 12.11 Plan 03: Transport Resolution in Push/Apply Paths Summary

**Wire resolvePanelTransport into chains/apply route (credential assembly) and panel-sync-client (push resolution) for Tailscale transport URLs**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-02T09:39:33Z
- **Completed:** 2026-05-02T09:44:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Removed broken `serverId` query on RemotePanel (field does not exist in schema)
- Built real panelCredentials with Tailscale transport resolution in chains/apply route
- Added hostname correlation matching to associate RemotePanel records with chain nodes
- Wired transport resolution into pushConfigToAllPanels with fallback to original panelUrl
- Added 2 transport resolution integration tests using __setDeps/__resetDeps pattern

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix chains/apply route credential assembly with transport resolution** - `671379f` (feat)
2. **Task 2: Wire transport resolution into panel-sync-client pushConfigToAllPanels** - `4913421` (feat)

## Files Created/Modified
- `src/app/api/chains/apply/route.ts` - Removed broken serverId query, added resolvePanelTransport import, built panelCredentials with Tailscale-resolved URLs (183 lines)
- `src/lib/panel-sync-client.ts` - Added transport resolution before pushConfigToPanel call with fallback (252 lines)
- `src/lib/__tests__/panel-sync-client.test.ts` - Added __setDeps/__resetDeps global setup, 2 transport resolution tests (18 total tests, all passing)

## Decisions Made
- `generateChainConfig` not awaited: still synchronous in chain-router.ts (Plan 02 runs in parallel and has not yet been applied). Plan 02 will make it async and add `await` in a separate commit.
- Hostname correlation matching: since RemotePanel has no serverId FK, panels are matched to chain nodes by checking if the panelUrl hostname contains the node hostname (or vice versa), with fallback to matching panel.id === node.serverId.
- Transport resolution in pushConfigToAllPanels is wrapped in try/catch with console.warn -- failure falls back to the registered panelUrl (non-blocking).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] generateChainConfig is still synchronous (not async)**
- **Found during:** Task 1
- **Issue:** Plan instructed to add `await` to `generateChainConfig` call, but Plan 02 (which makes it async) has not been applied yet. Adding `await` to a synchronous function is harmless but misleading.
- **Fix:** Did NOT add `await`. The call remains synchronous. Plan 02 will add `await` when it modifies chain-router.ts.
- **Files modified:** None (omission is intentional)
- **Verification:** `grep -c 'await generateChainConfig'` returns 0

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Negligible. Plan 02 will handle the async conversion.

## Known Stubs
None.

## Next Phase Readiness
- chains/apply route now resolves Tailscale transport URLs for panel credentials
- panel-sync-client now resolves Tailscale transport URLs before pushing config
- Both paths fall back gracefully when transport resolution fails
- Plan 02 (running in parallel) will make generateChainConfig async; a subsequent integration commit may be needed to add `await` in route.ts

---
*Phase: 12.11-tailscale-transport-chain-apply*
*Completed: 2026-05-02*
