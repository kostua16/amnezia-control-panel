---
phase: 12.11-tailscale-transport-chain-apply
verified: 2026-05-02T12:00:00Z
status: passed
score: 15/15 must-haves verified
overrides_applied: 0
gaps: []
---

# Phase 12.11: Tailscale Transport in Chain Apply Verification Report

**Phase Goal:** Chain apply and routing paths that target remote panels use Tailscale/panel URL transport resolution (resolvePanelTransport / tailnet APIs), not ad hoc hostname with SSH default port.
**Verified:** 2026-05-02T12:00:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | resolvePanelTransport returns Tailscale IP from DB cache (Tier 1) | VERIFIED | `transport-resolver.ts:70-73` -- `server.tailnetIP` used directly |
| 2   | resolvePanelTransport falls back to CLI via tailnetHostname (Tier 2) | VERIFIED | `transport-resolver.ts:77-82` -- `getNodeIP(server.tailnetHostname)` |
| 3   | resolvePanelTransport falls back to CLI via hostname (Tier 3) | VERIFIED | `transport-resolver.ts:86-91` -- `getNodeIP(server.hostname)` |
| 4   | resolvePanelTransport caches resolved IP back to Server on Tier 2/3 | VERIFIED | `transport-resolver.ts:105-123` -- `prisma.server.update` with `tailnetIP` + `tailnetHostname` |
| 5   | resolvePanelTransport returns null when all tiers fail | VERIFIED | `transport-resolver.ts:95-101` -- returns `null`, non-throwing try/catch wrapper at lines 139-146 |
| 6   | resolvePanelTransport constructs panelUrl via `new URL()` | VERIFIED | `transport-resolver.ts:129` -- `new URL('https://${resolvedIP}:${defaultPort}').toString()` |
| 7   | WireGuard peer endpoints use Tailscale IPs, not raw hostname | VERIFIED | `chain-router.ts:112-115` -- `hostname: transport.tailscaleIP`; test at `chain-router.test.ts:175-193` |
| 8   | WireGuard peer port uses Service model or default 51820, not SSH 22 | VERIFIED | `chain-router.ts:88-99` -- `prisma.service.findFirst({ type: 'AWG' })` with `wireguardPort = wireguardService?.port ?? 51820`; test at `chain-router.test.ts:196-211` |
| 9   | chain-config route uses resolvePanelTransport | VERIFIED | `chain-config/route.ts:5` import, `route.ts:316` usage -- `await resolvePanelTransport(server, ...)` |
| 10  | chain-config route endpoints contain Tailscale IPs | VERIFIED | `chain-config/route.ts:319-324` -- `hostname: transport.tailscaleIP` when transport succeeds |
| 11  | generateChainConfig resolves transport for all nodes | VERIFIED | `chain-router.ts:75-131` -- `Promise.all(template.nodes.map(async ... resolvePanelTransport(...)))` |
| 12  | chains/apply builds panelCredentials with Tailscale URLs | VERIFIED | `chains/apply/route.ts:134-149` -- `resolvePanelTransport(server, ...)` then `panelUrl = transport.panelUrl` |
| 13  | chains/apply no longer queries RemotePanel.serverId | VERIFIED | `chains/apply/route.ts:105-106` -- `prisma.remotePanel.findMany({ where: { isActive: true } })` -- no `serverId` filter |
| 14  | pushConfigToAllPanels resolves panelUrl via Tailscale | VERIFIED | `panel-sync-client.ts:211-231` -- `resolvePanelTransport(server, ...)` with fallback try/catch |
| 15  | Panel push URLs contain Tailscale IPs when resolution succeeds | VERIFIED | `panel-sync-client.ts:223-227` -- `panelUrl = transport.panelUrl` passed to `pushConfigToPanel` at line 233 |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/lib/transport-resolver.ts` | 3-tier Tailscale resolver, 60+ lines | VERIFIED | 147 lines; exports `resolvePanelTransport`, `ResolvedTransport`, `__setDeps`, `__resetDeps` |
| `src/lib/__tests__/transport-resolver.test.ts` | Unit tests, 80+ lines | VERIFIED | 144 lines; 6 tests covering all tiers, all-fail, custom port, URL validity |
| `src/lib/chain-router.ts` | Async generateChainConfig with transport, 100+ lines | VERIFIED | 419 lines; `generateChainConfig` is async, uses `resolvePanelTransport` + `prisma.service.findFirst` |
| `src/app/api/panels/push/chain-config/route.ts` | Route using transport resolver, 200+ lines | VERIFIED | 368 lines; imports `resolvePanelTransport`, resolves per-node, fallback to panelUrl parsing |
| `src/app/api/chains/apply/route.ts` | Route with transport + panelCredentials, 120+ lines | VERIFIED | 183 lines; `panelApiKeys` in schema, `await generateChainConfig`, no broken `serverId` query |
| `src/lib/panel-sync-client.ts` | Push with transport resolution, 230+ lines | VERIFIED | 252 lines; `resolvePanelTransport` imported and used in `pushConfigToAllPanels` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `chain-router.ts` | `transport-resolver.ts` | `import resolvePanelTransport` | WIRED | Line 14 import, line 105 usage |
| `chain-router.ts` | `prisma.service` | `prisma.service.findFirst` | WIRED | Lines 91-98, Service model lookup for AWG port |
| `chain-config/route.ts` | `transport-resolver.ts` | `import resolvePanelTransport` | WIRED | Line 5 import, line 316 usage |
| `chains/apply/route.ts` | `transport-resolver.ts` | `import resolvePanelTransport` | WIRED | Line 7 import, line 143 usage |
| `chains/apply/route.ts` | `chain-router.ts` | `await generateChainConfig` | WIRED | Line 4 import, line 80 awaited call |
| `panel-sync-client.ts` | `transport-resolver.ts` | `import resolvePanelTransport` | WIRED | Line 6 import, line 224 usage |
| `transport-resolver.ts` | `tailscale.ts` | `import getNodeIP, isReachable` | WIRED | Line 63 dynamic import: `_deps ?? (await import('./tailscale'))` |
| `transport-resolver.ts` | `prisma.server` | `prisma.server.update` | WIRED | Line 107 dynamic import, line 108 update for cache-back |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `chain-router.ts` | `transport.tailscaleIP` | `resolvePanelTransport` -> `getNodeIP` / DB | Real: DB cache or CLI resolution | FLOWING |
| `chain-config/route.ts` | `transport.tailscaleIP` | `resolvePanelTransport` -> `getNodeIP` / DB | Real: DB cache or CLI resolution | FLOWING |
| `chains/apply/route.ts` | `panelUrl` | `resolvePanelTransport` -> `getNodeIP` / DB | Real: DB cache or CLI resolution; fallback to `matchedPanel.panelUrl` | FLOWING |
| `panel-sync-client.ts` | `panelUrl` | `resolvePanelTransport` -> `getNodeIP` / DB | Real: DB cache or CLI resolution; fallback to `panel.panelUrl` | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| transport-resolver tests (6/6) | `node --import tsx src/lib/__tests__/transport-resolver.test.ts` | pass 6, fail 0 | PASS |
| chain-router tests (9/9) | `node --import tsx src/lib/__tests__/chain-router.test.ts` | pass 9, fail 0 | PASS |
| panel-sync-client tests (18/18) | `node --import tsx src/lib/__tests__/panel-sync-client.test.ts` | pass 18, fail 0 | PASS |
| config-applier tests (7/7) | `node --import tsx src/lib/__tests__/config-applier.test.ts` | pass 7, fail 0 | PASS |
| resolvePanelTransport imported in all 4 consumer files | `grep -c resolvePanelTransport` across src/ | 4 consumer files + test files | PASS |
| await generateChainConfig in chains/apply | `grep "await generateChainConfig" chains/apply/route.ts` | Line 80 match | PASS |
| No broken serverId query on RemotePanel | `grep "serverId.*in.*chainConfig.nodes" chains/apply/route.ts` | 0 matches | PASS |
| No SSH port 22 in WG endpoint generation | `grep "server.port" chain-router.ts` | 0 matches in endpoint context | PASS |
| panelUrl uses new URL() not string concat | `grep -c "new URL(" transport-resolver.ts` | 1 match (line 129) | PASS |
| 51820 default WG port present | `grep "51820" chain-router.ts` | Line 89 | PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| ----------- | ------------ | ----------- | ------ | -------- |
| TSCL-04 | 12.11-01, 12.11-02, 12.11-03 | Panel uses Tailscale IPs as transport addresses for inter-panel communication | SATISFIED | `resolvePanelTransport` wired into chain-router, chain-config route, chains/apply route, panel-sync-client |
| CHAIN-01 | 12.11-02, 12.11-03 | Pushed chain config is actually applied to AWG and 3x-ui services on remote servers (transport slice) | SATISFIED | `generateChainConfig` uses Tailscale IPs for WG peer endpoints; `chains/apply` builds panelCredentials with resolved URLs |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `panel-sync-client.ts` | 62 | `configVersion: 0` comment "Placeholder -- real version set at push time" | Info | Pre-existing; not introduced by 12.11; overwritten at push time (line 208) |

No blockers. No warnings introduced by this phase.

### Human Verification Required

None -- all truths verified programmatically through code inspection, import tracing, and test execution.

### Gaps Summary

No gaps found. All 15 must-have truths verified across 3 plans. All 5 key artifacts exist and are substantive (meet minimum line counts). All 8 key links are wired. All 40 tests pass (6 + 9 + 18 + 7). Requirements TSCL-04 and CHAIN-01 (transport slice) are satisfied.

---

_Verified: 2026-05-02T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
