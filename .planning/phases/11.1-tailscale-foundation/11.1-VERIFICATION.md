---
phase: 11.1-tailscale-foundation
verified: 2026-05-01T16:52:00Z
status: passed
score: 8/8 must-haves verified
gaps: []
human_verification: []
---

# Phase 11.1: Tailscale Foundation Verification Report

**Phase Goal:** Tailscale infrastructure foundation -- TypeScript types, Prisma schema extension, CLI utility, setup wizard APIs, node listing, and transport address resolution for multi-panel communication.
**Verified:** 2026-05-01T16:52:00Z
**Status:** passed
**Re-verification:** Initial verification (no prior VERIFICATION.md)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Admin can configure Tailscale subnet router on any server via step-by-step API wizard (TSCL-01) | VERIFIED | POST /api/tailscale/setup/verify dispatches 5 steps (install, auth, ip-forwarding, subnet-config, tailnet-check). POST /api/tailscale/setup/advertise calls TailscaleManager.advertiseRoutes() with zod + internal CIDR double validation. Setup auto-detects default subnets (10.0.0.0/24 for AmneziaWG, 172.16.0.0/12 for Xray). |
| 2 | Central panel can list all Tailscale nodes in the tailnet with IPs, hostnames, and online status (TSCL-02) | VERIFIED | GET /api/tailscale/nodes returns normalized TailscaleNodeInfo[] (id, hostname, dnsName, tailscaleIPs, online, os, relay, primaryRoutes, isSelf). Self node separated, onlineCount and version included. TailscaleManager.getNodes() wraps `tailscale status --json` and normalizes peers. |
| 3 | Panel uses Tailscale IPs (not public IPs) as transport addresses for inter-panel API calls (TSCL-03) | VERIFIED | TailscaleManager.resolveTransportAddress() returns { tailscaleIP, hostname, port: 443 }. GET /api/servers/:id/tailnet implements 3-tier fallback: DB tailnetIP -> CLI resolve via tailnetHostname -> fallback via hostname. Resolved IPs cached to Server record. Server model has panelUrl field configurable as Tailscale IP. |
| 4 | Each server's VPN subnet is properly advertised and reachable from other nodes in the tailnet (TSCL-04) | VERIFIED | Server.advertisedSubnets field in Prisma schema stores JSON-encoded subnet list. TailscaleManager.advertiseRoutes() calls `tailscale set --advertise-routes=` with CIDR validation. Setup wizard advertises subnets and confirms routes reflected in AllowedIPs/PrimaryRoutes post-call. |
| 5 | TailscalePeer and TailscaleNodeInfo TypeScript interfaces defined | VERIFIED | src/types/tailscale.ts: TailscalePeer (mirrors tailscale status --json), TailscaleStatus, TailscaleNodeInfo (normalized, no sensitive fields), TransportAddress. |
| 6 | Server Prisma model extended with tailnet fields | VERIFIED | prisma/schema.prisma: tailnetIP (String?), tailnetHostname (String?), dnsName (String?), advertisedSubnets (String @default("[]"), JSON-encoded). TypeScript Server/ServerCreate/ServerUpdate interfaces updated in src/types/server.ts. |
| 7 | TailscaleManager CLI utility with security mitigations | VERIFIED | src/lib/tailscale.ts: 8 exported async functions (verifyInstalled, getStatus, getNodes, getNodeIP, isReachable, advertiseRoutes, getBackendState, resolveTransportAddress). Uses execFile (array args) to prevent shell injection (T-11.1-01). Binary fallback pattern (D-04). toNodeInfo strips PublicKey (T-11.1-02). CIDR validation before advertiseRoutes CLI call. |
| 8 | Tailscale status endpoint with sanitized peer data | VERIFIED | GET /api/tailscale/status returns self info, backendState, peerCount, and TailscaleNodeInfo[] peers. Sensitive fields (PublicKey, CurAddr, RxBytes, TxBytes, LastSeen, AllowedIPs) stripped by TailscaleManager.toNodeInfo (T-11.1-02, T-11.1-09). |

**Score:** 8/8 truths verified

### Deferred Items

| Item | Reason | Future Phase |
|------|--------|-------------|
| Dashboard UI for Tailscale setup wizard | No .tsx consumer in src/ calls any /api/tailscale/* endpoint. Setup wizard APIs exist but have no frontend UI. Admin must use API calls directly. | Future UI phase |
| E2E Tailscale network verification | Requires running tailscaled daemon and a real tailnet with multiple nodes. | Operator-level test |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/types/tailscale.ts` | TailscalePeer, TailscaleStatus, TailscaleNodeInfo, TransportAddress | VERIFIED | 4 interfaces defined. TailscaleNodeInfo is the normalized shape used in API responses. |
| `src/types/server.ts` | Extended Server with tailnet fields | VERIFIED | tailnetIP, tailnetHostname, dnsName, advertisedSubnets added to Server, ServerCreate, ServerUpdate |
| `prisma/schema.prisma` | Server model tailnet fields | VERIFIED | 4 nullable fields: tailnetIP, tailnetHostname, dnsName; advertisedSubnets String @default("[]") (JSON-encoded) |
| `src/lib/tailscale.ts` | TailscaleManager utility | VERIFIED | 8 exported async functions. runTailscale() with execFile, binary fallback, 10s timeout, structured CliResult. |
| `src/app/api/tailscale/setup/verify/route.ts` | 5-step setup wizard POST | VERIFIED | Steps: install, auth, ip-forwarding, subnet-config, tailnet-check. Zod validation. CIDR overlap detection (D-06). Auto-default subnets (D-05). |
| `src/app/api/tailscale/setup/advertise/route.ts` | Subnet route advertisement POST | VERIFIED | Zod + internal double CIDR validation (T-11.1-05). Post-call route confirmation. |
| `src/app/api/tailscale/status/route.ts` | Node status GET | VERIFIED | Self info, backendState, peerCount, sanitized TailscaleNodeInfo[] peers. |
| `src/app/api/tailscale/nodes/route.ts` | Node listing GET | VERIFIED | All tailnet nodes (self + peers) as TailscaleNodeInfo[]. selfNode, totalNodes, onlineCount, version. 503 when Tailscale not running. |
| `src/app/api/servers/[id]/tailnet/route.ts` | Per-server transport address resolution GET | VERIFIED | 3-tier fallback (D-07): DB tailnetIP -> CLI via tailnetHostname -> fallback via hostname. Online status from tailscale (D-08). IP caching to DB. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `setup/verify/route.ts` | `src/lib/tailscale.ts` | `import { verifyInstalled, getStatus, getBackendState, getNodes }` | WIRED | Lines 6-9 |
| `setup/advertise/route.ts` | `src/lib/tailscale.ts` | `import { advertiseRoutes, getStatus }` | WIRED | Lines 3-4 |
| `status/route.ts` | `src/lib/tailscale.ts` | `import { getStatus, getNodes, getNodeIP }` | WIRED | Lines 2-3 |
| `nodes/route.ts` | `src/lib/tailscale.ts` | `import { getNodes, getStatus }` | WIRED | Lines 1-2 |
| `servers/[id]/tailnet/route.ts` | `src/lib/tailscale.ts` | `import { getNodeIP, isReachable }` | WIRED | Lines 3-4 |
| `servers/[id]/tailnet/route.ts` | `prisma.server` | `findUnique / update` | WIRED | Lines 31-33, 85-91 |
| `tailscale.ts` | `src/types/tailscale.ts` | `import type { TailscaleStatus, TailscalePeer, TailscaleNodeInfo, TransportAddress }` | WIRED | Lines 3-8 |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| TSCL-01 | Admin can configure Tailscale subnet router on any server | SATISFIED | POST /api/tailscale/setup/verify (5 steps), POST /api/tailscale/setup/advertise (CIDR validation), auto-default subnets, route confirmation |
| TSCL-02 | Central panel lists all Tailscale nodes with IPs, hostnames, online status | SATISFIED | GET /api/tailscale/nodes returns TailscaleNodeInfo[] with all required fields. TailscaleManager.getNodes() normalizes from CLI output. |
| TSCL-03 | Panel uses Tailscale IPs as transport addresses | SATISFIED | resolveTransportAddress(), GET /api/servers/:id/tailnet (3-tier fallback + caching), Server.panelUrl configurable as Tailscale IP |
| TSCL-04 | VPN subnets properly advertised and reachable | SATISFIED | Server.advertisedSubnets in schema, advertiseRoutes() with validation, setup wizard handles advertisement with post-call confirmation |

### Anti-Patterns Found

No blocker anti-patterns. All API routes are substantive with real CLI calls, proper error handling, zod validation, and structured response envelopes. No TODO/FIXME/placeholder comments.

### Human Verification Required

None for code-level verification. Full E2E validation (Tailscale setup wizard end-to-end, multi-node tailnet reachability) requires a running Tailscale network and is an operator-level test.

### Gaps Summary

No code-level gaps. All TSCL-01 through TSCL-04 requirements are satisfied at the API/backend level.

**Note:** No dashboard UI exists for Tailscale setup. The setup wizard APIs are backend-only; an admin must invoke them via API calls or a future UI phase. This does not block TSCL requirements which specify "admin can configure" and "panel lists" -- the APIs fulfill these requirements. A future UI phase would provide the operator-facing frontend.

---
_Verified: 2026-05-01T16:52:00Z_
_Verifier: Claude (gsd-execute-phase 12.7)_
