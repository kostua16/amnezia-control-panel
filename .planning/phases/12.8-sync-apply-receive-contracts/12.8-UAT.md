---
status: resolved
phase: 12.8-sync-apply-receive-contracts
source: 12.8-01-SUMMARY.md, 12.8-02-SUMMARY.md
started: 2026-05-02T01:15:00+04:00
updated: 2026-05-02T21:00:00+04:00
---

## Current Test

[testing complete]

## Tests

### 1. config-applier unit tests (7 tests)
expected: All 7 config-applier tests pass (applyAwgConfig: 5, applyThreeXuiConfig: 2). Covers auth headers, response unwrap, HMAC verification.
result: pass

### 2. panel-sync-client unit tests (16 tests)
expected: All 16 panel-sync-client tests pass. Covers signPayload, verifySignature, generatePerPanelConfig, pushConfigToPanel.
result: pass

### 3. chain-router unit tests (5 tests)
expected: All 5 chain-router tests pass. Covers panelUrl usage, apiKey forwarding, missing-credential errors, full success path, sourceIp passthrough.
result: issue
reported: "Cannot find package '@/lib/prisma' imported from geo-routing.ts — chain-router imports resolveGeoRoute which imports @/lib/prisma, not mocked in test environment"
severity: major

### 4. HMAC auth headers in config-applier code
expected: applyAwgConfig, applyThreeXuiConfig, applyPanelConfig accept apiKey parameter, sign payloads with signPayload(), send X-API-Key + X-Signature headers.
result: pass

### 5. Response unwrap in config-applier and panel-sync-client
expected: Both files correctly unwrap resp.data before checking applied / configVersion. SyncReceiveResponse type uses nested { success, data } shape.
result: pass

### 6. SyncApplyResponse and SyncReceiveResponse types
expected: Types updated: SyncReceiveResponse is nested { success, data }. SyncApplyResponse and SyncApplyResponseData types exist in panel-sync.ts.
result: pass

### 7. chain-router panelCredentials parameter
expected: applyChainConfig accepts optional panelCredentials: Map<number, { panelUrl, apiKey }>. Uses creds.panelUrl instead of http://hostname:port. Skips nodes without credentials with error.
result: pass

### 8. chains/apply route updated with empty panelCredentials
expected: Route builds empty panelCredentials map (bcrypt hashes only). TODO comment references Phase 12.10.
result: pass

### 9. No old http://hostname:port pattern remains in chain-router
expected: Grep for http://${node.hostname} in chain-router.ts returns zero matches.
result: pass

## Summary

total: 9
passed: 8
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "All 5 chain-router tests pass"
  status: resolved
  reason: "Cannot find package '@/lib/prisma' imported from geo-routing.ts — chain-router imports resolveGeoRoute which imports @/lib/prisma, not mocked in test environment"
  severity: major
  test: 3
  root_cause: "chain-router.ts imports resolveGeoRoute from geo-routing.ts, which has runtime value imports (import { prisma } from '@/lib/prisma', import { lookupGeoIP } from '@/lib/geoip-manager'). No vitest.config.ts exists to resolve @/ path aliases. config-applier and panel-sync-client tests only use @/ for type-only imports (erased at compile time), so they pass. chain-router.test.ts is the first to hit runtime @/ resolution failure."
  artifacts:
    - path: "src/lib/geo-routing.ts"
      issue: "Runtime @/ value imports require path alias resolution"
    - path: "src/lib/__tests__/chain-router.test.ts"
      issue: "Missing mock for ./geo-routing module"
  missing:
    - "Mock geo-routing module in chain-router.test.ts so @/lib/prisma is never imported at test runtime"
    - "Optionally add vitest.config.ts with resolve.alias for @/ -> src/"
