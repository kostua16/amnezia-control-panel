---
plan: 12.8-02
phase: 12.8
status: complete
---

# Plan 12.8-02: Add panelCredentials to chain-router and update chains/apply route

## What was built

- **panelCredentials map parameter**: `applyChainConfig` accepts optional `panelCredentials: Map<number, { panelUrl: string; apiKey: string }>` for per-node credential lookup
- **Removed http://hostname:port**: chain-router no longer constructs ad-hoc URLs; uses `creds.panelUrl` from credentials map
- **Credential lookup with error**: Nodes without matching credentials are skipped with descriptive error message
- **chains/apply route updated**: Builds empty `panelCredentials` map (plaintext keys unavailable from DB — bcrypt hashes only). TODO comment added for Phase 12.10
- **Tests created**: `chain-router.test.ts` with 5 tests covering panelUrl usage, apiKey forwarding, missing-credential errors, full success path, sourceIp passthrough

## Key files

- `src/lib/chain-router.ts` — panelCredentials parameter, credential-aware node loop
- `src/app/api/chains/apply/route.ts` — empty panelCredentials construction with TODO
- `src/lib/__tests__/chain-router.test.ts` — new, 5 tests

## Deviations

None.

## Self-Check: PASSED

- All 5 chain-router tests pass
- All 7 config-applier tests pass (no regressions)
- All 16 panel-sync-client tests pass (no regressions)
- Grep verification: `panelCredentials` present in chain-router and chains/apply route, old `http://${node.hostname}` pattern absent from chain-router, `applyPanelConfig` called with 4 args (panelUrl, label, apiKey, payload)
