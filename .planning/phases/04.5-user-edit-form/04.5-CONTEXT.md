# Phase 4.5: User Edit Form - Context

**Gathered:** 2026-04-29
**Status:** Ready for planning

<domain>
Build form to edit user settings (name, limits, protocols). Similar to create form but pre-filled. Reuse Dialog component from Phase 4.3. Wire to PUT /api/users/[id].

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices at Claude's discretion — reuses create-form pattern. Edit form in modal, pre-filled, same fields minus password (or optional password change).

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- Dialog component (src/components/ui/dialog.tsx)
- CreateUserForm pattern (src/components/users/create-user-form.tsx)
- UpdateUserPayload type in src/types/user.ts
- UserList with Edit button placeholder (console.log)

### Integration Points
- user-list.tsx — Edit button → open edit modal
- PUT /api/users/[id] — Phase 4.6
- src/lib/vpn-services.ts — update user in VPN services

</code_context>

<specifics>
No specific requirements — standard edit form.

</specifics>

<deferred>
None.
