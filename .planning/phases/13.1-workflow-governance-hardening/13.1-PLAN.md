# Phase 13.1 Plan: Workflow governance hardening

## Goal
Centralize GitHub workflow trust policy so maintainer-only triggers, trusted automation branches, blocking labels, and manual-only paths are defined once and enforced consistently.

## Scope
- Keep maintainer associations in one policy file.
- Distinguish trusted automation branches from manual-only workflow and planning branches.
- Enforce manual-only handling for `.github/**` and `.planning/**` changes.
- Route issue auto-fix through explicit maintainer intent rather than broad labels.

## Deliverables
- Shared workflow policy file and helpers
- Maintainer-gated `@claude` and `/fix` flows
- Manual-only branch/path enforcement for downstream workflows

## Intake
<!-- AUTO-PR-IMPROVE-START -->
<!-- PR-IMPROVE:181 START -->
### Intake from PR #181: fix(workflows): harden planning PR helper output
- Source: https://github.com/kostua16/amnezia-control-panel/pull/181
- Formalize stdout-isolation trust gate for JSON-emitting workflow scripts -- PR #181's core fix routes capture:false child stdout to stderr so it cannot corrupt JSON on stdout. This pattern should be a codified trust gate: any script whose stdout is consumed as structured data (upsert-planning-pr.cjs, evaluate-pr-policy.cjs, resolve-pr-context.cjs) must guarantee child commands never reach stdout. Currently only upsert-planning-pr.cjs enforces this; the other scripts use console.log/error inconsistently. (owner: maintainer)
<!-- PR-IMPROVE:181 END -->

<!-- AUTO-PR-IMPROVE-END -->
