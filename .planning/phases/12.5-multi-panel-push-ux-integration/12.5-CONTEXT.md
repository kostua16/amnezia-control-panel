# Phase 12.5: Multi-panel push UX integration - Context

**Gathered:** 2026-05-01
**Status:** Ready for planning

<domain>
## Phase Boundary

Default admin push flow uses signed remote panel push (diff, progress, rollback, actionable errors). PushWizard is mounted on `/panels/push` as the primary push experience. ChainFlowEditor shows panel boundaries via `panels`/`serverPanelMap` props.

Gap closure from v1.1 audit: CPUSH-01–06, CHAIN-01, VISED-03 all flagged as "partial" because PushWizard exists but isn't mounted on the actual push page.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices at Claude's discretion — frontend integration phase.

Key constraints:
- `PushWizard` component exists at `src/components/push/push-wizard.tsx` — fully implemented with 4-step wizard (select panels, preview diff, push, results)
- PushWizard requires `panels: PushWizardPanel[]` prop
- Current `/panels/push/page.tsx` renders `ChainFlowEditor` with only `servers` prop (line 66)
- ChainFlowEditor accepts `panels` and `serverPanelMap` props for panel boundary display (per 11.6-VERIFICATION.md)
- Need to build `serverPanelMap` from panels + servers data (map server ID to panel ID)
- The push page should show PushWizard when panels exist, with ChainFlowEditor as optional visual context

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/components/push/push-wizard.tsx` — full 4-step wizard (panel selector, diff preview, push, results)
- `src/components/push/config-diff-view.tsx` — diff display
- `src/components/chains/chain-flow-editor.tsx` — visual chain editor with panel boundary support
- `src/app/api/panels/push/route.ts` — push endpoint
- `src/app/api/panels/push/chain-config/route.ts` — per-panel chain config
- `src/app/api/panels/diff/route.ts` — config diff endpoint
- `src/app/api/panels/rollback/route.ts` — rollback endpoint

### Established Patterns
- Panel data: `{ id, name, panelUrl, isActive, createdAt, updatedAt }`
- Server data: `{ id, hostname, ip, isActive, ... }`
- Fetch pattern: `fetch('/api/...').then(res => res.json())`
- UI: Tailwind CSS, shadcn/ui components (Card, Button, etc.)

### Integration Points
- `src/app/(dashboard)/panels/push/page.tsx` — must be rewritten to mount PushWizard
- `src/components/chains/chain-flow-editor.tsx` — add panels/serverPanelMap props when used on push page

</code_context>

<specifics>
## Specific Ideas

No specific requirements — close CPUSH/CHAIN/VISED gaps from audit.

</specifics>

<deferred>
## Deferred Ideas

Advanced push scheduling, batch rollback across panels — future phases.

</deferred>
