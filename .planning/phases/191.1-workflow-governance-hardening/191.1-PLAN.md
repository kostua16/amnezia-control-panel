# Phase 191.1 Plan: Workflow governance hardening

## Goal
Centralize GitHub workflow trust policy so maintainer-only triggers, trusted automation branches, blocking labels, and manual-only paths are defined once and enforced consistently.

## Scope
- Keep maintainer associations in one policy file.
- Distinguish trusted automation branches from manual-only workflow and planning branches.
- Enforce manual-only handling for `.github/**` and `.planning/**` changes.
- Route issue auto-fix through explicit maintainer intent rather than broad labels.

## Deliverables
- Shared workflow policy file and helpers
- Maintainer-gated `@claude` and `/fix` flows
- Manual-only branch/path enforcement for downstream workflows

## Intake
<!-- AUTO-PR-IMPROVE-START -->
<!-- PR-IMPROVE:191 START -->
### Intake from PR #191: [codex] ci: orchestrate PR automation flow
- Source: https://github.com/kostua16/amnezia-control-panel/pull/191
- Centralize trust policy in the orchestrator with maintainer-only event gates -- PR #191 removes pull_request/pull_request_target triggers from worker workflows and routes everything through pr-flow.yml. This is the foundation for Phase 191.1's central policy, but the trust boundary is incomplete: (1) the orchestrator still relies on evaluate-pr-policy.cjs as a separate script rather than a unified policy surface; (2) issue_comment /review dispatches bypass the orchestrator entirely and go straight to code-review.yml; (3) manual-only path rules (policy.json manualOnlyPathGlobs) are evaluated per-worker rather than centrally. The orchestrator should become the single trust gate that all events pass through. (owner: maintainer)
<!-- PR-IMPROVE:191 END -->

<!-- AUTO-PR-IMPROVE-END -->
