---
phase: 999
plan: 999-113
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
source_artifact: ".planning/quick/260714-pr754-workflow-improve/260714-pr754-PLAN.md"
source_artifact_sha256: 9f38ad92cd7d95d6cabc19c7cc0b16f57b2d2a13160a48d8aa206b18a479bd4a
source_pr: "754"
must_haves:
  truths:
    - "Use the merged source artifact as the implementation specification."
    - "Keep implementation changes scoped and reviewable."
  artifacts:
    - path: ".planning/quick/260714-pr754-workflow-improve/260714-pr754-PLAN.md"
      provides: "Merged planning artifact selected from main."
  key_links:
    - ".planning/quick/260714-pr754-workflow-improve/260714-pr754-PLAN.md"
---

# Plan 999-113: Quick Plan: PR #754 workflow improvement intake

## Source
- Artifact: `.planning/quick/260714-pr754-workflow-improve/260714-pr754-PLAN.md`
- SHA-256: `9f38ad92cd7d95d6cabc19c7cc0b16f57b2d2a13160a48d8aa206b18a479bd4a`
- Source PR: #754
- Queue: `.planning/phases/999-gh-planning-execution-queue`

## Goal
Execute the merged planning artifact as a concrete repository change.

## Implementation Instructions
- Read the source artifact before editing code.
- Translate the artifact into the smallest useful implementation.
- Keep changes within the artifact intent; leave unrelated refactors alone.
- Add or update tests when behavior changes.
- Do not commit, push, merge, or open pull requests; workflow automation handles Git operations.

## Acceptance Criteria
- The implementation addresses the selected planning artifact.
- Relevant tests, linting, and formatting checks pass or any blockers are documented in the PR body.
- The resulting PR remains reviewable and can be risk-classified by workflow policy.

## Source Artifact Snapshot

```markdown
# Quick Plan: PR #754 workflow improvement intake

## Source
- PR #754: fix: resolve #731 - WHO-E01: Persist per-run health summaries for trend analysis (workflow-health-optimize)
- URL: https://github.com/kostua16/amnezia-control-panel/pull/754

## Summary
PR #754 adds persist-health-summary.cjs (JSONL writer) + artifact upload step to workflow-health-optimize.yml's collect-runs job. No unit tests for the new script. E2E scenarios doc not updated. Analysis complete.

## Quick Wins
- Add unit tests for persist-health-summary.cjs -- New script at .github/workflows/scripts/persist-health-summary.cjs has zero test coverage. Repo convention: every workflow script under scripts/ has a corresponding __tests__/*.test.cjs. Cover empty array → null, normal array writes JSONL with newline trailer, custom opts (outputDir, prefix), timestamp formatting. (owner: claude, type: quick-task)
- Update docs/workflow-e2e-scenarios.md for WHO-E01 artifact path -- PR adds upload-artifact step (health-summary-${{ github.run_id }}, retention-days: 7) but docs/workflow-e2e-scenarios.md has no mention. CLAUDE.md 'Workflow change protocol' mandates e2e doc update on workflow behavior changes. (owner: claude, type: workflow-improvement)
- Guard summary-path output when persist returns null -- In workflow-health-optimize.yml collect step, core.setOutput('summary_path', summaryPath) runs unconditionally. When runSummaries is empty, persistHealthSummary returns null and setOutput writes literal 'null' string. Downstream upload step checks summary_path != '' which passes for 'null'. Wrap in if (summaryPath) guard. (owner: claude, type: quick-task)

## pr754.x Phase Candidates
- pr754.2: Feed persisted health summaries into the optimize job Claude prompt -- Optimize job only receives truncated runs_data via job output. The persisted JSONL artifact from collect-runs is uploaded but never consumed downstream. Wire download-artifact in optimize job and pass trend context into the Claude prompt — completes the WHO-E01 'trend analysis' goal. (owner: claude)
- pr754.2: Aggregate health summaries across hourly runs into a rolling artifact -- Each hourly run produces a separate health-summary-*.jsonl with 7-day retention but no merge step exists. Add lightweight merge in collect-runs: download previous artifact, append new entries, re-upload. Builds the 'CI 20% slower this week' signal the PR promises. (owner: claude)
```
