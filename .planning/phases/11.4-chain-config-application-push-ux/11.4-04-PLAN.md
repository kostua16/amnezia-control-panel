---
phase: 11.4-chain-config-application-push-ux
plan: 04
type: execute
wave: 3
depends_on: ["11.4-01", "11.4-02", "11.4-03"]
files_modified:
  - src/app/(dashboard)/panels/push/page.tsx
  - src/components/push/push-wizard.tsx
  - src/components/push/panel-selector.tsx
  - src/components/push/config-diff-view.tsx
  - src/components/push/push-progress-tracker.tsx
  - src/components/push/push-result-summary.tsx
  - src/components/push/error-recommendation.tsx
autonomous: false
requirements: [CPUSH-04, CPUSH-05, CPUSH-06]
user_setup: []

must_haves:
  truths:
    - "Admin sees a multi-step push wizard at /panels/push with 4 steps: Select Panels, Preview Changes, Push, Results"
    - "Step 1 shows all registered panels with checkboxes and status badges, pre-selects connected panels"
    - "Step 2 shows side-by-side config diff per panel (current vs new) with green/red highlight"
    - "Step 3 shows real-time per-panel push progress via WebSocket with spinner/success/failure indicators"
    - "Step 4 shows summary with succeeded/failed counts and per-panel rollback buttons"
    - "Rollback is single-click per panel (no confirmation dialog per CONTEXT.md D-03)"
    - "Push errors show structured message, recommendation, and collapsible known fix"
  artifacts:
    - path: "src/app/(dashboard)/panels/push/page.tsx"
      provides: "Push configuration page at /panels/push route"
    - path: "src/components/push/push-wizard.tsx"
      provides: "Multi-step wizard container managing step state"
    - path: "src/components/push/panel-selector.tsx"
      provides: "Panel checklist with status badges and select-all"
    - path: "src/components/push/config-diff-view.tsx"
      provides: "Side-by-side diff display per panel"
    - path: "src/components/push/push-progress-tracker.tsx"
      provides: "Per-panel real-time push progress with WebSocket"
    - path: "src/components/push/push-result-summary.tsx"
      provides: "Push results summary with rollback buttons"
    - path: "src/components/push/error-recommendation.tsx"
      provides: "Inline error display with recommendation and known fix"
  key_links:
    - from: "src/app/(dashboard)/panels/push/page.tsx"
      to: "src/components/push/push-wizard.tsx"
      via: "import PushWizard"
      pattern: "import.*PushWizard.*from.*push-wizard"
    - from: "src/components/push/push-wizard.tsx"
      to: "src/components/push/panel-selector.tsx"
      via: "import PanelSelector"
      pattern: "import.*PanelSelector"
    - from: "src/components/push/push-wizard.tsx"
      to: "src/components/push/config-diff-view.tsx"
      via: "import ConfigDiffView"
      pattern: "import.*ConfigDiffView"
    - from: "src/components/push/push-wizard.tsx"
      to: "src/components/push/push-progress-tracker.tsx"
      via: "import PushProgressTracker"
      pattern: "import.*PushProgressTracker"
    - from: "src/components/push/push-wizard.tsx"
      to: "src/components/push/push-result-summary.tsx"
      via: "import PushResultSummary"
      pattern: "import.*PushResultSummary"
    - from: "src/components/push/push-wizard.tsx"
      to: "/api/panels/diff"
      via: "fetch POST /api/panels/diff"
      pattern: "/api/panels/diff"
    - from: "src/components/push/push-wizard.tsx"
      to: "/api/panels/push"
      via: "fetch POST /api/panels/push"
      pattern: "/api/panels/push"
    - from: "src/components/push/push-result-summary.tsx"
      to: "/api/panels/rollback"
      via: "fetch POST /api/panels/rollback"
      pattern: "/api/panels/rollback"
    - from: "src/components/push/push-progress-tracker.tsx"
      to: "socket.io"
      via: "socket.on('panel:push-progress')"
      pattern: "panel:push-progress"
---

<objective>
Build the Push Configuration UI page at `/panels/push` with a 4-step wizard: Select Panels, Preview Changes, Push, Results. Implements the full push UX with real-time WebSocket progress, side-by-side config diff, one-click rollback, and structured error display with recommendations.

Purpose: This is the admin-facing interface for the chain config push workflow. Per CONTEXT.md D-04, push flow is preview -> confirm -> push -> results (multi-step wizard). Per D-01, UI location is `/panels/push`. Per UI-SPEC, copywriting and visual design follow the contract exactly.

Output: Complete push wizard page with all 6 new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.4-chain-config-application-push-ux/11.4-UI-SPEC.md
@.planning/phases/11.4-chain-config-application-push-ux/11.4-CONTEXT.md

<interfaces>
From src/components/ui/button.tsx:
```typescript
type ButtonVariant = 'default' | 'destructive' | 'outline' | 'ghost';
type ButtonSize = 'default' | 'sm' | 'lg' | 'icon';
// Button accepts variant, size, className, and standard button props
```

From src/components/ui/card.tsx:
```typescript
// Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
```

From src/components/panels/panel-status-badge.tsx:
```typescript
interface PanelStatusBadgeProps { status: PanelConnectionStatus; }
export type PanelConnectionStatus = 'connected' | 'degraded' | 'offline' | 'unknown';
```

From src/types/config-push.ts:
```typescript
export interface ConfigDiffResult {
  panelId: number; panelName: string; hasChanges: boolean;
  sections: ConfigDiffSection[];
  currentConfigFormatted: string | null; newConfigFormatted: string;
}
export interface ConfigDiffSection {
  label: string; lines: ConfigDiffLine[];
  summary: { added: number; removed: number; unchanged: number };
}
export interface ConfigDiffLine {
  type: 'added' | 'removed' | 'unchanged'; content: string;
}
export interface PushProgressEvent {
  panelId: number; panelName: string;
  status: 'pending' | 'pushing' | 'applying' | 'success' | 'failed';
  service?: 'awg' | 'three_xui'; latencyMs?: number;
  error?: StructuredPushError; timestamp: string;
}
export interface StructuredPushError {
  type: 'connection_timeout' | 'auth_failure' | 'invalid_config' | 'service_error' | 'docker_error' | 'unknown';
  message: string; recommendation: string; knownFix: string | null; rawError: string | null;
}
```

From src/types/panel-sync.ts:
```typescript
export interface PushResult {
  panelId: number; panelName: string; success: boolean;
  configVersion: number | null; latencyMs: number | null;
  error: string | null; retries: number;
}
export interface PushAllResult {
  totalPanels: number; succeeded: number; failed: number;
  results: PushResult[]; configVersion: number; pushedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create push wizard container and panel selector components</name>
  <files>src/app/(dashboard)/panels/push/page.tsx, src/components/push/push-wizard.tsx, src/components/push/panel-selector.tsx</files>
  <read_first>
    - src/app/(dashboard)/panels/page.tsx (existing panels page for pattern reference)
    - src/components/panels/panel-list.tsx (existing panel list for pattern reference)
    - .planning/phases/11.4-chain-config-application-push-ux/11.4-UI-SPEC.md (copywriting and interaction patterns)
  </read_first>
  <action>
**Create `src/app/(dashboard)/panels/push/page.tsx`:**
- 'use client' directive
- Page heading: "Push Configuration" (text-2xl font-bold per UI-SPEC)
- Subtitle: "Preview and push chain configuration to panels" (text-muted-foreground text-sm)
- Renders `<PushWizard />`
- Fetches available chain configs from the existing chain system (GET /api/chains or similar -- check what exists)
- If no chains are available, show empty state: "No configuration changes to push" / "Select a chain and panels to preview and push configuration changes."

**Create `src/components/push/push-wizard.tsx`:**
- 'use client' directive
- Manages wizard step state: `currentStep: 1 | 2 | 3 | 4`
- Step indicator bar at top showing 4 steps: "1. Select Panels" / "2. Preview Changes" / "3. Push" / "4. Results"
  - Active step: text-accent, font-bold
  - Completed step: text-green-500 with check icon
  - Future step: text-muted-foreground
- Props: `chains` (available chain configs), `onComplete` callback
- State management:
  - `selectedPanelIds: Set<number>` (panels selected for push)
  - `diffResults: ConfigDiffResult[]` (from diff API)
  - `pushResults: PushAllResult | null` (from push API)
  - `pushProgress: Map<number, PushProgressEvent>` (from WebSocket)
  - `selectedChain: ChainConfig | null`
- Navigation buttons:
  - Step 1->2: "Next: Preview Changes" (Button variant="outline"), disabled if no selection
  - Step 2->1: "Back" (Button variant="ghost")
  - Step 2->3: "Push Configuration" (Button variant="default", bg-accent per UI-SPEC)
  - Step 3: no back navigation (cannot navigate back during active push per UI-SPEC)
  - Step 4: "Push Again" (Button variant="outline") and "Back to Panels" (link to /panels)
- Step content renders the appropriate child component based on currentStep
- On step 2 transition: calls POST /api/panels/diff with chainConfig, stores diffResults
- On step 3 transition: calls POST /api/panels/push with chainConfig + panelApiKeys, listens for WebSocket events
- Sets up Socket.io connection for 'panel:push-progress' events, updates pushProgress map
- 30-second polling fallback if Socket.io unavailable (GET /api/panels/push/status per UI-SPEC)
- Auto-advances to step 4 when all panels complete

**Create `src/components/push/panel-selector.tsx`:**
- 'use client' directive
- Props: `panels: RemotePanelWithHistory[]`, `selectedPanelIds: Set<number>`, `onSelectionChange: (ids: Set<number>) => void`
- Checklist of panels with checkboxes
- Each row shows: checkbox, panel name, panel URL, PanelStatusBadge (derived from connectionHistory)
- "Select all connected" quick action button at top (filters to panels with status === 'connected')
- Pre-selects all connected panels on mount
- Disabled checkboxes for offline panels (grayed out)
- Search/filter input at top (reuse Input component) to filter panels by name
- Uses Card wrapper with CardHeader "Select Panels" and CardContent containing the list
- Empty state when no panels registered: "No panels registered. Add panels from the Panels page."
  </action>
  <verify>
    <automated>grep -c "PushWizard" src/app/\(dashboard\)/panels/push/page.tsx && grep -c "currentStep" src/components/push/push-wizard.tsx && grep -c "PanelSelector" src/components/push/push-wizard.tsx && grep -c "onSelectionChange" src/components/push/panel-selector.tsx && grep -c "panel:push-progress" src/components/push/push-wizard.tsx</automated>
  </verify>
  <done>
    - /panels/push page renders PushWizard with page heading per UI-SPEC copywriting
    - PushWizard manages 4-step state with step indicator bar
    - PanelSelector shows checkboxes with status badges, pre-selects connected panels
    - PanelSelector has "Select all connected" button and search filter
    - PushWizard calls POST /api/panels/diff on step 2 transition
    - PushWizard calls POST /api/panels/push on step 3 transition
    - PushWizard listens for panel:push-progress WebSocket events
    - 30s polling fallback for WebSocket
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Create diff view, progress tracker, result summary, and error recommendation components</name>
  <files>src/components/push/config-diff-view.tsx, src/components/push/push-progress-tracker.tsx, src/components/push/push-result-summary.tsx, src/components/push/error-recommendation.tsx, src/components/push/push-wizard.tsx</files>
  <read_first>
    - .planning/phases/11.4-chain-config-application-push-ux/11.4-UI-SPEC.md (component specs, interaction patterns, copywriting)
    - src/components/push/push-wizard.tsx (created in Task 1 -- needs wiring to new components)
    - src/types/config-push.ts (ConfigDiffResult, PushProgressEvent, StructuredPushError types)
  </read_first>
  <action>
**Create `src/components/push/config-diff-view.tsx`:**
- Props: `diffResults: ConfigDiffResult[]`
- Tab bar or accordion listing each panel name (from diffResults)
- For each selected panel, show two columns side-by-side:
  - Left: "Current Configuration" heading + formatted JSON (or "No current config" if null)
  - Right: "New Configuration" heading + formatted JSON
- Each section within the diff has:
  - Section label (e.g., "WireGuard Peers") with summary badge: "+{added} -{removed}"
  - Lines colored: added = `bg-green-500/10 text-green-400`, removed = `bg-red-500/10 text-red-400`, unchanged = default
- Monospace font (`font-mono text-xs`) for config content
- Scrollable container with max-height for each panel's diff
- Empty state per panel if hasChanges === false: "No changes for this panel"
- Render all config text as React text nodes inside `<pre>` or `<code>` elements (XSS-safe by default)

**Create `src/components/push/push-progress-tracker.tsx`:**
- Props: `progress: Map<number, PushProgressEvent>`, `panelIds: number[]`
- Per-panel progress row showing:
  - Panel name
  - Status indicator: pending = gray circle, pushing = blue spinner (animate-spin), success = green checkmark (lucide CheckCircle), failed = red X (lucide XCircle)
  - Latency display after success: "({latencyMs}ms)" per UI-SPEC copywriting
  - Error message after failure (if error field present)
- Shows "Pushing configuration to {panelName}..." text while status === 'pushing' per UI-SPEC
- Updates in real-time as WebSocket events arrive
- Per UI-SPEC: cannot navigate back during active push

**Create `src/components/push/push-result-summary.tsx`:**
- Props: `results: PushAllResult | null`, `onRollback: (panelId: number) => void`, `onRetry: () => void`
- Summary card at top: "{succeeded} of {totalPanels} panels updated" per UI-SPEC
- If partial failure: warning badge + "{succeeded} of {totalPanels} panels updated successfully. Review failed panels below." per UI-SPEC
- Per-panel result list:
  - Success: green dot + panel name + latency + "Configuration applied to {panelName} ({latencyMs}ms)"
  - Failed: red dot + panel name + error message
- Per failed panel: "Rollback" button (Button variant="ghost", text-red-500 per UI-SPEC) that calls `onRollback(panelId)`
  - NO confirmation dialog per CONTEXT.md D-03
- "Push Again" button at bottom (Button variant="outline") that calls onRetry
- "Back to Panels" link (navigates to /panels)
- On rollback success, show brief inline text: "Rolled back to previous configuration on {panelName}" per UI-SPEC

**Create `src/components/push/error-recommendation.tsx`:**
- Props: `error: StructuredPushError`
- Inline expandable card per UI-SPEC:
  - Error type badge (color-coded: connection_timeout = yellow, auth_failure = red, etc.)
  - Error message in bold
  - "Recommended action:" label + recommendation text (per UI-SPEC copywriting)
  - "Known fix:" label + known fix text (per UI-SPEC copywriting)
  - Known fix commands shown in `font-mono text-xs` (monospace) inside `<pre>` elements
  - Collapsible by default (except first error expanded per UI-SPEC)
  - Uses lucide-react ChevronDown/ChevronUp for toggle icon
- Render all text as React text nodes only (never use innerHTML or raw HTML insertion)

**Update `src/components/push/push-wizard.tsx`:**
- Wire in ConfigDiffView for step 2
- Wire in PushProgressTracker for step 3
- Wire in PushResultSummary for step 4
- Implement `onRollback` handler: calls POST /api/panels/rollback with panelId, updates results state
- Implement `onRetry` handler: resets push state, goes back to step 1
- All copywriting must match UI-SPEC exactly (see Copywriting Contract table)

**IMPORTANT copywriting from UI-SPEC (use exactly these strings):**
- Primary CTA: "Push Configuration"
- Empty state heading: "No configuration changes to push"
- Error state: "Push failed on {panelName}: {error.message}. {error.recommendation}"
- Push in progress: "Pushing configuration to {panelName}..."
- Push success: "Configuration applied to {panelName} ({latencyMs}ms)"
- Push partial failure: "{succeeded} of {total} panels updated successfully. Review failed panels below."
- Diff preview heading: "Configuration Preview -- {panelName}"
- Diff section current: "Current Configuration"
- Diff section new: "New Configuration"
- Rollback CTA: "Rollback"
- Rollback success: "Rolled back to previous configuration on {panelName}"
- Push error title: "Push Error -- {panelName}"
- Panel selector placeholder: "Select panels to push to..."
- Wizard step labels: "1. Select Panels" / "2. Preview Changes" / "3. Push" / "4. Results"
  </action>
  <what-built>Push Configuration page at /panels/push with 4-step wizard (Select Panels, Preview Changes, Push, Results), real-time WebSocket progress tracking, side-by-side config diff, one-click rollback, and structured error display</what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Navigate to http://localhost:3000/panels/push
3. Verify step indicator bar shows "1. Select Panels" / "2. Preview Changes" / "3. Push" / "4. Results"
4. Verify "No configuration changes to push" empty state appears when no chains available (or panel list loads)
5. Verify PanelSelector shows panel checkboxes with status badges
6. Verify "Select all connected" quick action button works
7. If panels and chains exist, navigate through the wizard steps and verify:
   - Step 2 shows side-by-side diff with green/red highlighting
   - Step 3 shows per-panel progress with spinners
   - Step 4 shows result summary with rollback buttons
8. Verify all copywriting matches UI-SPEC exactly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>
    <automated>grep -c "ConfigDiffView" src/components/push/push-wizard.tsx && grep -c "PushProgressTracker" src/components/push/push-wizard.tsx && grep -c "PushResultSummary" src/components/push/push-wizard.tsx && grep -c "ErrorRecommendation" src/components/push/push-result-summary.tsx && grep -c "Configuration Preview" src/components/push/config-diff-view.tsx && grep -c "Rollback" src/components/push/push-result-summary.tsx && grep -c "Recommended action" src/components/push/error-recommendation.tsx && grep -c "Known fix" src/components/push/error-recommendation.tsx</automated>
  </verify>
  <done>
    - ConfigDiffView shows side-by-side current vs new config with colored diff lines
    - PushProgressTracker shows real-time per-panel progress via WebSocket
    - PushResultSummary shows succeeded/failed counts with per-panel rollback buttons
    - ErrorRecommendation shows structured error with recommendation and collapsible known fix
    - PushWizard wires all components together with correct step transitions
    - All copywriting matches UI-SPEC exactly
    - Rollback is single-click without confirmation per CONTEXT.md D-03
    - 30-second polling fallback if WebSocket unavailable
    - All text rendered as React text nodes inside pre/code elements (XSS-safe)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->push UI | Admin session required (enforced by API routes, not UI) |
| UI->diff API | POST /api/panels/diff requires session cookie |
| UI->push API | POST /api/panels/push requires session cookie + panel API keys |
| UI->rollback API | POST /api/panels/rollback requires session cookie |
| WebSocket->UI | Socket.io events on 'panel:push-progress' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.4-11 | S | push page | accept | API routes enforce session auth. UI does not expose additional attack surface |
| T-11.4-12 | X | config-diff-view (rendered JSON) | mitigate | Config JSON rendered as React text nodes inside pre/code elements. No raw HTML injection |
| T-11.4-13 | X | error-recommendation (knownFix) | mitigate | Known fix text rendered as React text nodes in monospace pre element only |
| T-11.4-14 | I | WebSocket events | accept | Push progress events only contain panelId, status, latencyMs, error. No sensitive config data in progress events |
</threat_model>

<verification>
- /panels/push page renders without errors
- All 6 new components exist in src/components/push/
- PushWizard manages 4-step state correctly
- PanelSelector renders panel list with status badges
- ConfigDiffView shows side-by-side diff with color coding
- PushProgressTracker listens for WebSocket events
- PushResultSummary shows rollback buttons per failed panel
- ErrorRecommendation shows recommendation and known fix
- All UI-SPEC copywriting strings are used exactly
- All text content rendered as React text nodes (no raw HTML insertion)
- TypeScript compiles: npx tsc --noEmit 2>&1 | grep -v node_modules | head -20
</verification>

<success_criteria>
- /panels/push page accessible and renders PushWizard
- 4-step wizard flow: Select Panels -> Preview Changes -> Push -> Results
- Panel selector shows all panels with status badges, pre-selects connected
- Config diff shows side-by-side current vs new with colored diff lines
- Push progress shows real-time per-panel status via WebSocket
- Results show per-panel status with one-click rollback (no confirmation dialog)
- Error display includes recommendation and collapsible known fix
- All copywriting matches UI-SPEC contract exactly
- All text rendered XSS-safe (React text nodes only)
</success_criteria>

<output>
After completion, create `.planning/phases/11.4-chain-config-application-push-ux/11.4-04-SUMMARY.md`
</output>
