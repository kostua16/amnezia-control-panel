---
phase: 11.4-chain-config-application-push-ux
plan: 05
subsystem: api, ui
tags: [chain-config, push-wizard, template-selector, nextjs-api, react]

# Dependency graph
requires:
  - phase: 11.4-04
    provides: "PushWizard skeleton with 6 UI components (PanelSelector, ConfigDiffView, PushProgressTracker, PushResultSummary, ErrorRecommendation, ConfigApplier)"
provides:
  - "ChainConfig generation endpoint using RemotePanel.id (not Server.id) as node.serverId"
  - "Push status polling endpoint (GET /api/panels/push/status)"
  - "ChainTemplateSelector component with template grid and node-to-panel mapping dropdowns"
  - "chainConfigRef.current wiring fix in push-wizard.tsx -- the wizard can now advance past Step 1"
affects: [11.5-geo-routing, 11.6-tailscale-subnet-router]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RemotePanel-based chain config generation (serverId = RemotePanel.id for generatePerPanelConfig compatibility)"
    - "Topology-parallel WireGuard/Xray generation logic (replicated from chain-router.ts)"

key-files:
  created:
    - "src/app/api/panels/push/chain-config/route.ts"
    - "src/app/api/panels/push/status/route.ts"
    - "src/components/push/chain-template-selector.tsx"
  modified:
    - "src/components/push/push-wizard.tsx"

key-decisions:
  - "Replicated WireGuard/Xray generation logic from chain-router.ts inline rather than extracting shared util (avoids touching verified files)"

requirements-completed: [CPUSH-04, CPUSH-05, CPUSH-06, CHAIN-01, CHAIN-02, CHAIN-03]

# Metrics
duration: 5min
completed: 2026-04-30
---

# Phase 11.4 Plan 05: Push Wizard ChainConfigRef Wiring Fix Summary

**ChainConfig generation from template + panel mapping with RemotePanel.id as serverId, template selector UI, and push status polling endpoint -- fixes the push wizard stuck on Step 1**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-30T19:42:09Z
- **Completed:** 2026-04-30T19:47:04Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- POST /api/panels/push/chain-config generates valid ChainConfig with node.serverId = RemotePanel.id (matching generatePerPanelConfig expectations)
- GET /api/panels/push/status returns empty events array as polling fallback (no more 404)
- ChainTemplateSelector component provides template grid selection and node-to-panel mapping dropdowns
- chainConfigRef.current is assigned in handleNextToDiff before diff/push API calls -- wizard can now advance through all 4 steps

## Task Commits

Each task was committed atomically:

1. **Task 1: Create chain config generation API and push status polling API** - `7e705a1` (feat)
2. **Task 2: Add chain template selector to wizard, fix chainConfigRef assignment, wire push flow end-to-end** - `92d40ed` (feat)

## Files Created/Modified
- `src/app/api/panels/push/chain-config/route.ts` - POST endpoint: generates ChainConfig from template + panelMapping using RemotePanel data, validates mappings, generates WireGuard peers and Xray routing rules
- `src/app/api/panels/push/status/route.ts` - GET endpoint: returns empty events array for polling fallback
- `src/components/push/chain-template-selector.tsx` - Template grid with topology badges, node-to-panel mapping with dropdown selects, deduplication of panel assignments
- `src/components/push/push-wizard.tsx` - Fixed chainConfigRef.current assignment, added template state/fetch, integrated ChainTemplateSelector in Step 1, updated button disabled logic

## Decisions Made
- Replicated WireGuard/Xray generation logic from chain-router.ts inline in the new endpoint rather than extracting a shared utility, to avoid modifying verified files (config-applier, chain-router) per plan constraints
- Default port 3000 for panel endpoint resolution (panel API runs on standard Next.js port)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed implicit any type on chain-config route parameter**
- **Found during:** Task 1 (TypeScript compilation check)
- **Issue:** Parameter `p` in `panels.map((p) => p.id)` had implicit `any` type
- **Fix:** Added explicit type annotation `(p: { id: number })`
- **Files modified:** `src/app/api/panels/push/chain-config/route.ts`
- **Verification:** `npx tsc --noEmit` returns no errors for modified files
- **Committed in:** `7e705a1` (amended into Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Minimal -- type annotation only, no logic change.

## Issues Encountered
- None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Push wizard is fully functional end-to-end (all 4 steps reachable)
- ChainConfig generation uses RemotePanel.id correctly for generatePerPanelConfig compatibility
- Geo-routing phase (11.5) can build on the chain config foundation
- Tailscale integration (11.6) will use the established push flow

---
*Phase: 11.4-chain-config-application-push-ux*
*Completed: 2026-04-30*
