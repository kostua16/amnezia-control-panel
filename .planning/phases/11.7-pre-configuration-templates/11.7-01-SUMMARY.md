---
phase: 11.7-pre-configuration-templates
plan: 01
subsystem: database
tags: [prisma, typescript, sqlite, chain-presets, config-templates]

# Dependency graph
requires: []
provides:
  - ChainPreset Prisma model and TypeScript interfaces
  - ConfigTemplate category field for template categorization
  - Chain presets service layer with 3 built-in presets and CRUD operations
  - Updated getTemplates() with category filtering support
  - Prisma client regenerated with both ConfigTemplate.category and ChainPreset types
affects: [11.7-02, 11.7-03, 11.7-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Built-in entity pattern: in-memory definitions array + idempotent seed function + CRUD with built-in protection"

key-files:
  created:
    - src/types/chain-preset.ts
    - src/lib/chain-presets.ts
  modified:
    - prisma/schema.prisma
    - src/types/config.ts
    - src/lib/config-templates.ts

key-decisions:
  - "Added category field to ConfigTemplate per D-01: extends existing model with @default('protocol')"
  - "ChainPreset model follows RoutingRuleTemplate pattern for consistency"
  - "3 built-in chain presets reference existing chain template IDs (2hop-linear, 3hop-linear) and routing bundle IDs (russia-direct, eu-privacy, full-tunnel)"

patterns-established:
  - "Chain preset seed pattern mirrors routing rule template seed (idempotent by name+isBuiltIn check)"
  - "JSON null handling uses project convention (as never cast) for Prisma Json fields"

requirements-completed: [TMPL-04]

# Metrics
duration: 6min
completed: 2026-05-01
---

# Phase 11.7 Plan 01: Data Model Foundation Summary

**ConfigTemplate category field with protocol/server/routing/chain categorization, ChainPreset Prisma model with 3 built-in presets (Simple Relay, Privacy Chain, High Performance), and full CRUD service layer**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-01T11:26:26Z
- **Completed:** 2026-05-01T11:32:36Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments
- Added `category` field (default "protocol") to ConfigTemplate model and all TypeScript interfaces
- Created ChainPreset Prisma model with topology, nodeCount, chainTemplateId, routingBundleId, protocolOverrides
- Created ChainPreset and ChainPresetCreate TypeScript interfaces mirroring RoutingRuleTemplate pattern
- Implemented chain-presets.ts service with 3 built-in presets and idempotent seeding
- Updated getTemplates() to support optional category filtering
- Pushed schema to SQLite and regenerated Prisma client types

## Task Commits

Each task was committed atomically:

1. **Task 1: Add category field to ConfigTemplate and create ChainPreset model + types** - `e060dd7` (feat)
2. **Task 2: Create chain presets service and update config-templates.ts for category filtering** - `7b140c1` (feat)
3. **Task 3: Push database schema** - No commit (infrastructure: schema push + Prisma generate, no source changes)

## Files Created/Modified
- `prisma/schema.prisma` - Added category field to ConfigTemplate, added ChainPreset model
- `src/types/config.ts` - Added category field to ConfigTemplate, ConfigTemplateCreate, ConfigTemplateUpdate
- `src/types/chain-preset.ts` - New: ChainPreset and ChainPresetCreate TypeScript interfaces
- `src/lib/config-templates.ts` - Updated getTemplates() with category filter, mapTemplate with category field
- `src/lib/chain-presets.ts` - New: 3 built-in chain presets, seedChainPresets(), CRUD operations with built-in protection

## Decisions Made
- Used `as never` cast for Prisma JSON null handling, matching existing project convention in config-templates.ts
- ChainPreset topology field stored as String (not enum) to match ChainTopology type flexibility
- Chain presets reference existing chain template IDs and routing bundle IDs by string name

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed Prisma JSON null type incompatibility**
- **Found during:** Task 2 (chain-presets.ts TypeScript compilation)
- **Issue:** `null` is not assignable to Prisma's `InputJsonValue | NullableJsonNullValueInput | undefined` for JSON fields
- **Fix:** Used `as never` cast to match existing project convention (config-templates.ts uses `content: data.content as never`)
- **Files modified:** src/lib/chain-presets.ts
- **Committed in:** `7b140c1` (amended into Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Type fix necessary for TypeScript compilation. No scope creep.

## Issues Encountered
- Pre-existing TypeScript errors in `chain-flow-utils.ts` (CrossPanelEdgeData type compatibility) and `routing-rule-templates.ts` (TemplateRuleDef[] JSON casting) -- documented as out of scope

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- ConfigTemplate category field available for downstream plans to categorize templates
- ChainPreset model and service layer ready for API route integration (Plan 02)
- Prisma client types regenerated and available at src/generated/prisma/
- Built-in chain presets seeded on first call to seedChainPresets()

---
*Phase: 11.7-pre-configuration-templates*
*Completed: 2026-05-01*

## Self-Check: PASSED
