# Phase 4.1: User Database Models - Context

**Gathered:** 2026-04-28
**Status:** Ready for planning
**Mode:** Auto-generated (infrastructure phase — schema already designed with all models)

<domain>
## Phase Boundary

Extend Prisma schema for users with limits and protocol assignments. Ensure migration captures current schema state. User model already has trafficQuotaBytes, speedLimitKbps. UserProtocol model handles AWG/3x-ui assignments. UserQuota model handles quota management.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion — pure infrastructure phase. Schema is already designed; main task is ensuring migration is properly created and applied.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- Prisma schema already has User, UserProtocol, UserQuota, TrafficLog, RoutingRule models
- SQLite database (prisma/dev.db) already has all tables via prior `prisma db push`
- Generated Prisma client at src/generated/prisma/

### Established Patterns
- Prisma client import: `import { PrismaClient } from '@/generated/prisma'`
- Singleton pattern in src/lib/prisma.ts

### Integration Points
- Migration dir: prisma/migrations/
- Init migration exists: 20260427211333_init (only admins table)
- Current migration status: 0 applied, 0 pending (DB was created via db push)

</code_context>

<specifics>
## Specific Ideas

No specific requirements — infrastructure phase. Focus on creating a proper migration that captures the current schema state.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>
