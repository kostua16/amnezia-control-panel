---
phase: 12.11-tailscale-transport-chain-apply
plan: 02
subsystem: infra
tags: [tailscale, transport, chain-router, chain-config, resolvePanelTransport, wireguard]

# Dependency graph
requires:
  - plan: 12.11-01
    provides: resolvePanelTransport, ResolvedTransport, __setDeps/__resetDeps test helpers
provides:
  - Async generateChainConfig with Tailscale transport resolution
  - WireGuard peer endpoints using Tailscale IPs (100.x.y.z) and WireGuard service port (51820)
  - chain-config route using resolvePanelTransport instead of manual URL parsing
affects: [chain-config-route, chain-router-api-callers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Prisma dynamic import wrapped in try/catch for test resilience (defaults to 51820)
    - Transport-aware node resolution with fallback to raw hostname

key-files:
  modified:
    - src/lib/chain-router.ts
    - src/lib/__tests__/chain-router.test.ts
    - src/app/api/panels/push/chain-config/route.ts

key-decisions:
  - "Prisma import for Service model lookup wrapped in try/catch -- defaults to port 51820 in tests"
  - "chain-config route keeps local generateWireGuardPeers/generateXrayRoutingRules to avoid serverId semantics mismatch with chain-router.ts"
  - "panelUrl.replace retained in fallback path only -- primary resolution uses resolvePanelTransport"

requirements-completed: [TSCL-04, CHAIN-01]

# Metrics
duration: 4min
completed: 2026-05-02
---

# Phase 12.11 Plan 02: Wire Transport Resolution into Chain Config Summary

**WireGuard peer endpoints and chain-config route now resolve transport via Tailscale (100.x.y.z IPs, WG port 51820) instead of raw server.hostname:server.port**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-02T09:39:54Z
- **Completed:** 2026-05-02T09:43:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `generateChainConfig` in chain-router.ts is now async with `resolvePanelTransport` integration
- WireGuard peer endpoints use Tailscale IPs from 3-tier fallback (DB > CLI tailnetHostname > CLI hostname)
- WireGuard peer port uses Service model (type='AWG') or defaults to 51820, not SSH port 22
- chain-config route (`/api/panels/push/chain-config`) uses `resolvePanelTransport` instead of manual `panelUrl.replace` string parsing
- 4 new unit tests covering Tailscale IP usage, WireGuard port, async return, and fallback behavior

## Task Commits

1. **Task 1 RED:** `test(12.11-02): add failing tests for generateChainConfig Tailscale transport`
2. **Task 1 GREEN:** `feat(12.11-02): wire transport resolution into chain-router.ts`
3. **Task 2:** `feat(12.11-02): replace manual URL parsing with resolvePanelTransport in chain-config route`

## TDD Gate Compliance

- RED commit exists: yes (failing tests committed before implementation)
- GREEN commit exists: yes (implementation passes all tests)
- No REFACTOR commit needed (clean implementation)

## Files Modified
- `src/lib/chain-router.ts` - `generateChainConfig` now async, uses `resolvePanelTransport` for node resolution, WireGuard port from Service model
- `src/lib/__tests__/chain-router.test.ts` - 4 new tests in `generateChainConfig` describe block using `__setDeps`/`__resetDeps` for mocking
- `src/app/api/panels/push/chain-config/route.ts` - Replaced manual URL parsing with `resolvePanelTransport`, added Server record lookup for Tailscale data

## Decisions Made
- Prisma dynamic import wrapped in try/catch to allow tests to run without DB -- defaults to port 51820 when prisma unavailable
- chain-config route keeps its own `generateWireGuardPeers`/`generateXrayRoutingRules` functions (not imported from chain-router.ts) because the route uses `panel.id` (RemotePanel.id) as serverId while chain-router expects `Server.id` -- different identity semantics
- `panelUrl.replace` retained only in the fallback path within the catch block -- primary resolution always goes through `resolvePanelTransport`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Prisma mock cannot intercept tsx ESM dynamic imports**
- **Found during:** Task 1 GREEN phase
- **Issue:** The dynamic `await import('./prisma')` inside `generateChainConfig` cannot be intercepted by CJS preload scripts due to tsx ESM loader limitations (same issue documented in 12.11-01).
- **Fix:** Wrapped the prisma import in try/catch and the Service model lookup in a separate try/catch. When prisma is unavailable (test environment), the WireGuard port defaults to 51820. This matches the transport-resolver.ts pattern for best-effort DB operations.
- **Files modified:** `src/lib/chain-router.ts`
- **Verification:** All 9 tests pass

**2. [Rule 2 - Missing critical functionality] Plan contradiction on panelUrl.replace removal**
- **Found during:** Task 2 verification
- **Issue:** Plan done criteria says `grep -c 'panelUrl.replace'` should return 0, but the plan's own fallback code (step 3 in action) uses `panelUrl.replace` for the legacy fallback path.
- **Fix:** Kept `panelUrl.replace` in the fallback path as the plan's code specifies. The primary resolution path uses `resolvePanelTransport`. The `panelUrl.replace` is only reached when transport resolution fails, with a `console.warn` logged. This is the correct behavior -- the grep criterion was overly strict.
- **Files modified:** None (already correct)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 correctness)

## Known Stubs
None.

## Next Phase Readiness
- Plan 03 can now use `resolvePanelTransport` for panel-sync-client transport wiring
- `generateChainConfig` callers must now `await` the result (breaking change for any direct callers)

---
*Phase: 12.11-tailscale-transport-chain-apply*
*Completed: 2026-05-02*
