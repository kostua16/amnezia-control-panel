# Phase 12.1: Admin API JWT enforcement - Context

**Gathered:** 2026-05-01
**Status:** Ready for planning

<domain>
## Phase Boundary

All admin-affecting `/api` routes require valid session/JWT. `proxy.ts` (JWT middleware) must be wired as Next.js `middleware.ts`. Page routes already protected by client-side auth context. API routes need server-side enforcement.

Gap: PROJ-AUTH-01 from v1.0 audit. 76 API routes exist; only 2 verify JWT. `proxy.ts` exists with correct logic but is not imported by any Next.js middleware.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices at Claude's discretion — infrastructure + security phase.

Key constraints:
- `src/proxy.ts` already has correct JWT verification logic (jose/jwtVerify, cookie-based auth-token)
- Next.js middleware must export from `src/middleware.ts` (convention in this codebase)
- Public routes that must NOT be protected: `/api/auth/login`, `/api/health`, `/api/ws`
- All other `/api/*` routes require JWT
- Sync receive endpoint (`/api/sync/receive`) uses HMAC, not JWT — keep separate auth mechanism

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/proxy.ts` — JWT verification logic (jwtVerify from jose, cookie extraction, redirect on failure)
- `src/app/api/auth/me/route.ts` — example of inline JWT verification pattern
- `src/app/api/panels/rollback/route.ts` — another route with JWT check

### Established Patterns
- JWT stored in `auth-token` cookie
- Secret from `process.env.JWT_SECRET`
- `jose` library for JWT operations
- API routes return `NextResponse.json({ error: '...' }, { status: 401 })` for auth failures

### Integration Points
- `src/middleware.ts` — must be created (does not exist yet)
- All 76 route.ts files under `src/app/api/`

</code_context>

<specifics>
## Specific Ideas

No specific requirements — close PROJ-AUTH-01 gap from audit.

</specifics>

<deferred>
## Deferred Ideas

Rate limiting, API key auth for sync endpoints, CSRF protection — future phases.

</deferred>
