---
phase: 12.8-sync-apply-receive-contracts
verified: 2026-05-02T10:30:00Z
status: passed
score: 15/15 must-haves verified
overrides_applied: 0
gaps: []
---

# Phase 12.8: Sync Apply and Receive Contracts Verification Report

**Phase Goal:** Remote apply and receive calls use the same auth headers, payloads, and response shapes the routes implement -- no unsigned `/api/sync/apply` from the config applier and no stale assumptions in `panel-sync-client` for `/api/sync/receive`.
**Verified:** 2026-05-02T10:30:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

#### Plan 12.8-01 Truths (GAPL-01)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | config-applier sends X-API-Key and X-Signature headers on every request to /api/sync/apply | VERIFIED | `config-applier.ts` lines 66-67, 164-165: `'X-API-Key': apiKey` and `'X-Signature': signature` in both `applyAwgConfig` and `applyThreeXuiConfig` fetch calls |
| 2 | config-applier correctly unwraps nested { success, data: { applied } } responses | VERIFIED | `config-applier.ts` lines 74-76: `const resp = await response.json(); const data = resp.data; if (data?.applied)` -- present in both service functions |
| 3 | panel-sync-client correctly unwraps nested { success, data: { applied, configVersion } } responses | VERIFIED | `panel-sync-client.ts` line 113: `const data = resp.data;` followed by line 114: `if (data?.applied && typeof data.configVersion === 'number')` |
| 4 | SyncReceiveResponse type matches actual route response shape (nested under data) | VERIFIED | `panel-sync.ts` lines 58-69: `SyncReceiveResponse` has `success: boolean` and `data: SyncReceiveResponseData` |
| 5 | SyncApplyResponse type exists and documents the /api/sync/apply response shape | VERIFIED | `panel-sync.ts` lines 44-56: `SyncApplyResponse` and `SyncApplyResponseData` exported |
| 6 | All existing tests pass with updated mock response shapes | VERIFIED | panel-sync-client.test.ts: 16/16 pass; mock responses use nested `{ success: true, data: { applied: true, configVersion: N } }` shape (lines 154, 169, 221) |
| 7 | New tests verify auth headers and response unwrapping for config-applier | VERIFIED | config-applier.test.ts: 7/7 pass; tests for X-API-Key header (line 18), X-Signature HMAC (line 72), response unwrapping (lines 37, 50), 401 error (line 62) |

#### Plan 12.8-02 Truths (CHAIN-01)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 8 | chain-router uses panelUrl from credentials map instead of constructing http://hostname:port | VERIFIED | `chain-router.ts` line 161: `applyPanelConfig(creds.panelUrl, ...)`; grep for `http://${node.hostname` returns 0 matches |
| 9 | chain-router passes apiKey to applyPanelConfig for each node | VERIFIED | `chain-router.ts` line 161: `applyPanelConfig(creds.panelUrl, node.label, creds.apiKey, panelConfig)` -- 4 args including apiKey |
| 10 | chain-router skips nodes that have no matching panel credential | VERIFIED | `chain-router.ts` lines 153-157: `const creds = panelCredentials?.get(node.serverId); if (!creds) { errors.push(...); continue; }` |
| 11 | chains/apply route passes panelCredentials parameter to applyChainConfig | VERIFIED | `route.ts` lines 108, 111: `const panelCredentials = new Map<...>();` then `applyChainConfig(chainConfig, undefined, panelCredentials)` |
| 12 | New tests verify panelUrl usage and apiKey forwarding in chain-router | VERIFIED | chain-router.test.ts: 5/5 pass; test for panelUrl (line 31), apiKey via X-API-Key (line 52), missing-credential error (line 71), success path (line 92) |

#### Plan 12.8-03 Truths (Test Isolation)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 13 | chain-router.test.ts loads and runs without triggering @/lib/prisma import | VERIFIED | Tests pass (5/5) using `node --import tsx --require _setup-geo-routing-mock.cjs --test chain-router.test.ts` -- no prisma generation needed |
| 14 | All 5 chain-router tests pass | VERIFIED | Behavioral spot-check: all 5 tests pass in 217ms with geo-routing mock preloaded |
| 15 | resolveGeoRoute is mocked so geo-routing.ts is never evaluated at test runtime | VERIFIED | `_setup-geo-routing-mock.cjs` patches `Module._resolveFilename` to redirect geo-routing imports to `_geo-routing-mock.cjs`; mock returns `{ matched: false, action: 'ALLOW' }` |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/types/panel-sync.ts` | SyncApplyResponse, SyncApplyResponseData, updated SyncReceiveResponse | VERIFIED | Lines 44-69: all three interfaces present with correct nested shape |
| `src/lib/config-applier.ts` | applyAwg/applyThreeXui/applyPanel with apiKey param, HMAC signing, response unwrap | VERIFIED | 276 lines, substantive; `signPayload` import (line 3), `apiKey` param in all 3 functions, `resp.data` unwrap in both service functions |
| `src/lib/panel-sync-client.ts` | pushConfigToPanel with correct response unwrapping | VERIFIED | Line 113: `const data = resp.data;` with `data?.applied` check |
| `src/lib/__tests__/config-applier.test.ts` | Unit tests for auth and response parsing | VERIFIED | 129 lines, 7 tests across 2 describe blocks |
| `src/lib/__tests__/panel-sync-client.test.ts` | Updated tests with nested mock responses | VERIFIED | 229 lines, 16 tests; all mock Responses use nested `{ success, data }` shape |
| `src/lib/chain-router.ts` | applyChainConfig with panelCredentials parameter | VERIFIED | Line 114: `panelCredentials?: Map<number, { panelUrl: string; apiKey: string }>` |
| `src/app/api/chains/apply/route.ts` | API route builds panelCredentials and passes to applyChainConfig | VERIFIED | Lines 108, 111: empty Map constructed and passed; TODO for Phase 12.10 at line 106 |
| `src/lib/__tests__/chain-router.test.ts` | Unit tests for chain-router auth and panelUrl | VERIFIED | 125 lines, 5 tests covering panelUrl, apiKey, missing creds, success path, sourceIp |
| `src/lib/__tests__/_setup-geo-routing-mock.cjs` | Preload script for geo-routing mock | VERIFIED | 46 lines; patches `Module._resolveFilename` for chain-router parent files |
| `src/lib/__tests__/_geo-routing-mock.cjs` | Mock module for geo-routing | VERIFIED | 11 lines; exports stub functions matching geo-routing.ts API |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `config-applier.ts` | `hmac.ts` | `import { signPayload } from './hmac'` | WIRED | Line 3 imports; lines 60, 158 call `signPayload` |
| `config-applier.ts` | `/api/sync/apply` | `fetch with X-API-Key + X-Signature headers` | WIRED | Lines 62-70, 160-168: authenticated fetch calls |
| `panel-sync-client.ts` | `/api/sync/receive` | `response.json() then resp.data unwrapping` | WIRED | Lines 112-114: `const resp = await response.json(); const data = resp.data;` |
| `chains/apply/route.ts` | `chain-router.ts` | `import and call with panelCredentials` | WIRED | Line 4 imports; line 111 calls `applyChainConfig(chainConfig, undefined, panelCredentials)` |
| `chain-router.ts` | `config-applier.ts` | `applyPanelConfig(panelUrl, label, apiKey, payload)` | WIRED | Line 161: 4-argument call with apiKey |
| `chains/apply/route.ts` | `prisma.remotePanel` | `DB query for panelUrl` | WIRED | Lines 92-97: `prisma.remotePanel.findMany({ where: { isActive: true, serverId: { in: ... } } })` |
| `chain-router.test.ts` | `chain-router.ts` | Direct import (module under test) | WIRED | Line 3: `import { applyChainConfig } from '../chain-router'` |
| `chain-router.test.ts` | `geo-routing (mocked)` | CJS preload mock via `_setup-geo-routing-mock.cjs` | WIRED | `Module._resolveFilename` patch redirects geo-routing imports to `_geo-routing-mock.cjs` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| config-applier.ts | `signPayload(bodyPayload, apiKey)` | `hmac.ts` signPayload function | FLOWING | HMAC-SHA256 produces deterministic hex signature; verified in test |
| config-applier.ts | `resp.data.applied` | fetch Response from `/api/sync/apply` | FLOWING | Response unwrapped from nested `{ success, data }` shape; tests mock this path |
| panel-sync-client.ts | `resp.data.applied`, `resp.data.configVersion` | fetch Response from `/api/sync/receive` | FLOWING | Correctly unwrapped; tests mock this path with `data?.applied` optional chaining |
| chain-router.ts | `creds.panelUrl`, `creds.apiKey` | `panelCredentials` Map parameter | FLOWING | Map is populated by caller (route.ts or tests); values flow to `applyPanelConfig` call |
| chains/apply/route.ts | `panelCredentials` Map | `prisma.remotePanel.findMany` query | STATIC | Map is always empty -- TODO at line 106 notes Phase 12.10 will add apiKey input. This is intentional and documented. |

**Note:** The `chains/apply/route.ts` `panelCredentials` map is intentionally empty because the DB stores bcrypt hashes, not plaintext keys. Phase 12.10 will add API key input to the request body. The push flow via `pushConfigToAllPanels` in `panel-sync-client.ts` correctly receives keys from the caller (line 193: `panelApiKeys.get(panel.id)`).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| chain-router tests run without Prisma | `node --import tsx --require ./src/lib/__tests__/_setup-geo-routing-mock.cjs --test src/lib/__tests__/chain-router.test.ts` | 5/5 pass, 217ms | PASS |
| config-applier tests pass | `node --import tsx src/lib/__tests__/config-applier.test.ts` | 7/7 pass, 27ms | PASS |
| panel-sync-client tests pass | `node --import tsx src/lib/__tests__/panel-sync-client.test.ts` | 16/16 pass, 15104ms | PASS |
| Old http://hostname:port pattern absent | `grep "http://${node.hostname}" src/lib/chain-router.ts` | 0 matches | PASS |
| signPayload wired in config-applier | `grep -c "signPayload" src/lib/config-applier.ts` | 3 matches (1 import + 2 calls) | PASS |
| X-API-Key in config-applier | `grep -c "X-API-Key" src/lib/config-applier.ts` | 2 matches | PASS |
| resp.data unwrap in panel-sync-client | `grep -c "resp.data" src/lib/panel-sync-client.ts` | 1 match | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| GAPL-01 | 12.8-01 | Remote config apply path aligns with central push (auth, response contracts) | SATISFIED | config-applier signs with HMAC, sends X-API-Key + X-Signature; unwraps nested responses. panel-sync-client unwraps nested responses. Types updated. 7+16 tests pass. |
| CHAIN-01 | 12.8-02 | Pushed chain config is actually applied to AWG and 3x-ui services on remote servers | SATISFIED (applier slice) | chain-router uses panelUrl from credentials, passes apiKey through to applyPanelConfig which dispatches to applyAwgConfig/applyThreeXuiConfig. 5 tests pass. Transport slice deferred to Phase 12.11 per ROADMAP. |

No orphaned requirements found. Both requirement IDs declared in plans (GAPL-01, CHAIN-01) map to REQUIREMENTS.md entries that are assigned to Phase 12.8.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/api/chains/apply/route.ts` | 106 | `// TODO: Phase 12.10 will add apiKey input...` | Info | Intentional -- documents planned future work. Not a blocker. |

### Human Verification Required

None. All truths verified programmatically through code inspection and behavioral spot-checks.

### Gaps Summary

No gaps found. All 15 must-have truths verified across all 3 plans. All artifacts exist, are substantive, and correctly wired. All key links verified. All 3 test suites pass (28 total tests). The only intentional limitation is the empty `panelCredentials` map in `chains/apply/route.ts`, which is documented with a TODO pointing to Phase 12.10 -- this is by design, not a gap.

---

_Verified: 2026-05-02T10:30:00Z_
_Verifier: Claude (gsd-verifier)_
