---
plan: 12.8-01
phase: 12.8
status: complete
---

# Plan 12.8-01: Fix config-applier auth/response parsing, types, and tests

## What was built

- **HMAC auth added to config-applier**: `applyAwgConfig`, `applyThreeXuiConfig`, `applyPanelConfig` now accept `apiKey: string` parameter, sign payloads with `signPayload()`, and send `X-API-Key` + `X-Signature` headers to `/api/sync/apply`
- **Response unwrap fixed**: Both config-applier and panel-sync-client now correctly unwrap `resp.data` before checking `applied` / `configVersion`
- **Types updated**: `SyncReceiveResponse` changed from flat to nested `{ success, data }` shape; `SyncApplyResponse` and `SyncApplyResponseData` types added
- **Tests created**: `config-applier.test.ts` with 7 tests covering auth headers, response unwrapping, HMAC verification
- **Tests updated**: `panel-sync-client.test.ts` mock responses updated to nested `{ success, data }` shape

## Key files

- `src/types/panel-sync.ts` — SyncApplyResponse, SyncReceiveResponse types
- `src/lib/config-applier.ts` — HMAC signing, apiKey param, response unwrap
- `src/lib/panel-sync-client.ts` — resp.data unwrap in pushConfigToPanel
- `src/lib/__tests__/config-applier.test.ts` — new, 7 tests
- `src/lib/__tests__/panel-sync-client.test.ts` — updated mock shapes

## Deviations

None.

## Self-Check: PASSED

- All 7 config-applier tests pass
- All 16 panel-sync-client tests pass (including HMAC and generatePerPanelConfig)
- Grep verification: `signPayload` present in config-applier, `X-API-Key` headers present, `resp.data` unwrap present in panel-sync-client, `SyncApplyResponse` type exists
- No old `http://${node.hostname}` pattern remains
