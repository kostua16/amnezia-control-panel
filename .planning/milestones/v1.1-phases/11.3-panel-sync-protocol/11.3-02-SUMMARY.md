---
phase: 11.3-panel-sync-protocol
plan: 02
subsystem: api, database
tags: [prisma, zod, hmac, sync, api-route]

# Dependency graph
requires:
  - phase: 11.2
    provides: "RemotePanel model with apiKeyHash for auth lookup"
provides:
  - "CachedPanelConfig Prisma model for storing pushed configs"
  - "POST /api/sync/receive endpoint with 3-layer validation (auth, HMAC, Zod)"
  - "SyncReceiveResponse type for typed responses"
affects: [11.3-03, 11.4-apply-config]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "3-layer request validation: API key auth, HMAC signature, Zod schema"
    - "Idempotent upsert by configVersion"
    - "findUnique + update/create fallback for SQLite nullable unique constraints"

key-files:
  created:
    - "src/app/api/sync/receive/route.ts"
  modified:
    - "prisma/schema.prisma"
    - "src/types/panel-sync.ts"

key-decisions:
  - "Used findUnique + update/create pattern instead of upsert for CachedPanelConfig (SQLite nullable @@unique compatibility)"
  - "Sequential bcrypt.compare loop over active panels for API key lookup (avoids indexing on hash column)"
  - "Zod schema matches PanelSyncPayload interface exactly for type-level contract alignment"

patterns-established:
  - "Sync receive pattern: extract headers -> auth -> validate -> HMAC -> idempotency -> store -> respond"

requirements-completed: [HAUT-01, CPUSH-01]

# Metrics
duration: 3min
completed: 2026-04-30
---

# Phase 11.3 Plan 02: Sync Receiver Summary

**CachedPanelConfig Prisma model and POST /api/sync/receive endpoint with 3-layer validation (bcrypt auth, HMAC-SHA256 signature, Zod schema) and idempotent config caching**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-29T20:25:23Z
- **Completed:** 2026-04-30T00:28:21Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- CachedPanelConfig Prisma model with @@unique([panelId]) for single-row-per-panel upsert semantics
- POST /api/sync/receive endpoint with complete 3-layer validation pipeline
- Idempotent config push: same configVersion returns early without re-writing
- SyncReceiveResponse type added to shared panel-sync types

## Task Commits

Each task was committed atomically:

1. **Task 1: Add CachedPanelConfig model to Prisma schema and push to DB** - `154faf1` (feat)
2. **Task 2: Create POST /api/sync/receive endpoint with auth, HMAC, Zod validation, and config caching** - `e7cdf2f` (feat)

## Files Created/Modified
- `prisma/schema.prisma` - Added CachedPanelConfig model with id, panelId, configVersion, config (Json), receivedAt
- `src/app/api/sync/receive/route.ts` - POST endpoint: API key auth, HMAC signature verification, Zod schema validation, idempotent upsert
- `src/types/panel-sync.ts` - Added SyncReceiveResponse interface

## Decisions Made
- Used findUnique + update/create pattern instead of direct upsert for SQLite compatibility with nullable @@unique constraints (plan specified this as fallback)
- Sequential bcrypt.compare loop over active panels instead of a single query -- necessary because apiKeyHash is not indexed and bcrypt hashing is inherently sequential
- Zod schema mirrors PanelSyncPayload interface exactly -- both 11.3-01 and 11.3-02 use the same field definitions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `npx prisma` intercepted by rtk hook -- resolved by calling `node node_modules/prisma/build/index.js` directly
- `tsc --noEmit` shows pre-existing Next.js type errors in node_modules -- not from this plan's code

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- CachedPanelConfig table is ready for storing pushed configurations
- /api/sync/receive endpoint is ready for central panel push requests
- Next: 11.3-03 will wire up the push flow from central panel to this receive endpoint

## Self-Check: PASSED

- FOUND: prisma/schema.prisma
- FOUND: src/app/api/sync/receive/route.ts
- FOUND: src/types/panel-sync.ts
- FOUND: .planning/phases/11.3-panel-sync-protocol/11.3-02-SUMMARY.md
- FOUND: 154faf1 (Task 1 commit)
- FOUND: e7cdf2f (Task 2 commit)

---
*Phase: 11.3-panel-sync-protocol*
*Completed: 2026-04-30*
