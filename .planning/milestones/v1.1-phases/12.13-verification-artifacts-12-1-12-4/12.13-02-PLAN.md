---
phase: 12.13-verification-artifacts-12-1-12-4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md
autonomous: true
requirements:
  - GAPL-01
  - GAPL-02
user_setup: []
gap_closure: true
must_haves:
  truths:
    - "POST /api/sync/apply endpoint exists with HMAC auth (X-API-Key + X-Signature)"
    - "sync/apply reads from CachedPanelConfig (DB read-through, not request body only)"
    - "config-applier uses temp file approach for CLI input (not stdin)"
    - "config-applier 404 fallback returns success:false (not success:true)"
    - "WS_TO_QUERY_KEYS mapping exists in providers.tsx"
    - "WS events invalidate correct React Query keys via useEffect bridge"
    - "/api/sync/apply is in PUBLIC_API_ROUTES (HMAC auth, not JWT)"
  artifacts:
    - path: ".planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md"
      provides: "Formal evidence that GAPL-01 and GAPL-02 are satisfied"
      min_lines: 100
  key_links:
    - from: "src/app/api/sync/apply/route.ts"
      to: "src/lib/hmac.ts"
      via: "import { verifySignature }"
      pattern: "import.*verifySignature.*hmac"
    - from: "src/components/providers.tsx"
      to: "React Query"
      via: "queryClient.invalidateQueries"
      pattern: "invalidateQueries"
    - from: "src/lib/config-applier.ts"
      to: "/api/sync/apply"
      via: "fetch with X-API-Key + X-Signature headers"
      pattern: "X-API-Key.*X-Signature"
---

<objective>
Create 12.4-VERIFICATION.md with structured evidence that Phase 12.4 (Sync Apply & WS Invalidation) satisfies GAPL-01 and GAPL-02.

Purpose: Close the GAPL-01 and GAPL-02 evidence gaps from the milestone audit. Phase 12.4 was executed and its SUMMARY lists requirements-completed: [GAPL-01, GAPL-02], but no VERIFICATION.md was created. The 12.8-VERIFICATION.md already provides additional evidence for GAPL-01 (auth contracts) and 12.9-VERIFICATION.md provides evidence for GAPL-02 (WS key alignment). This document focuses on the original 12.4 scope: endpoint existence, HMAC auth, config-applier temp file approach, 404 fix, and initial WS-to-React-Query bridge.

Output: .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v12.x-MILESTONE-AUDIT.md
@.planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-PLAN.md
@.planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-01-SUMMARY.md
@.planning/phases/12.8-sync-apply-receive-contracts/12.8-VERIFICATION.md
@.planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 12.4-VERIFICATION.md with GAPL-01 and GAPL-02 evidence</name>
  <files>.planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md</files>
  <read_first>
    - src/app/api/sync/apply/route.ts (endpoint being verified)
    - src/lib/config-applier.ts (404 fix + temp file approach + HMAC signing)
    - src/components/providers.tsx (WS_TO_QUERY_KEYS bridge)
    - src/middleware.ts (PUBLIC_API_ROUTES -- must include /api/sync/apply)
    - .planning/phases/12.8-sync-apply-receive-contracts/12.8-VERIFICATION.md (format reference + GAPL-01 supplementary evidence)
    - .planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md (format reference)
    - src/lib/hmac.ts (verifySignature function -- used by sync/apply)
  </read_first>
  <action>
Create `.planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md` following the EXACT format of 12.8-VERIFICATION.md and 12.11-VERIFICATION.md. The file must have:

**YAML frontmatter:**
```yaml
---
phase: 12.4-sync-apply-and-ws-invalidation
verified: 2026-05-03T12:00:00Z
status: passed
score: N/N must-haves verified
overrides_applied: 0
gaps: []
---
```

**Sections (in this order):**

1. **Header**: Phase goal ("Central push/apply path does not rely on missing remote endpoints; dashboard/resource queries invalidate on relevant WebSocket events"), verified date, status, re-verification flag

2. **Goal Achievement > Observable Truths**: A numbered table with columns: #, Truth, Status, Evidence. Each truth must have a grepable file:line reference. The truths to verify (read actual source files and derive evidence):

   **GAPL-01 truths (Sync Apply):**

   | # | Truth | Evidence source |
   |---|-------|-----------------|
   | 1 | `POST /api/sync/apply` endpoint exists at `src/app/api/sync/apply/route.ts` | File exists, exports POST handler |
   | 2 | Endpoint validates X-API-Key header (bcrypt comparison against stored hash) | sync/apply/route.ts: `request.headers.get('X-API-Key')` + bcrypt.compare |
   | 3 | Endpoint validates X-Signature header via HMAC | sync/apply/route.ts: `verifySignature(body, apiKey, signature)` |
   | 4 | Missing auth headers return 401 `{ success: false, error: 'Missing X-API-Key or X-Signature header' }` | sync/apply/route.ts lines ~159-163 |
   | 5 | Endpoint reads cached config from `CachedPanelConfig` via Prisma | sync/apply/route.ts: `prisma.cachedPanelConfig.findUnique` |
   | 6 | For `awg` service: config applied via temp file + CLI (`wg-quick` or `amneziawg`) | sync/apply/route.ts: `applyWireguardConfig()` writes to tmp file, passes path to CLI |
   | 7 | For `three_xui` service: rules applied via temp file + CLI (`xui apply-rules`) | sync/apply/route.ts: `applyThreeXuiRules()` writes JSON to tmp file |
   | 8 | `/api/sync/apply` is in `PUBLIC_API_ROUTES` in middleware.ts | middleware.ts: `/api/sync/apply` in array |
   | 9 | Temp files are cleaned up in `finally` blocks | sync/apply/route.ts: `fs.unlink(tmpFile)` in finally blocks |
   | 10 | Endpoint returns `{ success: true, data: { applied: true, configVersion, service } }` on success | sync/apply/route.ts: lines ~274-283 |
   | 11 | config-applier sends X-API-Key and X-Signature on every request | config-applier.ts: `'X-API-Key': apiKey` and `'X-Signature': signature` |
   | 12 | config-applier 404 fallback returns `success: false` (not `success: true`) | config-applier.ts: lines ~96-111 show `success: false` with `service_error` type |
   | 13 | Response shape uses nested `{ success, data: { applied, configVersion } }` | sync/apply/route.ts success response structure |

   **GAPL-02 truths (WS-to-React-Query):**

   | # | Truth | Evidence source |
   |---|-------|-----------------|
   | 14 | `WS_TO_QUERY_KEYS` mapping exists in providers.tsx | providers.tsx: `const WS_TO_QUERY_KEYS: Partial<Record<WsEventType, string[][]>>` |
   | 15 | `stats:update` maps to `[['dashboard-stats']]` | providers.tsx: WS_TO_QUERY_KEYS entry |
   | 16 | `resource:update` maps to `[['system-resources']]` | providers.tsx: WS_TO_QUERY_KEYS entry |
   | 17 | `user:status-change` maps to `[['users']]` | providers.tsx: WS_TO_QUERY_KEYS entry |
   | 18 | `panel:fallback-change` maps to `[['fleet-status']]` | providers.tsx: WS_TO_QUERY_KEYS entry |
   | 19 | useEffect bridge watches `lastEvent` and calls `queryClient.invalidateQueries` | providers.tsx: useEffect with Object.entries(WS_TO_QUERY_KEYS) |
   | 20 | Polling fallback preserved (staleTime: 10_000, refetchInterval: 30_000) | providers.tsx: QueryClient defaultOptions |

3. **Required Artifacts**: Table

   | Artifact | Expected | Status |
   |----------|----------|--------|
   | `src/app/api/sync/apply/route.ts` | POST endpoint with HMAC auth, CachedPanelConfig read-through, temp file CLI, 200+ lines | VERIFIED |
   | `src/lib/config-applier.ts` | HMAC signing, X-API-Key/X-Signature headers, 404 -> success:false, 250+ lines | VERIFIED |
   | `src/components/providers.tsx` | WS_TO_QUERY_KEYS mapping, useEffect bridge, QueryClient with polling fallback, 80+ lines | VERIFIED |
   | `src/middleware.ts` | `/api/sync/apply` in PUBLIC_API_ROUTES | VERIFIED |
   | `src/lib/hmac.ts` | `verifySignature` function used by sync/apply | VERIFIED |

4. **Key Link Verification**: Table

   | From | To | Via | Status |
   |------|----|-----|--------|
   | `sync/apply/route.ts` | `hmac.ts` | `import { verifySignature }` | WIRED |
   | `sync/apply/route.ts` | `prisma.cachedPanelConfig` | `prisma.cachedPanelConfig.findUnique` | WIRED |
   | `sync/apply/route.ts` | `prisma.remotePanel` | `prisma.remotePanel.findMany` (bcrypt auth) | WIRED |
   | `config-applier.ts` | `hmac.ts` | `import { signPayload }` | WIRED |
   | `config-applier.ts` | `/api/sync/apply` | `fetch` with X-API-Key + X-Signature headers | WIRED |
   | `providers.tsx` | `@tanstack/react-query` | `queryClient.invalidateQueries` | WIRED |
   | `providers.tsx` | `use-websocket.ts` | `import { useWebSocket }` | WIRED |
   | `middleware.ts` | PUBLIC_API_ROUTES | `/api/sync/apply` in exclusion list | WIRED |

5. **Behavioral Spot-Checks**: Table with commands and results

   | Behavior | Command | Expected Result |
   |----------|---------|-----------------|
   | sync/apply exports POST | `grep "export async function POST" src/app/api/sync/apply/route.ts` | 1 match |
   | verifySignature imported | `grep "verifySignature" src/app/api/sync/apply/route.ts` | 1+ matches |
   | HMAC auth headers checked | `grep "X-API-Key\|X-Signature" src/app/api/sync/apply/route.ts` | 2+ matches |
   | CachedPanelConfig read | `grep "cachedPanelConfig" src/app/api/sync/apply/route.ts` | 2+ matches |
   | Temp file cleanup in finally | `grep -c "finally" src/app/api/sync/apply/route.ts` | 2+ matches |
   | success:false on 404 | `grep "success: false" src/lib/config-applier.ts | grep -c "404"` (contextual) | Lines 96-111 show success:false |
   | WS_TO_QUERY_KEYS exists | `grep "WS_TO_QUERY_KEYS" src/components/providers.tsx` | 1+ matches |
   | invalidateQueries in useEffect | `grep "invalidateQueries" src/components/providers.tsx` | 1 match |
   | sync/apply in PUBLIC_API_ROUTES | `grep "sync/apply" src/middleware.ts` | 1 match |
   | dashboard-stats in query keys | `grep "dashboard-stats" src/components/providers.tsx` | 1 match |
   | staleTime preserved | `grep "staleTime" src/components/providers.tsx` | 1 match |

6. **Requirements Coverage**: Table

   | Requirement | Source Plan | Description | Status | Evidence |
   |-------------|-------------|-------------|--------|----------|
   | GAPL-01 | 12.4-01 | Remote config apply path aligned with central push | SATISFIED | `/api/sync/apply` exists with HMAC auth, reads CachedPanelConfig, applies via temp-file CLI; config-applier sends correct auth headers; 404 returns success:false. Supplementary: 12.8-VERIFICATION.md confirms auth contracts. |
   | GAPL-02 | 12.4-01 | WebSocket events invalidate React Query for dashboard/resources | SATISFIED | `WS_TO_QUERY_KEYS` maps 4 event types to query keys; useEffect bridge in providers.tsx; polling fallback preserved. Supplementary: 12.9-VERIFICATION.md confirms key alignment with hooks. |

7. **Anti-Patterns Found**: None introduced by Phase 12.4. Note that `alert:new` is handled by `use-alerts.ts` directly (not in WS_TO_QUERY_KEYS) -- this is intentional per 12.9-01 decision.

8. **Gaps Summary**: No gaps. All truths verified. GAPL-01 and GAPL-02 satisfied.

**CRITICAL:** Read the actual source files and use REAL line numbers in the Evidence column. Run the behavioral spot-check commands and report actual results.

**CRITICAL:** Do NOT modify any source code. This is a read-only verification task.
  </action>
  <verify>
    <automated>
      File exists: `test -f .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md`
      File has frontmatter: `head -1 .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md | grep "phase:"`
      File has >100 lines: `wc -l .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md`
      GAPL-01 mentioned: `grep "GAPL-01" .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md`
      GAPL-02 mentioned: `grep "GAPL-02" .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md`
      Status is passed: `grep "status: passed" .planning/phases/12.4-sync-apply-and-ws-invalidation/12.4-VERIFICATION.md`
    </automated>
  </verify>
  <done>
    12.4-VERIFICATION.md exists with YAML frontmatter, observable truths table (13 GAPL-01 + 7 GAPL-02 truths) with file:line evidence, artifacts table, key link verification, behavioral spot-checks with actual command results, requirements coverage showing GAPL-01 and GAPL-02 SATISFIED, and gaps summary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates a read-only verification document. No code changes, no new trust boundaries. |

## STRIDE Threat Register

No threats apply -- documentation-only plan with no code modifications.
</threat_model>

<verification>
1. 12.4-VERIFICATION.md exists in `.planning/phases/12.4-sync-apply-and-ws-invalidation/`
2. YAML frontmatter has phase, verified, status: passed, score
3. All observable truths reference real file:line evidence (not guessed)
4. GAPL-01 and GAPL-02 marked as SATISFIED with evidence
5. Cross-references to 12.8-VERIFICATION.md (GAPL-01 supplementary) and 12.9-VERIFICATION.md (GAPL-02 supplementary) included
6. No source code files modified
</verification>

<success_criteria>
- 12.4-VERIFICATION.md follows exact format of 12.8-VERIFICATION.md / 12.11-VERIFICATION.md
- GAPL-01 has formal evidence of satisfaction (endpoint exists, HMAC auth, temp file, 404 fix)
- GAPL-02 has formal evidence of satisfaction (WS_TO_QUERY_KEYS bridge, correct key mappings, polling fallback)
- All truths verified with actual grep/read evidence
- File is >100 lines and substantive
</success_criteria>

<output>
After completion, create `.planning/phases/12.13-verification-artifacts-12-1-12-4/12.13-02-SUMMARY.md`
</output>
