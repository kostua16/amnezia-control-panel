---
phase: 12.4-sync-apply-and-ws-invalidation
plan: 01
subsystem: api, realtime
tags: [hmac, sync, websocket, react-query, invalidation, wireguard, three-xui]

# Dependency graph
requires:
  - phase: 11.3-panel-sync-protocol
    provides: CachedPanelConfig model, HMAC verification, /api/sync/receive
  - phase: 11.4-chain-config-application-push-ux
    provides: config-applier.ts, StructuredPushError type
  - phase: 12.2-realtime-socketio-alignment
    provides: Socket.IO server, broadcastEvent, WS event types
provides:
  - POST /api/sync/apply endpoint for applying cached config to AWG/3x-ui
  - WebSocket-to-React-Query invalidation bridge in Providers
  - Fixed config-applier 404 fallback to report real failure
affects: [12.5-multi-panel-push-ux-integration, 12.7-tailscale-milestone-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - WS event -> query key prefix mapping for automatic cache invalidation
    - HMAC-authenticated sync apply with cached config read-through
    - Temp-file-based CLI config application (no stdin injection risk)

key-files:
  created:
    - src/app/api/sync/apply/route.ts
  modified:
    - src/middleware.ts
    - src/components/providers.tsx
    - src/lib/config-applier.ts

key-decisions:
  - "Temp file for CLI config input instead of stdin (avoids execFile TS overload issue and shell injection risk)"
  - "WS_TO_QUERY_KEYS mapping table for declarative event-to-query invalidation"
  - "404 changed from success:true to success:false with service_error type for actionable categorization"

patterns-established:
  - "WS invalidation bridge: centralized mapping in Providers useEffect, consumer hooks still work independently"
  - "Sync apply reads from CachedPanelConfig (not request body directly) for consistency with receive flow"

requirements-completed: [GAPL-01, GAPL-02]

# Metrics
duration: 5min
completed: 2026-05-01
---

# Phase 12.4 Plan 01: Sync apply endpoint + WS query invalidation Summary

**POST /api/sync/apply endpoint with HMAC auth reading CachedPanelConfig for AWG/3x-ui CLI application, plus WebSocket-to-React-Query invalidation bridge in Providers**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-01T16:21:19Z
- **Completed:** 2026-05-01T16:25:58Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Created POST /api/sync/apply endpoint that verifies HMAC, reads CachedPanelConfig, and applies AWG/3x-ui config via CLI
- Wired WebSocket events to React Query invalidation with a declarative WS_TO_QUERY_KEYS mapping
- Fixed config-applier.ts silent 404 success to correctly report failure with actionable error details

## Task Commits

Each task was committed atomically:

1. **Task 1: Create /api/sync/apply endpoint** - `21fc142` (feat)
2. **Task 2: Wire WebSocket events to React Query invalidation** - `b10f894` (feat)
3. **Task 3: Fix config-applier 404 fallback** - `71f6a75` (fix)

## Files Created/Modified
- `src/app/api/sync/apply/route.ts` - New endpoint: HMAC auth, CachedPanelConfig read-through, AWG/3x-ui CLI application via temp files
- `src/middleware.ts` - Added /api/sync/apply to PUBLIC_API_ROUTES (HMAC auth, not JWT)
- `src/components/providers.tsx` - Added useEffect with WS_TO_QUERY_KEYS mapping for automatic query invalidation on WS events
- `src/lib/config-applier.ts` - Changed 404 fallback from success:true to success:false with service_error type

## Decisions Made
- Used temp files instead of stdin for CLI config input to avoid TypeScript execFile overload issues and eliminate shell injection risk
- Centralized WS-to-query invalidation in Providers.tsx rather than scattering it across individual hooks
- Kept polling fallback (staleTime/refetchInterval) as a safety net alongside WS invalidation

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed execFile TypeScript overload error**
- **Found during:** Task 1 (Create /api/sync/apply endpoint)
- **Issue:** `promisify(execFile)` with `input` option does not match any TypeScript overload for `ExecFileOptionsWithStringEncoding`
- **Fix:** Replaced stdin input with temp file approach: write config/rules to a temp file, pass file path as CLI argument, clean up in finally block
- **Files modified:** src/app/api/sync/apply/route.ts
- **Verification:** `npx tsc --noEmit` shows no errors in sync/apply/route.ts
- **Committed in:** `21fc142` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Fix necessary for TypeScript compilation. Improved security posture by eliminating stdin-based input. No scope creep.

## Issues Encountered
- Pre-existing TypeScript errors in chain-flow-editor.tsx and chain-presets/route.ts (unrelated to this plan, deferred)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- /api/sync/apply endpoint is ready for config-applier integration
- WS invalidation bridge is active for all 7 event types
- Phase 12.5 (Multi-panel push UX integration) can now rely on working sync/apply endpoint

---
*Phase: 12.4-sync-apply-and-ws-invalidation*
*Completed: 2026-05-01*

## Self-Check: PASSED

All files exist, all commits verified, no accidental deletions, no untracked generated files.
