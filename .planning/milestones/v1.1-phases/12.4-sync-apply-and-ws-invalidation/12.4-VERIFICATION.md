---
phase: 12.4-sync-apply-and-ws-invalidation
verified: 2026-05-03T08:27:00Z
status: passed
score: 20/20 must-haves verified
overrides_applied: 0
gaps: []
---

# Phase 12.4: Sync Apply and WS Cache Invalidation Verification Report

**Phase Goal:** Central push/apply path does not rely on missing remote endpoints; dashboard/resource queries invalidate on relevant WebSocket events.
**Verified:** 2026-05-03T08:27:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

#### GAPL-01 Truths (Sync Apply Endpoint)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `POST /api/sync/apply` endpoint exists at `src/app/api/sync/apply/route.ts` | VERIFIED | `src/app/api/sync/apply/route.ts:153` -- `export async function POST(request: NextRequest)` |
| 2 | Endpoint validates X-API-Key header (bcrypt comparison against stored hash) | VERIFIED | `src/app/api/sync/apply/route.ts:156` -- `request.headers.get('X-API-Key')`; line 175 -- `bcrypt.compare(apiKey, panel.apiKeyHash)` |
| 3 | Endpoint validates X-Signature header via HMAC | VERIFIED | `src/app/api/sync/apply/route.ts:205` -- `if (!verifySignature(body, apiKey, signature))`; line 4 -- `import { verifySignature } from '@/lib/hmac'` |
| 4 | Missing auth headers return 401 `{ success: false, error: 'Missing X-API-Key or X-Signature header' }` | VERIFIED | `src/app/api/sync/apply/route.ts:159-163` -- returns `{ success: false, error: 'Missing X-API-Key or X-Signature header' }` with status 401 |
| 5 | Endpoint reads cached config from `CachedPanelConfig` via Prisma | VERIFIED | `src/app/api/sync/apply/route.ts:213-215` -- `prisma.cachedPanelConfig.findUnique({ where: { panelId: matchedPanel.id } })` |
| 6 | For `awg` service: config applied via temp file + CLI (`wg-quick` or `amneziawg`) | VERIFIED | `src/app/api/sync/apply/route.ts:59-97` -- `applyWireguardConfig()` writes to `tmpFile` (line 68-69), passes path to CLI via `runCommand(wgBinary, ['apply-conf', tmpFile])` (line 76) |
| 7 | For `three_xui` service: rules applied via temp file + CLI (`xui apply-rules`) | VERIFIED | `src/app/api/sync/apply/route.ts:106-148` -- `applyThreeXuiRules()` writes JSON to `tmpFile` (line 125-126), passes path to CLI via `runCommand('xui', ['apply-rules', tmpFile])` (line 128) |
| 8 | `/api/sync/apply` is in `PUBLIC_API_ROUTES` in middleware.ts | VERIFIED | `src/middleware.ts:17` -- `'/api/sync/apply'` entry in `PUBLIC_API_ROUTES` array |
| 9 | Temp files are cleaned up in `finally` blocks | VERIFIED | `src/app/api/sync/apply/route.ts:92-96` and `144-147` -- two `finally` blocks, each with `fs.unlink(tmpFile)` cleanup |
| 10 | Endpoint returns `{ success: true, data: { applied: true, configVersion, service } }` on success | VERIFIED | `src/app/api/sync/apply/route.ts:274-283` -- `return NextResponse.json({ success: true, data: { applied: true, configVersion: cachedConfig.configVersion, service, message } })` |
| 11 | config-applier sends X-API-Key and X-Signature on every request | VERIFIED | `src/lib/config-applier.ts:66-67` -- `'X-API-Key': apiKey` and `'X-Signature': signature` in `applyAwgConfig`; lines 164-165 in `applyThreeXuiConfig` |
| 12 | config-applier 404 fallback returns `success: false` (not `success: true`) | VERIFIED | `src/lib/config-applier.ts:96-113` -- `success: false` with `error: { type: 'service_error', ... }` for AWG; lines 193-211 for 3x-ui |
| 13 | Response shape uses nested `{ success, data: { applied, configVersion } }` | VERIFIED | `src/app/api/sync/apply/route.ts:275-283` -- success response structure: `{ success: true, data: { applied: true, configVersion, service, message } }` |

#### GAPL-02 Truths (WS-to-React-Query Bridge)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 14 | `WS_TO_QUERY_KEYS` mapping exists in providers.tsx | VERIFIED | `src/components/providers.tsx:35` -- `const WS_TO_QUERY_KEYS: Partial<Record<WsEventType, string[][]>>` |
| 15 | `stats:update` maps to `[['dashboard-stats']]` | VERIFIED | `src/components/providers.tsx:36` -- `'stats:update': [['dashboard-stats']]` |
| 16 | `resource:update` maps to `[['system-resources']]` | VERIFIED | `src/components/providers.tsx:37` -- `'resource:update': [['system-resources']]` |
| 17 | `user:status-change` maps to `[['users']]` | VERIFIED | `src/components/providers.tsx:38` -- `'user:status-change': [['users']]` |
| 18 | `panel:fallback-change` maps to `[['fleet-status']]` | VERIFIED | `src/components/providers.tsx:39` -- `'panel:fallback-change': [['fleet-status']]` |
| 19 | useEffect bridge watches `lastEvent` and calls `queryClient.invalidateQueries` | VERIFIED | `src/components/providers.tsx:68-76` -- `useEffect` iterates `Object.entries(WS_TO_QUERY_KEYS)`, checks `lastEvent[eventType]`, calls `queryClient.invalidateQueries({ queryKey })` |
| 20 | Polling fallback preserved (staleTime: 10_000, refetchInterval: 30_000) | VERIFIED | `src/components/providers.tsx:55-56` -- `staleTime: 10_000` and `refetchInterval: 30_000` in QueryClient defaultOptions |

**Score:** 20/20 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/app/api/sync/apply/route.ts` | POST endpoint with HMAC auth, CachedPanelConfig read-through, temp file CLI, 200+ lines | VERIFIED | 303 lines; exports POST handler with verifySignature, bcrypt compare, CachedPanelConfig findUnique, applyWireguardConfig, applyThreeXuiRules |
| `src/lib/config-applier.ts` | HMAC signing, X-API-Key/X-Signature headers, 404 -> success:false, 250+ lines | VERIFIED | 276 lines; `signPayload` import (line 3), `'X-API-Key': apiKey` (lines 66, 164), `'X-Signature': signature` (lines 67, 165), `success: false` on 404 (lines 96-113, 193-211) |
| `src/components/providers.tsx` | WS_TO_QUERY_KEYS mapping, useEffect bridge, QueryClient with polling fallback, 80+ lines | VERIFIED | 86 lines; `WS_TO_QUERY_KEYS` (line 35), `useEffect` bridge (lines 68-76), `staleTime: 10_000` + `refetchInterval: 30_000` (lines 55-56) |
| `src/middleware.ts` | `/api/sync/apply` in PUBLIC_API_ROUTES | VERIFIED | Line 17: `'/api/sync/apply'` in `PUBLIC_API_ROUTES` array |
| `src/lib/hmac.ts` | `verifySignature` function used by sync/apply | VERIFIED | 27 lines; `verifySignature(payload, secret, signature)` (line 16) using `timingSafeEqual` |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `sync/apply/route.ts` | `hmac.ts` | `import { verifySignature } from '@/lib/hmac'` | WIRED |
| `sync/apply/route.ts` | `prisma.cachedPanelConfig` | `prisma.cachedPanelConfig.findUnique` | WIRED |
| `sync/apply/route.ts` | `prisma.remotePanel` | `prisma.remotePanel.findMany` (bcrypt auth) | WIRED |
| `config-applier.ts` | `hmac.ts` | `import { signPayload } from './hmac'` | WIRED |
| `config-applier.ts` | `/api/sync/apply` | `fetch` with X-API-Key + X-Signature headers | WIRED |
| `providers.tsx` | `@tanstack/react-query` | `queryClient.invalidateQueries` | WIRED |
| `providers.tsx` | `use-websocket.ts` | `import { useWebSocket } from '@/hooks/use-websocket'` | WIRED |
| `middleware.ts` | PUBLIC_API_ROUTES | `/api/sync/apply` in exclusion list | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Expected | Result |
|----------|---------|----------|--------|
| sync/apply exports POST | `grep -n "export async function POST" src/app/api/sync/apply/route.ts` | 1 match | PASS -- 1 match at line 153 |
| verifySignature imported | `grep -n "verifySignature" src/app/api/sync/apply/route.ts` | 1+ matches | PASS -- 2 matches (import line 4, usage line 205) |
| HMAC auth headers checked | `grep -n "X-API-Key\|X-Signature" src/app/api/sync/apply/route.ts` | 2+ matches | PASS -- 2 matches (lines 156-157) |
| CachedPanelConfig read | `grep -n "cachedPanelConfig" src/app/api/sync/apply/route.ts` | 1+ matches | PASS -- 1 match at line 213 |
| Temp file cleanup in finally | `grep -c "finally" src/app/api/sync/apply/route.ts` | 2+ matches | PASS -- 2 matches (lines 92, 144) |
| success:false on 404 | `grep -n "success: false" src/lib/config-applier.ts` | 3+ matches | PASS -- 3+ matches (lines 102, 119, 128 -- error return paths) |
| WS_TO_QUERY_KEYS exists | `grep -n "WS_TO_QUERY_KEYS" src/components/providers.tsx` | 1+ matches | PASS -- 2 matches (declaration line 35, usage in Object.entries line 69) |
| invalidateQueries in useEffect | `grep -n "invalidateQueries" src/components/providers.tsx` | 1 match | PASS -- 1 match at line 72 |
| sync/apply in PUBLIC_API_ROUTES | `grep -n "sync/apply" src/middleware.ts` | 1+ matches | PASS -- 2 matches (comment line 10, array entry line 17) |
| dashboard-stats in query keys | `grep -n "dashboard-stats" src/components/providers.tsx` | 1 match | PASS -- 1 match at line 36 |
| staleTime preserved | `grep -n "staleTime" src/components/providers.tsx` | 1 match | PASS -- 1 match at line 55 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| GAPL-01 | 12.4-01 | Remote config apply path aligned with central push | SATISFIED | `/api/sync/apply` exists with HMAC auth (bcrypt + verifySignature), reads CachedPanelConfig via Prisma, applies AWG config via temp file + wg-quick/amneziawg CLI, applies 3x-ui rules via temp file + xui apply-rules CLI. config-applier sends correct X-API-Key + X-Signature headers on every request. 404 returns `success: false` with actionable `service_error`. Supplementary: 12.8-VERIFICATION.md confirms auth contracts (request/response shape alignment). |
| GAPL-02 | 12.4-01 | WebSocket events invalidate React Query for dashboard/resources | SATISFIED | `WS_TO_QUERY_KEYS` maps 4 event types (`stats:update` -> `[['dashboard-stats']]`, `resource:update` -> `[['system-resources']]`, `user:status-change` -> `[['users']]`, `panel:fallback-change` -> `[['fleet-status']]`). `useEffect` bridge in providers.tsx watches `lastEvent` and calls `queryClient.invalidateQueries`. Polling fallback preserved (`staleTime: 10_000`, `refetchInterval: 30_000`). Supplementary: 12.9-VERIFICATION.md confirms key alignment with hooks (`use-dashboard-stats`, `use-system-resources`, `use-multi-panel-status`). |

No orphaned requirements found. Both requirement IDs declared in the plan (GAPL-01, GAPL-02) map to REQUIREMENTS.md entries assigned to Phase 12.4.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/providers.tsx` | 25 | `alert:new` in WS_EVENTS but not in WS_TO_QUERY_KEYS | Info | Intentional -- `alert:new` is handled by `use-alerts.ts` directly (single source of truth per 12.9-01 decision). |

No blockers. No warnings introduced by Phase 12.4.

### Human Verification Required

None -- all truths verified programmatically through code inspection, import tracing, and behavioral spot-checks.

### Gaps Summary

No gaps found. All 20 must-have truths verified. GAPL-01 (13 truths) covers the sync apply endpoint, HMAC auth, CachedPanelConfig read-through, temp file CLI application, and config-applier 404 fix. GAPL-02 (7 truths) covers the WS-to-React-Query bridge with correct key mappings and polling fallback. All 5 required artifacts exist and are substantive. All 8 key links are wired. All 11 behavioral spot-checks pass.

---

_Verified: 2026-05-03T08:27:00Z_
_Verifier: Claude (gsd-verifier)_
