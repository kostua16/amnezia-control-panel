---
phase: 11.7-pre-configuration-templates
plan: 02
subsystem: api
tags: [nextjs, zod, prisma, chain-presets, config-presets, api-routes]

# Dependency graph
requires:
  - phase: 11.7-01
    provides: "ChainPreset Prisma model, chain-presets.ts service layer, chain-preset.ts types"
provides:
  - "4 API route files: chain-presets CRUD, seed, apply"
  - "4 server presets in config-presets.ts (Hetzner, DigitalOcean, Vultr, Contabo)"
affects: [11.7-03]

# Tech tracking
tech-stack:
  added: []
  patterns: ["chain-preset API routes follow existing config-templates pattern", "apply endpoint resolves routing bundle by name lookup"]

key-files:
  created:
    - src/app/api/chain-presets/route.ts
    - src/app/api/chain-presets/[id]/route.ts
    - src/app/api/chain-presets/seed/route.ts
    - src/app/api/chain-presets/apply/route.ts
  modified:
    - src/lib/config-presets.ts

key-decisions:
  - "Apply endpoint looks up routing bundle by name (findFirst by name) rather than requiring a numeric ID"
  - "Server presets use serviceType AWG since they are OS/network-level WireGuard settings"

patterns-established:
  - "Chain preset API follows same Zod validation, error response, and log prefix patterns as config-templates"
  - "Server presets extend existing ConfigPreset in-memory array with provider-specific MTU/sysctl settings"

requirements-completed: [TMPL-02, TMPL-04]

# Metrics
duration: 2min
completed: 2026-05-01
---

# Phase 11.7 Plan 02: Chain Preset API Routes + Server Presets Summary

**Chain preset CRUD/seed/apply API routes with Zod validation, plus 4 VPS provider server presets (Hetzner, DigitalOcean, Vultr, Contabo) in config-presets.ts**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-01T11:35:05Z
- **Completed:** 2026-05-01T11:36:27Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- 4 API route files created following existing patterns (GET/POST list+create, GET/DELETE by ID, seed, apply)
- Apply endpoint resolves chain preset's routing bundle by name and delegates to existing applyTemplateRules
- 4 server presets added to config-presets.ts with provider-specific MTU and sysctl forwarding settings

## Task Commits

Each task was committed atomically:

1. **Task 1: Create chain preset API routes (CRUD + seed + apply)** - `06bb9d9` (feat)
2. **Task 2: Extend config-presets.ts with 4 server presets** - `cf2d415` (feat)

## Files Created/Modified
- `src/app/api/chain-presets/route.ts` - GET list all presets, POST create with Zod validation
- `src/app/api/chain-presets/[id]/route.ts` - GET by ID, DELETE with built-in protection (403)
- `src/app/api/chain-presets/seed/route.ts` - POST idempotent seed of built-in chain presets
- `src/app/api/chain-presets/apply/route.ts` - POST apply routing bundle rules from a chain preset
- `src/lib/config-presets.ts` - Added 4 server presets (Hetzner MTU 1400, DigitalOcean MTU 1380, Vultr MTU 1380, Contabo MTU 1350)

## Decisions Made
- Apply endpoint looks up routing bundle template by name (findFirst where name matches routingBundleId) since chain presets store string references, not numeric template IDs
- Server presets use serviceType 'AWG' and protocol 'wireguard' as the default since they represent OS/network-level settings

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- API surface complete for gallery UI (Plan 03) to consume
- Server presets automatically exposed via existing getter functions (getPresets, getPreset, getPresetsByServiceType)

---
*Phase: 11.7-pre-configuration-templates*
*Completed: 2026-05-01*
