---
phase: 12.12-twelve-x-verification-artifacts
plan: 03
type: execute
wave: 2
depends_on: ["12.12-01", "12.12-02"]
files_modified:
  - .planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TSCL-04, PROJ-AUTH-01, GEO-03, GEO-04, TSCL-01, TSCL-02, TSCL-03]
user_setup: []
must_haves:
  truths:
    - "12.11-VERIFICATION.md exists with TSCL-04 and CHAIN-01 (transport slice) evidence citing actual source files"
    - "REQUIREMENTS.md PROJ-AUTH-01 checkbox updated to [x] with justification comment"
    - "REQUIREMENTS.md GEO-03 checkbox updated to [x] (verified via 12.6 VERIFICATION)"
    - "REQUIREMENTS.md GEO-04 checkbox updated to [x] (narrowed scope, verified via 12.6 VERIFICATION)"
    - "REQUIREMENTS.md TSCL-01 checkbox already [x] (verified via 12.7 VERIFICATION)"
    - "REQUIREMENTS.md TSCL-02 checkbox already [x] (verified via 12.7 VERIFICATION)"
    - "REQUIREMENTS.md TSCL-03 checkbox already [x] (verified via 12.7 VERIFICATION)"
    - "REQUIREMENTS.md TSCL-04 checkbox updated to [x] (verified via 12.11 VERIFICATION)"
    - "REQUIREMENTS.md traceability table updated for all 6 requirement IDs"
  artifacts:
    - path: ".planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md"
      provides: "TSCL-04 and CHAIN-01 transport verification evidence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled checkboxes reflecting verified integration behavior"
  key_links:
    - from: "12.11-VERIFICATION.md"
      to: "REQUIREMENTS.md TSCL-04"
      via: "checkbox reconciliation"
      pattern: "\\[x\\] \\*\\*TSCL-04\\*\\*"
---

<objective>
Create VERIFICATION.md for Phase 12.11 (Tailscale Transport Chain Apply) documenting TSCL-04 and CHAIN-01 transport evidence, and reconcile REQUIREMENTS.md checkboxes for PROJ-AUTH-01, GEO-03, GEO-04, TSCL-01, TSCL-02, TSCL-03, TSCL-04.

Purpose: Close the final verification artifact gap (12.11 has no VERIFICATION.md despite 3 completed plans) and reconcile REQUIREMENTS.md body checkboxes with verified integration behavior as identified in the closure audit.
Output: One VERIFICATION.md file and updated REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v12.x-audit-closure-MILESTONE-AUDIT.md

## VERIFICATION.md format

Follow `.planning/phases/12.8-sync-apply-receive-contracts/12.8-VERIFICATION.md`:
- YAML frontmatter: phase, verified date, status, score, overrides_applied, gaps
- Phase Goal section
- Goal Achievement with Observable Truths table (#, Truth, Status, Evidence)
- Each truth: VERIFIED status with file:line evidence
- Required Artifacts table
- Behavioral Spot-Checks with grep commands
- Requirements Coverage table

## Phase 12.11 context

Read all three SUMMARY files for the complete record:

**12.11-01-SUMMARY.md:** resolvePanelTransport utility with 3-tier fallback (DB cache > CLI tailnetHostname > CLI hostname), ResolvedTransport interface, 6 unit tests, DB cache-back best-effort.

**12.11-02-SUMMARY.md:** WireGuard peer endpoints use Tailscale IPs (100.x.y.z) from resolvePanelTransport, WireGuard port from Service model (not SSH port 22), chain-config route uses resolvePanelTransport.

**12.11-03-SUMMARY.md:** chains/apply route builds panelCredentials with Tailscale transport resolution, hostname correlation matching for RemotePanel-to-chain-node, panel-sync-client pushConfigToAllPanels uses resolvePanelTransport with fallback.

Key evidence sources to grep:
- `grep -n "resolvePanelTransport" src/lib/transport-resolver.ts src/lib/chain-router.ts src/app/api/chains/apply/route.ts src/lib/panel-sync-client.ts src/app/api/panels/push/chain-config/route.ts`
- `grep -n "ResolvedTransport" src/lib/transport-resolver.ts`
- `grep -n "100\." src/lib/chain-router.ts` (Tailscale IP range in WireGuard peers)

## REQUIREMENTS.md reconciliation

Read `.planning/REQUIREMENTS.md` and check each requirement checkbox against the verification evidence created in Plans 01, 02, and this plan.

**Current state (from REQUIREMENTS.md):**
- PROJ-AUTH-01: `[ ]` with note "Phase 12.12 adds strict 12.1-VERIFICATION.md"
- GEO-03: `[ ]` -- needs update, verified by 12.6
- GEO-04: `[ ]` -- needs update, verified by 12.6 (narrowed scope)
- TSCL-01: `[x]` -- already checked by 12.7
- TSCL-02: `[x]` -- already checked by 12.7
- TSCL-03: `[x]` -- already checked by 12.7
- TSCL-04: `[ ]` with note about Phase 12.11

**Reconciliation rules:**
- Change `[ ]` to `[x]` ONLY if the VERIFICATION.md from Plans 01/02/this plan confirms SATISFIED status
- Add inline justification comment after the checkbox: `<!-- verified: 12.X-VERIFICATION.md -->`
- If a requirement remains unsatisfied, leave `[ ]` and add a comment explaining why
- The traceability table at the bottom of REQUIREMENTS.md must also be updated: change "Pending" to "Done" for all 6 requirements, adding "(12.12)" suffix to indicate when verification was completed

**IMPORTANT: Do NOT modify checkboxes for requirements outside this phase's scope** (PROJ-RT-01, GAPL-01, GAPL-02, PROJ-TRACE-01, etc. -- those are handled by their respective phases).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 12.11-VERIFICATION.md for TSCL-04 and CHAIN-01 (transport slice)</name>
  <files>.planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12.11-tailscale-transport-chain-apply/12.11-01-SUMMARY.md (Plan 01 context)
    - .planning/phases/12.11-tailscale-transport-chain-apply/12.11-02-SUMMARY.md (Plan 02 context)
    - .planning/phases/12.11-tailscale-transport-chain-apply/12.11-03-SUMMARY.md (Plan 03 context)
    - src/lib/transport-resolver.ts (evidence -- resolvePanelTransport, ResolvedTransport)
    - src/lib/chain-router.ts (evidence -- Tailscale IP in WireGuard peers)
    - src/app/api/chains/apply/route.ts (evidence -- panelCredentials with transport)
    - src/lib/panel-sync-client.ts (evidence -- transport resolution in push)
    - src/app/api/panels/push/chain-config/route.ts (evidence -- chain-config transport)
    - .planning/phases/12.8-sync-apply-receive-contracts/12.8-VERIFICATION.md (format reference)
  </read_first>
  <action>
Create `.planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md` following the 12.8-VERIFICATION.md format.

**YAML frontmatter:**
```yaml
---
phase: 12.11-tailscale-transport-chain-apply
verified: [current ISO timestamp]
status: passed
score: [N]/[N] must-haves verified
overrides_applied: 0
gaps: []
---
```

**Phase Goal:** "Chain apply and routing paths that target remote panels use Tailscale/panel URL transport resolution (resolvePanelTransport / tailnet APIs), not ad hoc hostname with SSH default port."

**Observable Truths for Plan 12.11-01 (transport resolver):**

1. `src/lib/transport-resolver.ts` exists and exports `resolvePanelTransport` function
2. `ResolvedTransport` interface has fields: panelUrl, tailscaleIP, hostname, port, online, source
3. 3-tier fallback: Tier 1 (DB cache via Prisma), Tier 2 (CLI tailnetHostname), Tier 3 (CLI hostname)
4. Unit tests pass: `src/lib/__tests__/transport-resolver.test.ts` (6 tests)
5. Resolver returns null on all-tiers-fail (non-throwing design)

**Observable Truths for Plan 12.11-02 (chain-router + chain-config transport):**

6. `generateChainConfig` in chain-router.ts is async and calls `resolvePanelTransport`
7. WireGuard peer endpoints use Tailscale IPs (100.x.y.z range) from transport resolution
8. WireGuard peer port uses Service model lookup (type='AWG') or defaults to 51820 (not SSH port 22)
9. chain-config route (`/api/panels/push/chain-config`) uses `resolvePanelTransport` instead of manual URL parsing
10. Unit tests for chain-router pass (including new transport tests)

**Observable Truths for Plan 12.11-03 (chains/apply + panel-sync-client transport):**

11. `chains/apply` route builds `panelCredentials` Map with Tailscale-resolved URLs
12. `panel-sync-client.ts` `pushConfigToAllPanels` resolves transport before each push
13. Transport resolution in push falls back to registered panelUrl on failure (best-effort)
14. All tests pass: chain-router (9), panel-sync-client (18), transport-resolver (6)

For each truth, grep the actual source file to get accurate line numbers.

**Required Artifacts table:**
- `src/lib/transport-resolver.ts` -- resolvePanelTransport, ResolvedTransport
- `src/lib/chain-router.ts` -- async generateChainConfig with Tailscale IPs
- `src/app/api/chains/apply/route.ts` -- panelCredentials with transport resolution
- `src/lib/panel-sync-client.ts` -- transport resolution in push
- `src/app/api/panels/push/chain-config/route.ts` -- chain-config transport
- `src/lib/__tests__/transport-resolver.test.ts` -- 6 tests
- `src/lib/__tests__/chain-router.test.ts` -- 9 tests
- `src/lib/__tests__/panel-sync-client.test.ts` -- 18 tests

**Behavioral Spot-Checks:**
- `grep -c "resolvePanelTransport" src/lib/transport-resolver.ts` (expect >= 1: export)
- `grep -c "resolvePanelTransport" src/lib/chain-router.ts` (expect >= 1: import or call)
- `grep -c "resolvePanelTransport" src/lib/panel-sync-client.ts` (expect >= 1: import or call)
- `grep -c "resolvePanelTransport" src/app/api/chains/apply/route.ts` (expect >= 1: import or call)
- `grep -c "ResolvedTransport" src/lib/transport-resolver.ts` (expect >= 1: interface export)

**Requirements Coverage:**

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| TSCL-04 | Panel uses Tailscale IPs as transport addresses for inter-panel communication | SATISFIED | resolvePanelTransport provides 3-tier Tailscale fallback. Wired into chain-router (WireGuard peers), chains/apply (panelCredentials), panel-sync-client (push), chain-config route. 33 tests pass across 3 suites. |
| CHAIN-01 | Pushed chain config applied to AWG and 3x-ui services (transport slice) | SATISFIED (transport) | chains/apply route resolves Tailscale URLs before dispatching to config-applier. Transport resolution falls back to registered panelUrl. WireGuard port from Service model (51820), not SSH port 22. Auth slice verified in 12.8-VERIFICATION.md. |

**Note:** CHAIN-01 has two slices: auth (verified in 12.8) and transport (verified here). Both SATISFIED.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.11-tailscale-transport-chain-apply/12.11-VERIFICATION.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>12.11-VERIFICATION.md exists with YAML frontmatter (phase, verified, status: passed, score), at least 10 observable truths with VERIFIED status and file:line evidence, requirements coverage mapping TSCL-04 to SATISFIED and CHAIN-01 (transport) to SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md checkboxes for all 6 requirement IDs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of all checkboxes)
    - .planning/phases/12.12-twelve-x-verification-artifacts/12.12-01-SUMMARY.md (PROJ-AUTH-01 evidence)
    - .planning/phases/12.12-twelve-x-verification-artifacts/12.12-02-SUMMARY.md (GEO-03, GEO-04, TSCL-01, TSCL-02, TSCL-03 evidence)
    - .planning/phases/12.12-twelve-x-verification-artifacts/12.12-03-SUMMARY.md (TSCL-04 evidence -- will be created by Task 1 in this plan)
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` to reconcile checkboxes with verified behavior. Perform these specific changes:

**1. PROJ-AUTH-01 (line ~173):**
Change from:
```
- [ ] **PROJ-AUTH-01**: Admin-affecting `/api` routes enforce session/JWT; implementation matches documented session layer; Phase **12.12** adds strict `12.1-VERIFICATION.md` / evidence (see `.planning/v12.x-audit-closure-MILESTONE-AUDIT.md`)
```
To:
```
- [x] **PROJ-AUTH-01**: Admin-affecting `/api` routes enforce session/JWT; implementation matches documented session layer <!-- verified: 12.1-VERIFICATION.md -->
```

**2. GEO-03 (line ~134):**
Change from:
```
- [ ] **GEO-03**: Traffic is routed to specific chain hops based on destination geo (via GeoIP lookup)
```
To:
```
- [x] **GEO-03**: Traffic is routed to specific chain hops based on destination geo (via GeoIP lookup) <!-- verified: 12.6-VERIFICATION.md -->
```

**3. GEO-04 (line ~135):**
Change from:
```
- [ ] **GEO-04**: System can auto-load routing rule files from v2fly/geoip repository (sendmiche/rulite support is a future enhancement)
```
To:
```
- [x] **GEO-04**: System can auto-load routing rule files from v2fly/geoip repository (sendmiche/rulite support is a future enhancement) <!-- verified: 12.6-VERIFICATION.md (narrowed scope) -->
```

**4. TSCL-04 (line ~148):**
Change from:
```
- [ ] **TSCL-04**: Panel uses Tailscale IPs as transport addresses for inter-panel communication (Phase **12.11** wires transport into push/apply per closure audit)
```
To:
```
- [x] **TSCL-04**: Panel uses Tailscale IPs as transport addresses for inter-panel communication <!-- verified: 12.11-VERIFICATION.md -->
```

**5. TSCL-01, TSCL-02, TSCL-03 (lines ~145-147):**
These are already `[x]` from Phase 12.7. Add verification comments:
```
- [x] **TSCL-01**: Each server runs Tailscale as a subnet router advertising its VPN subnet <!-- verified: 11.1-VERIFICATION.md, 12.7-VERIFICATION.md -->
- [x] **TSCL-02**: Central panel can list all Tailscale nodes in the tailnet <!-- verified: 11.1-VERIFICATION.md, 12.7-VERIFICATION.md -->
- [x] **TSCL-03**: Tailscale subnet router setup is documented as step-by-step guide <!-- verified: 11.1-VERIFICATION.md, 12.7-VERIFICATION.md (API-level wizard) -->
```

**6. Traceability table (bottom of REQUIREMENTS.md):**
Update the Status column for these requirements:
- TSCL-01: change "Pending" to "Done (12.12)"
- TSCL-02: change "Pending" to "Done (12.12)"
- TSCL-03: change "Pending" to "Done (12.12)"
- TSCL-04: change "Pending" to "Done (12.12)"
- GEO-03: change "Pending" to "Done (12.12)"
- GEO-04: change "Pending" to "Done (12.12)"
- PROJ-AUTH-01: change "Pending" to "Done (12.12)"

**IMPORTANT:** Do NOT change checkboxes for requirements outside this phase's scope (PROJ-RT-01, GAPL-01, GAPL-02, PROJ-TRACE-01, CPUSH-*, etc.). Only modify the 7 checkboxes listed above.
  </action>
  <verify>
    <automated>grep -cE '\[x\].*\*\*PROJ-AUTH-01\*\*' .planning/REQUIREMENTS.md && grep -cE '\[x\].*\*\*GEO-03\*\*' .planning/REQUIREMENTS.md && grep -cE '\[x\].*\*\*GEO-04\*\*' .planning/REQUIREMENTS.md && grep -cE '\[x\].*\*\*TSCL-04\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md updated: PROJ-AUTH-01, GEO-03, GEO-04, TSCL-04 changed from [ ] to [x] with verification comments. TSCL-01/02/03 retain [x] with added verification comments. Traceability table Status updated for all 7 requirements. No other checkboxes modified.</done>
</task>

</tasks>

<verification>
- 12.11-VERIFICATION.md exists with YAML frontmatter (phase, verified, status: passed, score)
- REQUIREMENTS.md has [x] for all 7 requirements: PROJ-AUTH-01, GEO-03, GEO-04, TSCL-01, TSCL-02, TSCL-03, TSCL-04
- Each updated checkbox has `<!-- verified: XX-VERIFICATION.md -->` comment
- Traceability table Status column updated for all 7 requirements
- No unrelated checkboxes were modified
- 12.11-VERIFICATION.md has at least 10 observable truths with VERIFIED status
- grep -c "verified: 12" returns >= 7 in REQUIREMENTS.md
</verification>

<success_criteria>
- 12.11-VERIFICATION.md created with TSCL-04 and CHAIN-01 transport evidence (status: passed)
- REQUIREMENTS.md reconciled: all 7 requirement checkboxes updated to [x] with verification comments
- Traceability table updated for all 7 requirements
- No scope creep into other requirements
</success_criteria>

<output>
After completion, create `.planning/phases/12.12-twelve-x-verification-artifacts/12.12-03-SUMMARY.md`
</output>
