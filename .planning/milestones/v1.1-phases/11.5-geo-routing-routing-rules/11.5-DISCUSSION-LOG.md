# Phase 11.5: Geo-Routing & Routing Rules - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-01
**Phase:** 11.5-Geo-Routing & Routing Rules
**Areas discussed:** GeoIP & ops, Rule evaluation & transport, External imports, Admin UX, Migration

---

## GeoIP source & operations

| Option | Description | Selected |
|--------|-------------|----------|
| MaxMind MMDB direct | Offline DB with MaxMind account | |
| HTTP API | Network lookup | |
| Hybrid | MMDB + HTTP fallback | |
| (User phrased) | On-disk geoip with periodic refresh like Xray; investigate v2fly/geoip + rulite | ✓ (framed decision) |

**User's choice:** On-disk GeoIP with periodic refresh aligned with Xray ecosystem; investigate v2fly/geoip and sendmiche/rulite.

| Option | Description | Selected |
|--------|-------------|----------|
| MaxMind direct | Panel uses MaxMind license | |
| v2fly bundle | Prebuilt artifacts / mirrors as Xray users use | ✓ |
| Both | Admin-selectable | |
| You decide | | |

**User's choice:** v2fly/geoip-style artifacts.

| Option | Description | Selected |
|--------|-------------|----------|
| Built-in schedule + manual | | ✓ |
| Manual only | | |
| External cron only | | |

**User's choice:** Built-in schedule + manual update.

| Option | Description | Selected |
|--------|-------------|----------|
| Fail open / fall through | | ✓ |
| Fail to admin default | | |
| Fail closed | | |

**User's choice:** Fail open (fall through rules / default).

**Notes:** User advanced to next area after four questions.

---

## Rule evaluation & transport

| Option | Description | Selected |
|--------|-------------|----------|
| Push resolved Xray config | Central expands geo; panels apply | ✓ |
| Central proxy-only | | |
| Panel-local DB + runtime lookup | | |

**User's choice:** Push resolved Xray routing config.

| Option | Description | Selected |
|--------|-------------|----------|
| IPv4 + IPv6 | | |
| IPv4 first | | |
| IPv4 only | | ✓ |

**User's choice:** IPv4 only for this phase.

| Option | Description | Selected |
|--------|-------------|----------|
| Strict numeric priority | | ✓ |
| Priority + type tie-break | | |
| Document order only | | |

**User's choice:** Strict admin priority ordering.

| Option | Description | Selected |
|--------|-------------|----------|
| SQLite only | | |
| SQLite + short-lived cache | | ✓ |

**User's choice:** SQLite canonical with performance cache.

**Notes:** User advanced to next area.

---

## External imports

| Option | Description | Selected |
|--------|-------------|----------|
| Merge all | | |
| Replace imported bundle tier only | | ✓ |
| Replace all | | |

**User's choice:** Two-tier model; replace imported bundle without implied full DB wipe.

| Option | Description | Selected |
|--------|-------------|----------|
| Skip duplicates | | |
| Overwrite after confirm | | ✓ |
| Keep both | | |

**User's choice:** Overwrite on conflict (with confirmation).

| Option | Description | Selected |
|--------|-------------|----------|
| Manual map only | | |
| Auto name match + manual remainder | | ✓ |
| Single chain deterministic | | |

**User's choice:** Auto-match tags to node labels; unmatched requires manual mapping.

| Option | Description | Selected |
|--------|-------------|----------|
| Geo-only import | | |
| Geo + routing snippets | | |
| Best-effort parse | | ✓ |

**User's choice:** Best-effort import with visible errors for unsupported constructs.

**Notes:** User advanced to next area.

---

## Admin UX

| Option | Description | Selected |
|--------|-------------|----------|
| Unified table | | |
| Tabs | | ✓ |
| Geo subpage only | | |

**User's choice:** Tabbed layout.

| Option | Description | Selected |
|--------|-------------|----------|
| Template on “new rule” only | | |
| Gallery only | | |
| Both picker + gallery | | ✓ |

**User's choice:** Both flows.

| Option | Description | Selected |
|--------|-------------|----------|
| Guided empty state | | |
| Minimal CTA | | |
| One-click starter rules (non-destructive demo) | | ✓ |

**User's choice:** Starter rules demo CTA.

| Option | Description | Selected |
|--------|-------------|----------|
| Drawer | | ✓ |
| Modal | | |
| Dedicated page | | |

**User's choice:** Drawer / slide-over editor.

**Notes:** User advanced to next area.

---

## Migration

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-migrate first boot | | ✓ |
| No auto-migrate | | |
| Leave to planner | | |

**User's choice:** Auto-migrate on first boot after upgrade.

**Notes:** User skipped granular questions on coexistence with existing `RoutingRule` rows; CONTEXT.md flags planner reconciliation.

---

## Claude's Discretion

- Tie-break when two rules share the same priority number
- Cache TTL, file path layout, exact duplicate-key definition for imports
- Component-level UI details for drawer and tabs

## Deferred Ideas

- IPv6 geo matching (post–11.5 if needed)
- HTTP-first GeoIP provider path
