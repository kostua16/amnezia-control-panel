---
phase: 11.4-chain-config-application-push-ux
plan: 02
subsystem: api, infra
tags: [rollback, config-versioning, sqlite, api]

dependency-graph:
  requires:
    - phase: 11.4-chain-config-application-push-ux
      plan: 01
      provides: CachedPanelConfig model, StructuredPushError type, pushConfigToPanel
    - phase: 11.3-panel-sync-protocol
      provides: PanelSyncPayload type, sync receive endpoint, panel-sync-client
  provides:
    - Rollback manager with storePreviousConfig and rollbackPanelConfig/WithPush
    - POST /api/panels/rollback endpoint with JWT auth
    - Previous config preservation in sync/receive flow
  affects: [11.4-push-ux, 11.4-diff-preview]

tech-stack:
  added: []
  patterns:
    - "One-level rollback: previous config swapped to current, old current discarded"
    - "Auto-preserve: every sync/receive call stores previous config before overwrite"
    - "JWT session auth on rollback endpoint (T-11.4-06)"

key-files:
  created:
    - src/lib/rollback-manager.ts
    - src/app/api/panels/rollback/route.ts
  modified:
    - prisma/schema.prisma
    - src/app/api/sync/receive/route.ts

key-decisions:
  - "Split rollback into two functions: rollbackPanelConfig (DB-only swap) and rollbackPanelConfigWithPush (swap + re-push) for testability"
  - "API key passed explicitly in rollback request body since plaintext key is not stored in DB"
  - "Used Prisma.InputJsonValue cast and Prisma.JsonNull for nullable Json field compatibility with SQLite"

requirements-completed: [CPUSH-05]

metrics:
  duration: 3min
  completed: 2026-04-30
---

# Phase 11.4 Plan 02: Rollback Manager & API Summary

**Per-panel config rollback with one-level-deep previous config storage, automatic preservation on every push, and JWT-authenticated rollback endpoint that re-pushes restored config to remote**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-30T06:11:31Z
- **Completed:** 2026-04-30T06:14:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Extended CachedPanelConfig with previousConfigVersion, previousConfig, previousConfigReceivedAt nullable fields
- Created rollback-manager.ts with storePreviousConfig (called before overwrite) and rollbackPanelConfig/WithPush (swap + optional re-push)
- Created POST /api/panels/rollback endpoint with JWT session auth, Zod validation, and structured error responses
- Modified sync/receive endpoint to automatically preserve previous config before every new push

## Task Commits

1. **Task 1: Extend CachedPanelConfig schema** - `70a32f1` (feat)
2. **Task 2: Create rollback manager and rollback API endpoint** - `b92d35d` (feat)

## Files Created/Modified
- `prisma/schema.prisma` - Added previousConfigVersion (Int?), previousConfig (Json?), previousConfigReceivedAt (DateTime?)
- `src/lib/rollback-manager.ts` - storePreviousConfig, rollbackPanelConfig, rollbackPanelConfigWithPush
- `src/app/api/panels/rollback/route.ts` - POST /api/panels/rollback with JWT auth
- `src/app/api/sync/receive/route.ts` - Added storePreviousConfig call before upsert

## Decisions Made
- Split rollback into rollbackPanelConfig (DB-only) and rollbackPanelConfigWithPush (DB + remote push) for testability and separation of concerns
- API key must be passed in the rollback request body since the DB only stores the hash -- the endpoint cannot look up the plaintext key
- Used Prisma.JsonNull sentinel for clearing nullable Json fields (SQLite compatibility)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed Prisma JsonValue type errors**
- **Found during:** Task 2 (rollback-manager.ts)
- **Issue:** `null` is not assignable to `NullableJsonNullValueInput | InputJsonValue | undefined` for Prisma Json fields. Also `@/generated/prisma` module not found.
- **Fix:** Cast Json values with `as Prisma.InputJsonValue`, use `Prisma.JsonNull` sentinel for null, fix import path to `@/generated/prisma/client`
- **Files modified:** src/lib/rollback-manager.ts
- **Verification:** `npx tsc --noEmit` passes with 0 errors
- **Committed in:** `b92d35d` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 type error)
**Impact on plan:** Fix was necessary for correctness. No scope creep.

## Self-Check: PASSED

All files exist, both commits verified, SUMMARY created.

---
*Phase: 11.4-chain-config-application-push-ux*
*Completed: 2026-04-30*
