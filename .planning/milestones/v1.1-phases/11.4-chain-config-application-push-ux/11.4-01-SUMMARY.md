---
phase: 11.4-chain-config-application-push-ux
plan: 01
subsystem: api, infra
tags: [config-application, websocket, error-reporting, wireguard, xray, tailwind]

dependency-graph:
  requires:
    - phase: 11.3-panel-sync-protocol
      provides: PanelSyncPayload type, pushConfigToPanel/All, sync receive endpoint, CachedPanelConfig model
  provides:
    - ConfigApplierResult, StructuredPushError, PushProgressEvent types
    - enrichError with 6-category pattern matching
    - applyAwgConfig, applyThreeXuiConfig, applyPanelConfig functions
    - Real applyChainConfig replacing console.log stub
    - panel:push-progress WebSocket event type and broadcasts
  affects: [11.4-push-ux, 11.4-diff-preview, 11.4-rollback]

tech-stack:
  added: []
  patterns:
    - "Error enrichment: raw errors mapped to 6 categories with actionable recommendations"
    - "Graceful 404 fallback: remote apply endpoint not available returns success with warning"
    - "Shell injection guard: DANGEROUS_CHARS regex on WG peer values before CLI/API use"

key-files:
  created:
    - src/types/config-push.ts
    - src/lib/error-reporter.ts
    - src/lib/config-applier.ts
  modified:
    - src/lib/chain-router.ts
    - src/lib/panel-sync-client.ts
    - src/lib/websocket.ts

key-decisions:
  - "applyPanelConfig accepts PanelSyncPayload types (not chain types) to match the sync payload shape"
  - "Remote apply endpoint uses /api/sync/apply with service discriminator; 404 = remote not ready, degrade gracefully"
  - "Shell injection validation on WG peer values (publicKey, allowedIPs, endpoint) before any processing"

requirements-completed: [CHAIN-01, CHAIN-02, CHAIN-03, CPUSH-06]

metrics:
  duration: 3min
  completed: 2026-04-30
---

# Phase 11.4 Plan 01: Config Application Backend Summary

**Real config applier with shell injection guards, structured error enrichment for 6 failure categories, and WebSocket push progress broadcasting replacing console.log stubs**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-30T06:07:00Z
- **Completed:** 2026-04-30T06:09:48Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created StructuredPushError type with 6 error categories and PushProgressEvent for real-time tracking
- Implemented applyAwgConfig, applyThreeXuiConfig, applyPanelConfig with shell metacharacter injection guard
- Replaced stub applyChainConfig in chain-router.ts with real applyPanelConfig calls
- Added panel:push-progress WebSocket broadcasts to panel-sync-client.ts (pushing/success/failed events)
- Error enrichment via pattern matching: timeout, auth, invalid config, docker, service, unknown

## Task Commits

Each task was committed atomically:

1. **Task 1: Create config push types and error reporter utility** - `b1e4060` (feat)
2. **Task 2: Implement real config applier and wire into chain-router** - `d8112bb` (feat)

## Files Created/Modified
- `src/types/config-push.ts` - ConfigApplierResult, StructuredPushError, PushProgressEvent types
- `src/lib/error-reporter.ts` - enrichError function with 6-category pattern matching
- `src/lib/config-applier.ts` - applyAwgConfig, applyThreeXuiConfig, applyPanelConfig with injection guards and 404 fallback
- `src/lib/chain-router.ts` - Replaced stub applyChainConfig with real applyPanelConfig integration
- `src/lib/panel-sync-client.ts` - Added push-progress WebSocket broadcasts on push start, success, failure
- `src/lib/websocket.ts` - Added 'panel:push-progress' to WsEventType union

## Decisions Made
- Changed applier function signatures to accept `PanelSyncPayload['wireguardPeers']` and `PanelSyncPayload['routingRules']` instead of chain types, since the applier receives payloads from the sync flow which have simpler shapes (no `nodeId`)
- Graceful 404 handling: when remote panel lacks `/api/sync/apply`, returns `success: true` with a warning error instead of failing, allowing end-to-end flow even if remote is not yet updated

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed type mismatch in applyPanelConfig**
- **Found during:** Task 2 (config-applier.ts)
- **Issue:** `applyAwgConfig` and `applyThreeXuiConfig` expected `WireGuardPeerConfig[]` and `XrayRoutingRule[]` (which have `nodeId` field), but `PanelSyncPayload` provides simpler types without `nodeId`
- **Fix:** Changed function signatures to accept `PanelSyncPayload['wireguardPeers']` and `PanelSyncPayload['routingRules']` directly, removed unused imports from `@/types/chain`
- **Files modified:** src/lib/config-applier.ts
- **Verification:** `npx tsc --noEmit` passes with 0 errors
- **Committed in:** `d8112bb` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 type mismatch)
**Impact on plan:** Fix was necessary for correctness. No scope creep.

## Issues Encountered
- TypeScript compilation caught the type mismatch between chain types and sync payload types in config-applier.ts. Fixed by aligning applier signatures to the actual payload shape.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Config application backend complete, ready for push UX (plan 11.4-02)
- Remote `/api/sync/apply` endpoint not yet implemented on remote panel side -- the 404 fallback handles this gracefully
- Threat mitigations in place: shell injection guard (T-11.4-01), JSON-serialized rules (T-11.4-02), 15s timeout (T-11.4-04)

## Self-Check: PASSED

All files exist, both commits verified, SUMMARY created.

---
*Phase: 11.4-chain-config-application-push-ux*
*Completed: 2026-04-30*
