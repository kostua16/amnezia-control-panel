# Phase 1.7: Login Page UI - Plan

**Created:** 2026-04-28
**Status:** Ready for execution

## Tasks (single wave, sequential)

### Task 1: Create Auth Layout
**File:** `src/app/(auth)/layout.tsx` (NEW)

Server component. Dark background, centered content.
```tsx
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      {children}
    </div>
  );
}
```

---

### Task 2: Create Login Page
**File:** `src/app/(auth)/login/page.tsx` (NEW if not exists, else REPLACE)

Client component ("use client"). Uses Card, Input, Button from Phase 1.5.

Structure:
- Card container (CardHeader with title "Amnezia Control Panel" + subtitle, CardContent with form)
- Form: two Input fields (username, password) + Button submit
- useState for loading, error states
- Form onSubmit prevents default, sets loading, simulates delay then resets (UI-only)
- Responsive: Card max-w-md w-full mx-4

Acceptance: page renders dark card centered, form fields work, button shows loading state.

---

### Task 3: Create Placeholder Auth Pages
**File:** `src/app/(auth)/login/page.tsx` already handled.

Check if other auth routes need placeholders. Likely none — login is the only auth page in Phase 1.

---

## File Changes

| Action | File |
|--------|------|
| CREATE | `src/app/(auth)/layout.tsx` |
| CREATE/MODIFY | `src/app/(auth)/login/page.tsx` |

## Verification

- `npx tsc --noEmit` passes
- `npm run lint` passes
- `/login` renders dark centered card with form
- Button shows loading state on click
