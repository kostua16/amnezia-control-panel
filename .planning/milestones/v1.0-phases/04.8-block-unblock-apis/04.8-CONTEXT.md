# Phase 4.8: Block/Unblock APIs - Context

**Gathered:** 2026-04-29
**Status:** Ready for planning

<domain>
Build APIs to block and unblock users in both systems. POST /api/users/[id]/block and POST /api/users/[id]/unblock endpoints. Wire to UI buttons.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices at Claude's discretion — infrastructure phase. Add block/unblock handlers, wire to UI buttons.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- src/lib/vpn-services.ts — block/unblock functions for AWG and 3x-ui
- user-list.tsx — Block/Unblock button placeholder (console.log)
- Prisma User.isBlocked field

### Integration Points
- src/app/api/users/[id]/block/route.ts and /unblock/route.ts — new files
- user-list.tsx — wire Block/Unblock buttons

</code_context>

<specifics>
No specific requirements.

</specifics>

<deferred>
None.
