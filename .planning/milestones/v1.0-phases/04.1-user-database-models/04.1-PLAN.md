---
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/<timestamp>_user_models/migration.sql
autonomous: true
requirements_addressed: []
---

# Plan 04.1-01: Baseline User Models Migration

## Objective
Create a proper Prisma migration that captures the current schema state (User, UserProtocol, UserQuota, TrafficLog, etc.) since the database was created via `prisma db push` and the existing migration only covers the admins table.

## Tasks

### Task 1: Create migration for current schema
<read_first>
- prisma/schema.prisma
- prisma/migrations/20260427211333_init/migration.sql
</read_first>

<action>
Run `npx prisma migrate dev --name user_models --create-only` to generate a migration SQL file that captures the full current schema (User, Server, Service, Configuration, TrafficLog, RoutingRule, Alert, UserQuota, UserProtocol models).

If the command prompts about data loss, respond 'y' since this is dev environment and the database already has these tables.

Then verify the generated migration SQL covers all tables by reading the migration file.
</action>

<acceptance_criteria>
- prisma/migrations/<latest_timestamp>_user_models/migration.sql exists
- Migration SQL contains CREATE TABLE statements for: users, servers, services, configurations, traffic_logs, routing_rules, alerts, user_quotas, user_protocols
- `npx prisma migrate status` shows no pending migrations
</acceptance_criteria>

### Task 2: Verify Prisma Client generation
<read_first>
- src/generated/prisma/models/User.ts
- src/generated/prisma/enums.ts
</read_first>

<action>
Run `npx prisma generate` to regenerate the Prisma Client with the latest schema.

Verify that:
- src/generated/prisma/enums.ts contains ServiceType, ServiceStatus, AlertSeverity, QuotaPeriod, RoutingAction
- src/generated/prisma/models/User.ts has fields: trafficQuotaBytes, speedLimitKbps
</action>

<acceptance_criteria>
- `npx prisma generate` exits successfully
- src/generated/prisma/enums.ts exists and contains all enum types
- TypeScript compilation passes (`npx tsc --noEmit` exits 0)
</acceptance_criteria>

## Verification Criteria
- Migration created and captures all 9 models
- Prisma Client generated successfully
- No TypeScript errors from generated types
