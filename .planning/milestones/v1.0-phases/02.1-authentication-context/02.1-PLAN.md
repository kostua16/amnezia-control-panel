# Phase 2.1: Authentication Context - Plan

**Created:** 2026-04-28
**Status:** Ready for execution

## Tasks

### Task 1: Create Auth Store (Zustand)
**File:** `src/lib/auth-store.ts` (NEW)

```typescript
interface User {
  id: string;
  username: string;
}

interface AuthState {
  user: User | null;
  isAuthenticated: boolean;
  isLoading: boolean;
  login: (username: string, password: string) => Promise<void>;
  logout: () => Promise<void>;
  checkAuth: () => Promise<void>;
}
```

- Use `create` from zustand
- login: placeholder that sets user + isAuthenticated (real API in Phase 2.2)
- logout: clears user + isAuthenticated (real API in Phase 2.5)
- checkAuth: placeholder (real token validation in Phase 2.3)
- Export `useAuthStore` hook

### Task 2: Export Auth Utilities
**File:** `src/lib/auth.ts` (NEW)

Export any auth helpers needed by other phases. Initially minimal — just re-export useAuthStore. Will grow in Phase 2.2-2.5.

---

## Verification

- `npx tsc --noEmit` passes
- `npm run lint` passes
- auth-store.ts exports useAuthStore with correct TypeScript types
