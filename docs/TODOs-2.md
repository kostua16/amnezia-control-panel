# TODOs-2 — Optimize / Tune / Audit Workflow Backlog

Review of the six scheduled optimize/tune/audit automation workflows, producing 10 improvements and 10 extensions per workflow. Evidence combines static YAML/script/agent-contract review with GitHub run-history mining (snapshot: 2026-07-12, last ~30 runs per workflow plus PR/issue searches).

## How to use this backlog

- **Rule (see CLAUDE.md):** before implementing any TODO, open (or claim) a GitHub issue for it, spun off from the workflow's umbrella tracking issue, and reference the TODO ID in the issue and PR.
- **Automated spin-off:** the hourly issue-catch-up run (and `/fix` on an umbrella) executes `umbrella-sub-issues.cjs`, which creates dedicated sub-issues for the top-priority unchecked items (max 3 open per umbrella, native sub-issue links, `umbrella-sub-issue` label), ticks the umbrella checklist when a sub-issue closes as completed, and closes the umbrella once every item is ticked.
- IDs are **stable** — safe to reference from issues, PRs, and commits. Do not renumber.
- **Type:** `I` = improvement (fix/harden existing behavior), `E` = extension (new capability).
- **Priority:** P0 = broken now, P1 = high value / real failure evidence, P2 = worthwhile hardening, P3 = polish.
- **Effort:** S ≤ 1h, M ≤ half day, L = larger.
- Items already tracked in `docs/gh-workflows-architecture-review.md` (P0-1 … P2-5) are cross-referenced, not duplicated.

## Umbrella tracking issues

| Workflow                       | Umbrella issue |
| ------------------------------ | -------------- |
| workflow-health-optimize (WHO) | #677           |
| audit-fix (AFX)                | #678           |
| audit-auto-prs (APR)           | #679           |
| security-audit-weekly (SEC)    | #680           |
| maintenance (MNT)              | #681           |
| monitor-github-runs (MON)      | #682           |

## Evidence snapshot (2026-07-12)

| Workflow                 | Cadence    | Last-30-run outcomes                                               | Typical / worst duration | PR family (merged / closed-unmerged) |
| ------------------------ | ---------- | ------------------------------------------------------------------ | ------------------------ | ------------------------------------ |
| workflow-health-optimize | hourly :07 | 29 success, 1 cancelled                                            | 24s–7min / 72min         | 31 / 12 (72%)                        |
| audit-fix                | 2×/day     | **1 success, 29 cancelled at ~30min timeout** (dead since ~Jun 30) | n/a — killed at timeout  | 21 / 9 (70%), none since early July  |
| audit-auto-prs           | every 3h   | 28 success, 2 cancelled at ~15min timeout                          | 1–17min / 32min          | 10 / 2 (83%)                         |
| security-audit-weekly    | weekly Mon | 3 success (all runs ever)                                          | ~30s                     | issues only                          |
| maintenance              | 2×/day     | 30 success                                                         | 1–8min / 18min           | issues only                          |
| monitor-github-runs      | hourly :56 | 28 success, 2 cancelled at ~30min                                  | 3–6min / 30min           | 27 / 2 (93%)                         |

Other mined signals:

- The `audit-manual-finding` label has **zero issues ever** — the audit-fix manual-findings→issue pipeline has never produced output.
- 32 `[workflow-health]` failure-tracking issues exist; sampled #512 failed in a **post-agent step** ("Detect exact duplicate optimization PR") after a successful (and paid) agent run.
- Timeout kills report conclusion `cancelled`, and every `report-failure` job is gated on `if: failure()` — so **timeout deaths are silent** (this is exactly how audit-fix died unnoticed for two weeks).
- security-audit-weekly's cron is Mon 06:17 UTC, but actual starts were 11:01 / 11:23 / 12:25 — a 5–6h schedule drift signal.

---

## 1. workflow-health-optimize.yml (WHO) — hourly health check & optimizer

**Current state.** Hourly: fleet gate → collect last-hour completed runs inline (log tails, timing summaries, 50KB truncation) → if problems, run-zai (25 turns, ≤3 files, minimal diffs) → duplicate/overlap detection → validate gate → draft PR. Blocked-gate and failure paths file tracking issues.

### Improvements

| ID      | Title                                                               | Category           | Prio | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                          |
| ------- | ------------------------------------------------------------------- | ------------------ | ---- | ------ | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WHO-I01 | Catch `cancelled` (timeout) runs in `report-failure`                | observability      | P1   | S      | low  | Run #433 was cancelled at 990s with no issue filed; `if: failure()` does not match cancelled. Shared fix with AFX-I02/APR-I02/MON-I01 (see XC-1).                                                                                             |
| WHO-I02 | Bound failure-log collection in `collect-runs`                      | reliability        | P1   | S      | low  | The collect script serially fetches jobs + log tails for every failed/slow run (up to 50) inside a 10-min job; a bad hour can blow the timeout. Cap processed failures (e.g. 10 newest) and note the cap in `runs_data`.                      |
| WHO-I03 | Make 50KB `runs_data` truncation JSON-safe                          | correctness        | P2   | S      | low  | `json.substring(0, 51200) + '...[truncated]'` can cut mid-object, feeding invalid JSON to the agent prompt. Drop whole trailing run entries instead.                                                                                          |
| WHO-I04 | Exclude self by workflow path, not display name                     | correctness        | P2   | S      | low  | Self-exclusion compares `r.name !== 'Workflow Health Check & Optimize'`; a rename re-enables self-analysis loops. Compare `r.path` to the workflow file. Also consider excluding sibling optimizer runs (MON) to reduce overlapping mandates. |
| WHO-I05 | Lighten the hourly collect job                                      | cost / runner-load | P1   | M      | low  | ~Half of hourly runs find no problems yet still consume a self-hosted slot with checkout + setup. Move collect to an API-only job on a light runner (cross-ref arch review P1-3).                                                             |
| WHO-I06 | Subtract queue time before flagging "slow"                          | correctness        | P2   | S      | low  | PR #643 evidence shows runs flagged slow with queueSec ≈ 1000 and 36s actual jobs. `durationSec > 300` counts queue wait; compute from job timing so queue starvation isn't misdiagnosed as workflow slowness.                                |
| WHO-I07 | Check for an existing open optimization PR before running the agent | cost / dedupe      | P2   | S      | low  | Duplicate detection runs after the (paid) 25-turn agent step. audit-auto-prs runs `check-pending-automation-pr.cjs` first; mirror that ordering.                                                                                              |
| WHO-I08 | Harden post-agent steps against wasted agent spend                  | reliability        | P2   | M      | low  | Issue #512: agent succeeded, then "Detect exact duplicate optimization PR" exited 1 and the whole run failed. Add retry/continue-on-error + explicit reporting for post-agent plumbing so a plumbing bug doesn't discard paid work.           |
| WHO-I09 | Prevent hourly runs stacking behind a slow run                      | runner-load        | P3   | S      | low  | Runs #425–427 took 21–72 min; with `cancel-in-progress: false` the next hourly run queues behind. Skip a scheduled run when the previous is still active (keep dispatch unaffected).                                                          |
| WHO-I10 | Audit token scopes (`GH_PAT` vs `GITHUB_TOKEN`) per job             | security           | P3   | S      | low  | Optimize job uses GH_PAT (needed for workflow-file pushes), but `actions: write` at workflow level and PAT use in report jobs deserve a minimum-scope pass (cross-ref arch review P2-4).                                                      |

### Extensions

| ID      | Title                                                 | Category       | Prio | Effort | Rationale                                                                                                                                                                                                               |
| ------- | ----------------------------------------------------- | -------------- | ---- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WHO-E01 | Persist per-run health summaries for trend analysis   | observability  | P1   | M      | Optimizer only sees the last hour; a small metrics store (artifact or committed JSONL) enables regression detection ("CI 20% slower this week") instead of point-in-time reactions.                                     |
| WHO-E02 | Cron-collision detector + stagger proposals           | runner-load    | P2   | M      | DONE: agent + skill updated with cron scan, collision detection, stagger proposal rules, and known schedule map. YAML prompt update still needed (workflow file).                                                                                         |
| WHO-E03 | Queue-latency telemetry and starvation alerts         | observability  | P2   | M      | Queue waits of ~1000s already observed. Emit queueSec percentiles per window; file one alert issue when p95 exceeds threshold.                                                                                          |
| WHO-E04 | Flaky-workflow detection across hours                 | reliability    | P2   | M      | Same job alternating pass/fail across windows should produce a "flaky" tracking issue, not repeated one-off fixes.                                                                                                      |
| WHO-E05 | Weekly cost/turn digest from `claude_metrics_json`    | cost           | P2   | M      | Failure issues already embed cost metrics ($0.84 for 5 turns in #512); aggregate cost/turns per workflow weekly to tune MAX_TURNS budgets.                                                                              |
| WHO-E06 | Auto re-run for transient infra failures              | reliability    | P2   | M      | Docker `npm ci` ECONNRESET-style failures need a re-run, not a YAML PR; add a deterministic transient-failure classifier + `actions_run_trigger` re-run path.                                                           |
| WHO-E07 | Impact-ranked problem selection                       | prompt-quality | P3   | S      | With a 3-file cap, the agent should rank problems by (failure count × workflow criticality) rather than input order; encode ranking in the prompt/contract.                                                             |
| WHO-E08 | Attach actionlint + invariant-test results to PR body | testability    | P3   | S      | The optimization PR body lists evidence but not the validation outputs; embed the validate-pr-gate summary for reviewer confidence.                                                                                     |
| WHO-E09 | Self-throttle on falling merge rate                   | backpressure   | P3   | M      | 12 of 43 optimization PRs closed unmerged. Track the rolling merge ratio; below a floor, switch to issue-only mode until a human resets.                                                                                |
| WHO-E10 | Classify `startup_failure` runs                       | reliability    | P3   | S      | Evidence in PR #643: "Fix Branch ×2 startup_failure — not workflow-fixable" — but startup failures are often YAML/trigger syntax errors, which is precisely this workflow's mandate. Include them in problem detection. |

---

## 2. audit-fix.yml (AFX) — twice-daily autonomous code audit & fix

**Current state.** 2×/day: fleet gate → heavy setup (deps, prisma, gsd, rtk, 9 plugins) → run-zai (70 turns) audits debt/smells/missing tests and fixes narrowly → lane classification (audit-safe vs manual-only via `policy.json auditSafe`) → validate gate → PR; manual findings should become issues; reconcile job closes resolved dep-vuln issues.

### Improvements

| ID      | Title                                                              | Category      | Prio   | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                                         |
| ------- | ------------------------------------------------------------------ | ------------- | ------ | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AFX-I01 | Fix the chronic 30-min timeout — workflow is dead                  | reliability   | **P0** | M      | med  | 29 of the last 30 runs (Jun 30 → Jul 11, schedule **and** dispatch) were cancelled at ~1830s. The 70-turn budget + heavy setup no longer fits `timeout-minutes: 30`. Raise the timeout and/or shrink scope (see AFX-E01); verify one full pass completes.    |
| AFX-I02 | File a failure issue on timeout-cancelled runs                     | observability | **P0** | S      | low  | `report-failure` is `if: failure()`; timeouts conclude `cancelled`, so two weeks of dead runs produced zero issues. Add cancelled-handling (XC-1). The kos-audit-fix contract even mislabels these cancels as "concurrency supersession" — correct that too. |
| AFX-I03 | Give the agent a wall-clock deadline                               | reliability   | P1     | M      | low  | When the job times out, **all** agent work is lost. Pass a STOP_BY timestamp (or remaining-minutes input) via prompt so the agent wraps up, verifies, and reports before the kill. Candidate shared `run-zai` input (XC-2).                                  |
| AFX-I04 | Trim the plugin list to the audit mission                          | cost          | P1     | S      | low  | 9 plugins including `frontend-design`, `superpowers`, `caveman` inflate setup time and context for a code-audit task. Keep lsp/serena/context7/code-review; measure setup-share before/after.                                                                |
| AFX-I05 | Verify the manual-findings→issue pipeline end-to-end               | correctness   | P1     | M      | low  | Label `audit-manual-finding` has **0 issues ever** despite months of runs. Either agents never emit `manual_findings`, or `upsert-audit-manual-findings.cjs` / `finding_count` gating is broken. Add a unit/e2e test that exercises the full path.           |
| AFX-I06 | Add a pending-PR check before the agent runs                       | dedupe        | P2     | S      | low  | audit-auto-prs checks for a pending sibling PR before spending turns; audit-fix doesn't — an unmerged audit PR plus a new run produces overlapping work. Mirror `check-pending-automation-pr.cjs`.                                                           |
| AFX-I07 | Surface (don't silently discard) agent edits to `.github/actions/` | observability | P2     | S      | low  | `git checkout HEAD -- .github/actions/` restores composite actions without a trace. Log the discarded paths to the step summary so repeated attempts to edit actions become visible signal.                                                                  |
| AFX-I08 | Let `workflow_dispatch` request a longer timeout                   | ux            | P2     | S      | low  | Dispatch runs #104–#111 also died at the fixed timeout. Add a `timeout_minutes`-style input (bounded) so a human can run one full deep pass.                                                                                                                 |
| AFX-I09 | Stagger 21:53 slot against the hourly :56 monitor                  | runner-load   | P3     | S      | low  | audit-fix 21:53 and monitor 21:56 land on `[self-hosted, big]` within 3 minutes. Shift one cron (cross-ref arch review P2-3).                                                                                                                                |
| AFX-I10 | Tighten the structured-output JSON schema                          | testability   | P3     | S      | low  | `additionalProperties: true` and optional fields on `fixed_findings` items make downstream parsing (classification, issue upsert) fragile; require `finding_id`/`severity`/`summary`/`files` on both arrays.                                                 |

### Extensions

| ID      | Title                                                 | Category      | Prio | Effort | Rationale                                                                                                                                                                            |
| ------- | ----------------------------------------------------- | ------------- | ---- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AFX-E01 | Audit-area rotation per run                           | reliability   | P1   | M      | A full-repo audit no longer fits one run. Rotate focus areas (components → hooks → lib → api → tests …) via a persisted cursor so each run fits the budget and coverage accumulates. |
| AFX-E02 | Persistent findings backlog across runs               | efficiency    | P1   | M      | Unfixed findings are rediscovered from scratch each run. Persist a findings ledger (e.g. `.planning/audit-backlog.json`) that runs append to and consume from.                       |
| AFX-E03 | Coverage-guided missing-test detection                | quality       | P2   | M      | Use vitest coverage output to point the agent at the worst-covered files instead of heuristic smell-hunting.                                                                         |
| AFX-E04 | Per-finding verification matrix in the PR body        | ux            | P3   | S      | Fixed-findings list exists; add per-finding "verify command → result" rows so reviewers can spot unverified fixes.                                                                   |
| AFX-E05 | Severity escalation lane for critical findings        | reliability   | P2   | M      | A critical finding in a manual-only path currently just waits in a draft PR; also open a labeled issue so it enters the triage/fix-issue pipeline.                                   |
| AFX-E06 | Dedupe findings against open issues/PRs before fixing | dedupe        | P2   | M      | Avoid re-fixing what fix-issue or an open audit PR already covers; query open items by file/finding fingerprint first.                                                               |
| AFX-E07 | "Quick wins only" mode under fleet back-pressure      | backpressure  | P3   | S      | When open-automation-PR count is near the cap, run a reduced S-effort-only audit instead of skipping entirely.                                                                       |
| AFX-E08 | Extend reconcile beyond dependency-vuln issues        | dedupe        | P2   | M      | `reconcile-audit-issues.sh` only closes dep-vuln issues; reconcile fixed findings against the findings ledger (AFX-E02) and close linked issues on merge.                            |
| AFX-E09 | Report turns-used vs budget per run                   | observability | P2   | S      | Emit turns/wall-time utilization to the step summary + metrics store (WHO-E01) so budget tuning is data-driven, not guesswork.                                                       |
| AFX-E10 | Cache warm deps between runs                          | cost          | P2   | M      | Twice-daily full `npm ci` + prisma generate on big runners; a shared cache (or preserved workdir strategy consistent with runner disk hygiene) reclaims minutes for actual auditing. |

---

## 3. audit-auto-prs.yml (APR) — 3-hourly audit of automation-created PRs

**Current state.** Every 3h: pending-PR/back-pressure check → run-zai (90 turns) inspects open automation PRs (duplicates, conflicts, stale, repeated work) using `rtk gh` evidence, read-only toward existing PRs; may implement one narrow systemic fix → duplicate/overlap detection → PR or rich no-change summary.

### Improvements

| ID      | Title                                                 | Category        | Prio | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                          |
| ------- | ----------------------------------------------------- | --------------- | ---- | ------ | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| APR-I01 | Align 90-turn budget with the 15-min job timeout      | reliability     | P1   | S      | low  | Runs #149 and #160 died at ~925s (the timeout); several successes run 800–1000s. Either raise `timeout-minutes` to ~25 or cut MAX_TURNS; add the STOP_BY deadline (XC-2).                                                                     |
| APR-I02 | Catch timeout-cancelled runs in `report-failure`      | observability   | P1   | S      | low  | Same `if: failure()` gap as siblings (XC-1); both timeout deaths were silent.                                                                                                                                                                 |
| APR-I03 | Allow `actionlint` in `allowed-tools`                 | correctness     | P1   | S      | low  | The prompt **requires** "run actionlint" for workflow YAML changes, but the allowed-tools list has no `Bash(actionlint:*)` — the agent cannot comply, so YAML fixes ship without the mandated check or the agent burns turns on failed calls. |
| APR-I04 | Use the shared fleet-back-pressure gate job           | consistency     | P2   | S      | low  | Siblings gate via `fleet-back-pressure`; this workflow re-implements throttling through `check-pending-automation-pr.cjs` flags inline. Converge on the shared action so thresholds live in one place.                                        |
| APR-I05 | Fix the empty "Auto PR audit report" evidence section | ux              | P2   | D      | low  | Resolved by PR #753: removed duplicate `claude-last-output` from audit-auto-prs evidence section.                                                                                                                                                   |
| APR-I06 | Apply the open-PR cap to `workflow_dispatch` too      | backpressure    | P2   | S      | low  | `--max-open-automation-prs` is `8` only for schedule and `0` (disabled) for dispatch; a manual run during PR pileup worsens the pile. Make dispatch opt-out explicit via an input instead.                                                    |
| APR-I07 | Pass `--title-prefix` to duplicate detection          | dedupe          | P3   | S      | low  | Unlike WHO/MON, the duplicate check runs without a title prefix, so unrelated automation PRs can suppress a legitimate audit PR (or vice versa).                                                                                              |
| APR-I08 | Keep `install-uv` (required by serena MCP)                   | cost            | P3   | S      | low  | Premise false: this workflow provisions serena MCP (`plugins: serena@claude-plugins-official` plus `mcp__plugin_serena_serena__*` entries in `allowed-tools`), and serena needs the Python/uv runtime (`setup-environment` `install-uv` desc; `.github/workflows/documentation.md`). Dropping `install-uv` breaks serena tool calls at runtime. Do not remove.                                                                                                                                          |
| APR-I09 | Normalize the `allowed-bots` list via policy.json     | maintainability | P3   | S      | low  | `github-actions,github-actions[bot],claude[bot]` here vs `claude[bot],github-actions[bot]` elsewhere; centralize the canonical list (single source in `policy.json`).                                                                         |
| APR-I10 | Make `no_new_pr_reason` conditional in the schema     | testability     | P3   | S      | low  | The schema requires `no_new_pr_reason` even when a PR **is** created, forcing filler values that pollute the structured report; allow empty/optional when `systemic_fix_made` is true.                                                        |

### Extensions

| ID      | Title                                               | Category      | Prio | Effort | Rationale                                                                                                                                                                             |
| ------- | --------------------------------------------------- | ------------- | ---- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| APR-E01 | Weekly human-disposition digest issue               | observability | P1   | M      | Recommendations for PRs needing human action (close/merge/rebase) are trapped in step summaries nobody reads. Publish one rolling digest issue with per-PR recommended actions.       |
| APR-E02 | File-overlap conflict matrix artifact               | observability | P2   | M      | PR #446 found 4 file-overlap conflicts across 6 GSD PRs; make the overlap matrix a standing artifact/step-summary table each run.                                                     |
| APR-E03 | Stale automation-PR close-list feed                 | dedupe        | P2   | M      | Produce a structured "recommend close" list consumable by project-manager, instead of prose-only recommendations.                                                                     |
| APR-E04 | Persist repeat-pattern memory across runs           | efficiency    | P2   | M      | Systemic patterns (e.g. duplicate-generator bugs) are re-derived every 3h; persist detected patterns + status so repeated findings escalate rather than repeat.                       |
| APR-E05 | Automation PR body-quality lint                     | quality       | P2   | M      | The audit already flags "missing evidence in autogenerated PR descriptions"; codify required sections per generator workflow and report offenders to their source workflow's backlog. |
| APR-E06 | Opt-in duplicate labeling of existing PRs           | ux            | P3   | M      | Hard rule forbids mutating PRs; add an explicitly user-enabled mode (input/label gate) where confirmed duplicates get a `duplicate-candidate` label to aid manual cleanup.            |
| APR-E07 | Risk-ranked review queue for open automation PRs    | ux            | P3   | M      | Score PRs by size, touched paths (policy globs), and check state; publish a ranked "review next" list in the step summary.                                                            |
| APR-E08 | Recurring orphan-branch check                       | hygiene       | P2   | S      | PR #446 found 26 orphan automation branches; add a standing orphan count to the audit report and dispatch `auto-pr-branch-cleanup` when above threshold.                              |
| APR-E09 | Cross-check auto PRs against roadmap/backlog scope  | quality       | P3   | M      | Detect automation PRs doing work outside `.planning/ROADMAP.md` / TODO backlogs — early warning for scope-drifted generators.                                                         |
| APR-E10 | Escalate twice-recommended systemic fixes to issues | reliability   | P2   | S      | If the same systemic fix is recommended in ≥2 consecutive audits without a merged PR, open a tracking issue so the pattern stops relying on PR luck.                                  |

---

## 4. security-audit-weekly.yml (SEC) — weekly npm audit

**Current state.** Mondays: `npm audit --audit-level=high` on a big runner; on advisory hit, create or body-refresh a single tracking issue with truncated output. No AI, no report-failure job, ~30s runtime, 3 runs of history.

### Improvements

| ID      | Title                                                   | Category      | Prio | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                             |
| ------- | ------------------------------------------------------- | ------------- | ---- | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SEC-I01 | Move off the `big` runner                               | cost          | P2   | S      | low  | A ~30s lockfile audit occupies the scarce `[self-hosted, big]` pool; plain self-hosted (or a light label) suffices.                                                                                                                              |
| SEC-I02 | Add a report-failure path for infra errors              | observability | P2   | S      | low  | `continue-on-error` + "open issue on failure" only handles advisory hits; if `npm audit` itself errors (registry outage) or issue creation fails, the run is silently green-ish. Distinguish "vulnerabilities found" from "audit could not run". |
| SEC-I03 | Label the tracking issue on creation                    | correctness   | P2   | S      | low  | `gh issue create` runs without `--label security`, so the weekly issue bypasses label-driven triage/routing that every other automation issue gets.                                                                                              |
| SEC-I04 | Preserve history when refreshing the tracking issue     | ux            | P2   | S      | low  | Refresh replaces the issue body, destroying the previous week's findings; append a comment per run (like WHO's blocked-gate issue) and keep the body as latest-state.                                                                            |
| SEC-I05 | Investigate the 5–6h schedule drift                     | reliability   | P3   | S      | low  | Cron 06:17 Mon vs actual starts 11:01–12:25 across all 3 runs — likely runner availability; confirm and either accept (document) or move the slot to when runners are idle.                                                                      |
| SEC-I06 | Use a marker/exact-match for issue dedupe search        | correctness   | P3   | S      | low  | `--search 'weekly npm audit in:title'` can match unrelated issues; search the exact title or a dedicated marker label.                                                                                                                           |
| SEC-I07 | Parse `npm audit --json` for a severity summary line    | ux            | P3   | S      | low  | The issue is a raw 60KB dump; a one-line `critical: n, high: m, fixAvailable: k` header makes weekly review instant.                                                                                                                             |
| SEC-I08 | Deconflict with maintenance's twice-daily security scan | dedupe        | P2   | S      | low  | maintenance also runs npm-audit sweeps and files `Security:` issues 14×/week; define single ownership (e.g. weekly = tracking issue of record, maintenance = only critical escalations) to stop dual reporting.                                  |
| SEC-I09 | Set `persist-credentials: false` on checkout            | security      | P3   | S      | low  | The job only reads the lockfile and calls `gh` via env token; drop the persisted git credential surface.                                                                                                                                         |
| SEC-I10 | Auto-close the tracking issue when the audit is clean   | correctness   | P2   | S      | low  | Nothing closes the issue after remediation; a clean weekly run should close (state_reason completed) or comment "resolved" on the open tracking issue.                                                                                           |

### Extensions

| ID      | Title                                       | Category    | Prio | Effort | Rationale                                                                                                                                                                              |
| ------- | ------------------------------------------- | ----------- | ---- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-E01 | Severity-tiered advisory table in the issue | ux          | P2   | S      | Parse `--json` into a table (package, severity, range, fixAvailable) instead of a raw CLI dump.                                                                                        |
| SEC-E02 | Auto-fix PR when `npm audit fix` is clean   | reliability | P1   | M      | If `npm audit fix` resolves advisories lockfile-only and tests pass, open an automation PR through the existing commit-and-push/upsert-PR actions — same pattern as sibling workflows. |
| SEC-E03 | License compliance scan                     | compliance  | P3   | M      | Add a license-checker pass with an allowlist; report violations in the same weekly issue.                                                                                              |
| SEC-E04 | Weekly SBOM artifact (CycloneDX)            | compliance  | P3   | M      | Generate and attach an SBOM artifact per run for supply-chain traceability (complements `supply-chain.yml`).                                                                           |
| SEC-E05 | Secret-scan sweep (gitleaks)                | security    | P2   | M      | Weekly full-history secret scan with issue-on-findings; complements the push-time checks.                                                                                              |
| SEC-E06 | `npm outdated` major-drift report           | hygiene     | P3   | S      | Append a majors-behind table so upgrade debt is visible before it becomes advisories.                                                                                                  |
| SEC-E07 | Advisory-age escalation                     | reliability | P2   | S      | Track first-seen per advisory; if unresolved after 2 weekly runs, escalate (label/priority bump or dispatch fix-issue).                                                                |
| SEC-E08 | Docker base-image scan (trivy)              | security    | P2   | M      | The repo ships a Docker image (`docker-image.yml`); scan the published image weekly, not just npm deps.                                                                                |
| SEC-E09 | Feed advisories into the audit-fix backlog  | integration | P2   | S      | Write advisories into the findings ledger (AFX-E02) so the autonomous fixer picks up dependency work in safe lanes.                                                                    |
| SEC-E10 | GitHub Actions dependency audit             | security    | P3   | M      | Weekly check of pinned action SHAs against upstream tags/advisories (extends `supply-chain.yml`'s pin verification with staleness/advisory data).                                      |

---

## 5. maintenance.yml (MNT) — twice-daily maintenance sweep

**Current state.** 2×/day: label ensure → run-zai (50 turns, continue-on-error) performs a 6-part sweep (gsd health, security scan, progress, TODO/FIXME in recent commits, stale issues, resolved-debug-doc pruning) and creates categorized issues via `gh` only where findings exist.

### Improvements

| ID      | Title                                                          | Category      | Prio | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                                                              |
| ------- | -------------------------------------------------------------- | ------------- | ---- | ------ | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MNT-I01 | Fix the lost-commit bug in debug-doc pruning                   | correctness   | P1   | S      | low  | The prompt tells the agent to delete stale debug docs and **commit**, but the workflow has no commit-and-push step and the agent must not push — pruning commits evaporate with the runner workdir. Either add a push path or convert pruning to a PR through the shared actions. |
| MNT-I02 | Add the fleet back-pressure gate                               | consistency   | P1   | S      | low  | Every sibling schedules through `fleet-back-pressure`; maintenance doesn't, so it runs (and files issues) even during PR pileups the gate exists to relieve.                                                                                                                      |
| MNT-I03 | Align 50-turn budget with the 10-min timeout                   | reliability   | P1   | S      | low  | Sweep durations already reach 8–18 min wall-clock windows; 50 turns across a 6-part sweep in 10 job-minutes is the same budget mismatch that killed audit-fix. Add deadline awareness (XC-2) and/or split the sweep.                                                              |
| MNT-I04 | Catch cancelled runs in `report-failure`                       | observability | P2   | S      | low  | Same `if: failure()` gap (XC-1); a timeout here would be silent today.                                                                                                                                                                                                            |
| MNT-I05 | Enforce programmatic issue dedupe before creation              | dedupe        | P2   | M      | low  | "Do NOT create issues if no findings" relies on agent discipline; 14 runs/week can duplicate `Security:` / `Maintenance:` issues. Give the agent exact `gh issue list --search` dedupe commands or a helper script gate (the kos-maintenance no-spam rule, enforced).             |
| MNT-I06 | Single ownership for dependency-vuln reporting                 | dedupe        | P2   | S      | low  | Coordinate with SEC-I08: maintenance escalates critical-only; the weekly audit owns the tracking issue of record.                                                                                                                                                                 |
| MNT-I07 | Fix the TODO/FIXME scan to inspect code, not commit messages   | correctness   | P2   | S      | low  | `git log --grep="TODO\|FIXME"` only matches commit **messages**; the intent is TODOs in recently changed code. Scan diffs (`git log -p -S` or grep changed files) instead.                                                                                                        |
| MNT-I08 | Clarify stale-issue boundaries vs stale.yml and issue-catch-up | dedupe        | P3   | S      | low  | Three workflows touch stale items (60d PR stale/close, 90d issue review, orphan catch-up); document boundaries in the prompts so items aren't triple-processed.                                                                                                                   |
| MNT-I09 | Trim allowed-tools to the sweep's needs                        | security      | P3   | S      | low  | The agent gets `mcp__github_inline_comment`, `gh pr comment`, and full `Bash(git *)` for a sweep that mostly reads and files issues; least-privilege the list.                                                                                                                    |
| MNT-I10 | Add structured output (JSON schema) + track-progress           | testability   | P3   | S      | low  | Siblings return machine-readable reports; maintenance's results live only in prose logs, making the no-spam discipline and category outcomes untestable.                                                                                                                          |

### Extensions

| ID      | Title                                         | Category      | Prio | Effort | Rationale                                                                                                                                                                                        |
| ------- | --------------------------------------------- | ------------- | ---- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| MNT-E01 | Runner disk-usage sweep with cleanup dispatch | reliability   | P1   | S      | Disk exhaustion (ENOSPC) is a known fleet failure mode; add a deterministic df check that dispatches `cleanup-runner.yml` below a threshold — cheaper and earlier than MON's log-grep self-heal. |
| MNT-E02 | Schedule-drift detector                       | observability | P2   | M      | Compare each scheduled workflow's cron vs actual `run_started_at` (SEC drifted 5–6h); report chronic drift as a runner-capacity signal.                                                          |
| MNT-E03 | Automation KPI digest                         | observability | P2   | M      | Per 12h window: PRs opened/merged/closed by the fleet, issues filed/closed, gate skips — one summary comment/issue so fleet output is reviewable at a glance (shares the WHO-E01 store).         |
| MNT-E04 | `.planning/` garbage collection               | hygiene       | P2   | M      | Completed-phase artifacts accumulate; extend the debug-doc pruning into a policy-driven archive step (via `gsd:cleanup`) with a PR.                                                              |
| MNT-E05 | Label-taxonomy audit                          | hygiene       | P3   | S      | Report unused/orphan labels and labels missing from `policy.json labels` so the label source of truth stays authoritative.                                                                       |
| MNT-E06 | Docs link-rot check                           | quality       | P3   | S      | Validate intra-repo links in README/docs; file one consolidated issue on breakage.                                                                                                               |
| MNT-E07 | Prisma/SQLite hygiene check                   | reliability   | P3   | M      | Verify `prisma migrate status` cleanliness and flag drifted schema/generated-client mismatches before they break automation runs.                                                                |
| MNT-E08 | Runner health self-check                      | observability | P2   | M      | Track queue latency and runner online-count per sweep; alert when the big pool is saturated (feeds WHO-E03).                                                                                     |
| MNT-E09 | Weekly rollup of maintenance issue outcomes   | observability | P3   | S      | How many maintenance-filed issues got fixed vs ignored — closes the loop on whether the sweep produces value or noise.                                                                           |
| MNT-E10 | TODO-backlog freshness check                  | integration   | P3   | S      | Cross-check `docs/TODOs-2.md` items against umbrella issue checklists and flag drift (unchecked-but-merged, checked-but-unmerged).                                                               |

---

## 6. monitor-amnezia-control-panel-github-runs.yml (MON) — hourly run monitor & fixer

**Current state.** Hourly: fleet gate → self-computed monitor window (since previous completed monitor run) → run-zai (80 turns) inspects failed/slow runs via `rtk gh` and applies narrow evidence-backed fixes → own changed-files verification → duplicate detection → validate gate → PR. Includes a deterministic disk-pressure self-heal that greps recent run logs for ENOSPC and dispatches cleanup.

### Improvements

| ID      | Title                                                                | Category        | Prio | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                                                                               |
| ------- | -------------------------------------------------------------------- | --------------- | ---- | ------ | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MON-I01 | Catch timeout-cancelled runs in `report-failure`                     | observability   | P1   | S      | low  | Runs #231 and #239 died at ~1811s (25-min job timeout) with no issue filed — same `if: failure()` gap (XC-1).                                                                                                                                                                                      |
| MON-I02 | Align 80-turn budget with the 25-min timeout                         | reliability     | P1   | S      | low  | The two cancels show investigations can exceed the window; add the STOP_BY deadline (XC-2) so partial findings get reported instead of destroyed.                                                                                                                                                  |
| MON-I03 | Replace the bespoke "Verify changed files" step with the shared gate | maintainability | P2   | M      | low  | ~90 lines of inline verification logic duplicate `validate-pr-gate` semantics (with subtle differences like the `.planning/` prettier carve-out); fold the carve-outs into the shared action to stop drift.                                                                                        |
| MON-I04 | Cache disk-heal log scans between runs                               | cost            | P2   | S      | low  | The self-heal greps up to 5 full run logs (90s cap each) **every hour**, and the same old ENOSPC runs re-trigger cleanup until they age out of the last-5 window. Persist checked run IDs / use a time window.                                                                                     |
| MON-I05 | Anchor the monitor window to scheduled runs only                     | correctness     | P2   | S      | low  | Any completed run (including `workflow_dispatch` experiments) advances the window; a dispatch run mid-hour narrows the next scheduled window and can skip runs. Filter the anchor to schedule-event runs.                                                                                          |
| MON-I06 | Review the `claude-workflow-optimize-` trust-prefix interplay        | security        | P2   | S      | low  | Monitor branches (`claude-workflow-optimize-github-runs-*`) inherit `trustedPlanning.branchPrefixes` matching for `claude-workflow-optimize-`; confirm the planning-trust path (auto-mergeable `.planning/**`) is intended for monitor PRs and document it in `policy.json`.                       |
| MON-I07 | De-overlap mandates with workflow-health-optimize                    | dedupe          | P2   | M      | med  | Both hourly workflows analyze failed/slow runs and edit workflow YAML (offset by ~49 min); only PR-overlap detection prevents duplicate fixes. Split mandates explicitly (e.g. WHO = YAML tuning from provided data; MON = log-deep root-cause + non-YAML fixes) or merge them (arch review P0-3). |
| MON-I08 | Tighten allowed-tools to rtk-first                                   | security        | P3   | S      | low  | The prompt mandates `rtk`-prefixed commands, but raw `Bash(gh:*)` and `Bash(git:*)` are allowed — one prompt drift away from unintended mutations. Allow the narrow raw commands actually needed.                                                                                                  |
| MON-I09 | Make disk self-heal dispatch idempotent per signature                | reliability     | P3   | S      | low  | Two matches trigger `cleanup-runner.yml` dispatch hourly while the signature persists; record last-dispatch time and skip if a cleanup ran since.                                                                                                                                                  |
| MON-I10 | Deduplicate window config between env and steps                      | maintainability | P3   | S      | low  | `MONITOR_WORKFLOW_FILE`, thresholds, and lookback defaults are spread across env, inputs, and inline node; consolidate into one config block/script with tests.                                                                                                                                    |

### Extensions

| ID      | Title                                                    | Category      | Prio | Effort | Rationale                                                                                                                                                         |
| ------- | -------------------------------------------------------- | ------------- | ---- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MON-E01 | Emit per-window health metrics to the shared trend store | observability | P1   | M      | The monitor already computes run/duration data hourly; persisting it (WHO-E01 store) turns two ad-hoc analyzers into one trend-aware system.                      |
| MON-E02 | Flaky-run detection across windows                       | reliability   | P2   | M      | Same workflow alternating fail/pass across windows → one flaky tracking issue instead of repeated per-failure investigations.                                     |
| MON-E03 | Queue-latency tracking per run                           | observability | P2   | S      | Record `run_started_at - created_at` for monitored runs; starvation shows up here first (queue waits of ~1000s already observed by WHO).                          |
| MON-E04 | Auto re-run for classified transient failures            | reliability   | P2   | M      | For deterministic transient signatures (ECONNRESET, registry 5xx), re-run the failed workflow via the actions API rather than proposing a code fix.               |
| MON-E05 | Suspect-commit correlation in failure reports            | quality       | P3   | M      | Correlate first-failure time with merges in the window and name suspect PRs in the report — cuts human triage time.                                               |
| MON-E06 | Per-run cost/turn telemetry aggregation                  | cost          | P2   | S      | Aggregate `claude_metrics_json` (cost visible in failure issues today) into the metrics store to tune the fleet's budgets (pairs with WHO-E05).                   |
| MON-E07 | Hung-runner detection                                    | reliability   | P2   | M      | Detect runs queued > threshold or in_progress > 2× historical p95 and alert/dispatch cleanup — extends self-heal beyond disk.                                     |
| MON-E08 | Trust-gate skip observability                            | observability | P3   | S      | Dozens of monitored runs conclude `skipped` by design (policy gates); summarize skip reasons per window so a misconfigured gate (everything skipping) is noticed. |
| MON-E09 | Weekly fleet SLA report                                  | observability | P3   | M      | Success rate, MTTR from failure→fix-merged, duration trends per workflow — one weekly issue (merge with MNT-E03/MNT-E09 rollups).                                 |
| MON-E10 | Repeated-signature escalation to a single tracking issue | dedupe        | P2   | S      | After 3+ identical failure signatures across windows, open/refresh one tracking issue and stop attempting per-window PR fixes.                                    |

---

## 7. triage.yml + issue-catch-up.yml (TRI) — issue triage & hourly catch-up

### Improvements

| ID      | Title                                            | Category | Prio | Effort | Risk | Rationale / evidence                                                                                                                                                                                                                                                                       |
| ------- | ------------------------------------------------ | -------- | ---- | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TRI-I01 | Drop the `issue_comment` trigger from triage.yml | noise    | P3   | S      | low  | Every comment on any issue or PR starts a triage run that ends `skipped` (dozens per day — pure runner noise). `/triage` comments are no longer the re-triage mechanism: issue-catch-up dispatches `triage.yml` via the workflow_dispatch API. Keep `issues: opened` + `workflow_dispatch`. |

## Cross-cutting rollup

These patterns repeat across workflows; fix once at the shared layer, then adopt per workflow.

| ID   | Theme                                                                                                                     | Member TODOs                                                  | Where to fix                                                                                                                                                                     |
| ---- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| XC-1 | **Silent timeout deaths** — every `report-failure` job uses `if: failure()`, which never matches timeout-`cancelled` runs | WHO-I01, AFX-I02, APR-I02, MNT-I04, MON-I01                   | One pattern change (e.g. `if: failure() \|\| cancelled()` + conclusion check) across the six workflows, or inside `report-failure` usage docs; add an e2e characterization test. |
| XC-2 | **Turn budget vs job timeout mismatch** — agents are killed mid-work and all output is lost                               | AFX-I01, AFX-I03, APR-I01, MNT-I03, MON-I02                   | Add a shared deadline input to `run-zai` (STOP_BY in prompt) + a budget-alignment table in `documentation.md`.                                                                   |
| XC-3 | **Inconsistent back-pressure** — gate present in WHO/AFX/MON, absent or re-implemented in APR/MNT/SEC                     | APR-I04, MNT-I02                                              | Adopt `fleet-back-pressure` uniformly; thresholds in `policy.json`.                                                                                                              |
| XC-4 | **Dual security reporting channels**                                                                                      | SEC-I08, MNT-I06                                              | Ownership decision documented in both prompts.                                                                                                                                   |
| XC-5 | **No shared metrics/trend store** — each analyzer sees only its window                                                    | WHO-E01, WHO-E05, AFX-E09, MNT-E03, MON-E01, MON-E06, MON-E09 | Design one metrics sink first (artifact vs committed JSONL vs issue), then feed all workflows into it.                                                                           |
| XC-6 | **Overlapping analyzer mandates** (WHO vs MON, plus arch review P0-3 fleet consolidation)                                 | WHO-I04, MON-I07                                              | Decide split-or-merge once; encode in prompts/contracts.                                                                                                                         |

## Global priority index

- **P0 (2):** AFX-I01, AFX-I02 — audit-fix is currently dead and died silently.
- **P1 (20):** WHO-I01, WHO-I02, WHO-I05, WHO-E01, AFX-I03, AFX-I04, AFX-I05, AFX-E01, AFX-E02, APR-I01, APR-I02, APR-I03, APR-E01, MNT-I01, MNT-I02, MNT-I03, MON-I01, MON-I02, MON-E01, SEC-E02 — (start with XC-1/XC-2 members; they share one root cause each).
- **P2 (…):** remaining hardening items — schedule after the P1 wave; prefer grouping by XC theme over by workflow.
- **P3:** polish; batch opportunistically into related PRs (respecting the workflow-change e2e protocol).

> Implementation protocol reminder: any change to `.github/workflows/**` must keep the e2e suite green (`npm run test-only` **and** `cd .github/workflows && node --test scripts/__tests__/*.test.cjs`) and update `docs/workflow-e2e-scenarios.md` when behavior changes. See `docs/code-standards.md` → "Workflow change protocol".
