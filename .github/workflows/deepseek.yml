name: DeepSeek Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened]
  pull_request_review:
    types: [submitted]

concurrency:
  group: deepseek-${{ github.event.issue.number || github.event.pull_request.number || github.sha }}
  cancel-in-progress: false

env:
  MAX_TURNS: 100
  NODE_VERSION: '22.x'

jobs:
  authorize:
    runs-on: self-hosted
    outputs:
      triggered: ${{ steps.gate.outputs.triggered }}
      trusted: ${{ steps.gate.outputs.trusted }}
      key-available: ${{ steps.check-key.outputs.available }}
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-environment
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          node-version: ${{ env.NODE_VERSION }}

      - name: Evaluate trigger trust
        id: gate
        shell: bash
        run: |
          node .github/workflows/scripts/evaluate-trigger-policy.cjs \
            --mode deepseek \
            --policy-file .github/workflows/policy.json \
            --event-path "$GITHUB_EVENT_PATH" \
            --event-name "$GITHUB_EVENT_NAME" \
            > /tmp/trigger-policy.json
          {
            echo "triggered=$(jq -r '.triggered' /tmp/trigger-policy.json)"
            echo "trusted=$(jq -r '.trusted' /tmp/trigger-policy.json)"
            echo "author_association=$(jq -r '.author_association' /tmp/trigger-policy.json)"
          } >> "$GITHUB_OUTPUT"

      - name: Check DeepSeek API key available
        id: check-key
        shell: bash
        run: |
          if [[ -z "${{ secrets.DEEPSEEK_API_KEY }}" ]]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

  deepseek:
    needs: [authorize]
    if: needs.authorize.outputs.triggered == 'true' && needs.authorize.outputs.trusted == 'true' && needs.authorize.outputs.key-available == 'true'
    runs-on: [self-hosted, big]
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
      issues: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: ./.github/actions/setup-environment
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          node-version: ${{ env.NODE_VERSION }}
          install-deps: 'true'
          install-gsd: 'true'
          install-rtk: 'true'

      - name: Setup git identity
        uses: ./.github/actions/setup-bot-git

      - name: Run DeepSeek Code
        id: deepseek
        uses: ./.github/actions/run-deepseek
        with:
          api-key: ${{ secrets.DEEPSEEK_API_KEY }}
          max-turns: '${{ env.MAX_TURNS }}'
          track-progress: 'true'
          allowed-tools: 'mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr review:*),Bash(gh pr edit:*),Bash(gh pr diff:*),Bash(gh issue comment:*),Bash(gh issue view:*),Bash(gh issue edit:*),Bash(npm *),Bash(npx *),Bash(node *),Bash(ls *),Bash(git *),Bash(cat *),Bash(grep *),Bash(find *),Bash(mkdir *),Bash(head *),Bash(tail *),Bash(sort *),Bash(wc *),Bash(echo *),Bash(date *),Bash(rtk:*)'
          prompt: 'Follow our coding standards. Ensure all new code has tests. Use TypeScript for new files. Route tasks to the right GSD command: /gsd:quick for small fixes and ad-hoc tasks, /gsd:debug for bug investigation, /gsd:ship for PR shipping pipeline, /gsd:capture for capturing ideas and notes, /gsd:progress to check project status.'
